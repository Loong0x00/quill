# T-0406 audit 报告

日期: 2026-04-25
reviewer: reviewer-T0406 (Opus 1M, ASCII name, per-ticket fresh agent, sub-agent)
worktree: /home/user/quill-impl-atlas (feat/T-0406)
commits 审 (3):
- 51f22a2 T-0406: claim by writer-T0406
- e08e883 T-0406: glyph atlas clear-on-full 替换 panic / anyhow Err (KISS, 非真 LRU)
- 137cfa4 T-0406: Status claimed -> in-review

**判定**: pass (P0 零阻塞, P2 观察 1 + P3 跟进 1, 均不阻塞)

---

## 4 门状态 (release 独立复跑)

```
$ cd /home/user/quill-impl-atlas

$ cargo build --release
Finished `release` profile [optimized] target(s) in 0.07s ✅

$ cargo clippy --all-targets -- -D warnings
Finished `dev` profile [optimized + debuginfo] target(s) in 9.65s ✅ (零 warning)

$ cargo fmt --check
(零输出) ✅

$ cargo test --release
running 81 tests (unit lib) → ok
running 5 tests (unit main) → ok
running 2 tests (atlas_clear_on_full) → ok ← T-0406 新加
    - atlas_full_triggers_clear_no_panic ✅
    - atlas_clear_then_renders_visible_pixels ✅
running 2 tests (cjk_fallback_e2e) → ok
running 2 tests (frame_stats_integration) → ok
running 3 tests (frame_stats_smoke) → ok
running 2 tests (glyph_atlas) → ok
running 3 tests (headless_screenshot) → ok
running 2 tests (ls_la_e2e) → ok
running 2 tests (pty_calloop_smoke) → ok
running 2 tests (pty_echo) → ok
running 3 tests (pty_resize) → ok
running 2 tests (pty_to_term) → ok
running 2 tests (resize_chain) → ok
running 11 tests (state_machine) → ok

TOTAL: 86 unit + 38 integration = 124 tests pass ✅
(派单 Acceptance "总测试 122 + 1~2 ≈ 123-124" — 实测 124 命中精确上界)
```

**4 门全绿** ✅。

---

## 视觉 verify (T-0408 三源 PNG SOP)

### 写码-T0406 自报 (commit body + 派单偏离 #3)

writer 跑 `/tmp/T-0406-baseline.png` (基线 prompt 路径, 46 KB, 不破 atlas baseline) 但
**没跑独立的"喂大字符集 → atlas 满 → PNG"** (派单 In #D 走该路径需改 main.rs 加
`--headless-fill-atlas` flag, 派单 Out 禁动 main.rs)。集成测试
`atlas_clear_then_renders_visible_pixels` 端到端覆盖等价视觉断言 (clear 后 'hello world'
仍渲染 >= 300 非 clear-color 像素)。

### Lead 已 Read /tmp/T-0406-baseline.png (派单上下文)

派单上下文 Lead 已视觉验过 baseline 路径 prompt 不破。

### reviewer 第三方独立验 (本审码独立复跑)

```
$ ls -la /tmp/T-0406-baseline.png /tmp/T-0406-reviewer.png
.rw-r--r-- user wheel 46 KB Sat Apr 25 20:16:57 2026 /tmp/T-0406-baseline.png   (writer)
.rw-r--r-- user wheel 46 KB Sat Apr 25 20:20:41 2026 /tmp/T-0406-reviewer.png   (reviewer)

$ cargo run --release -- --headless-screenshot=/tmp/T-0406-reviewer.png
... TextSystem::new: locked PREFERRED monospace face primary_face=DejaVu Sans Mono
... TextSystem::new: emoji face blacklist populated emoji_face_count=1
... wgpu adapter (headless) 选中 backend=Vulkan name=NVIDIA GeForce RTX 5090
... render_headless stats cell_vertex_count=90 glyph_vertex_count=90 atlas_count=12
... headless screenshot 写出 PNG 完成 path=/tmp/T-0406-reviewer.png bytes=7680000
```

Read 两 PNG 视觉对比:
- **writer baseline**: 1600×1200 PNG, 第 1 行 `[user@userPC ~]$` 浅灰 #d3d3d3 fg + cell
  black bg + 深蓝 #0a1030 clear color, 字形清晰 (T-0407 D fixup 路径)
- **reviewer 独立跑**: 1600×1200 PNG, 第 1 行 `[user@userPC ~]$` 浅灰 + 深蓝 + cell black,
  与 writer baseline 视觉完全一致

trace 数学也对: cell_vertex_count=90 / glyph_vertex_count=90 / atlas_count=12 — 与
T-0405 / T-0407 / T-0408 audit 同源 (15 ASCII 字符 prompt × 6 vertices/cell = 90, atlas
存 12 unique glyph: `[user@PC~]$ ` + 空格)。

**三源一致**: writer ✅ Lead ✅ reviewer ✅, T-0408 三源 PNG verify SOP 任一不一致即 P0
— 三源全一致, baseline 路径不破 ✅

### 集成测试覆盖"atlas 满后视觉" (替代 In #D PNG)

`atlas_clear_then_renders_visible_pixels` 走端到端: pass 1 灌 16000 不同 CJK glyph 必触
atlas 满 → clear; pass 2 ASCII "hello world" 单行渲染, region 400×60 内非 clear-color
像素 >= 300 (实测通过)。这是 In #D "PNG 验 atlas 满后视觉一致" 的等价语义 (不走 PNG 编
码路径, 直接验 RGBA 像素分布), 派单 Out 禁动 main.rs 时 ROI 高于改 main flag + 跑 PNG。

---

## 代码改动审视

### allocate_glyph_slot panic → clear-on-full (派单 In #A) ✅

`src/wl/render.rs:1444-1453` 实装:

```rust
if atlas.cursor_y + raster.height > ATLAS_H {
    tracing::warn!(
        "glyph atlas full (allocations={}), clearing for re-raster",
        atlas.allocations.len()
    );
    atlas.allocations.clear();
    atlas.cursor_x = 0;
    atlas.cursor_y = 0;
    atlas.row_height = 0;
}
let x = atlas.cursor_x;  // ← fall through, clear 后 cursor=0, 必装得下
```

**4 字段 reset 完整** (allocations / cursor_x / cursor_y / row_height) ✅。
**fall through 走下方 shelf packing** (line 1454 `let x = atlas.cursor_x`) ✅。
**不 clear texture / 不动 bind_group / view / sampler** (zero GPU resource churn) ✅。
**注释 4 段** (why clear-on-full 不是 LRU / why 不 clear texture / 跨帧 cache 失效后果),
conventions §2 "B. 引派单 + INV + KISS 路径" 命中 ✅。

### render_headless anyhow Err → clear-on-full (writer 偏离 #1) ✅ 接受

`src/wl/render.rs:1801-1809` 实装:

```rust
if atlas_cursor_y + raster.height > ATLAS_H {
    tracing::warn!(
        "headless glyph atlas full (allocations={}), clearing for re-raster",
        allocations.len()
    );
    allocations.clear();
    atlas_cursor_x = 0;
    atlas_cursor_y = 0;
    atlas_row_height = 0;
}
```

与 Renderer 路径**字面对齐** (同 4 字段 reset, 同 tracing::warn! 模板仅加 "headless"
prefix), 注释引"派单 In #D acceptance + 派单'大量字符变化不会 panic'" traceability ✅。

**接受偏离理由**:
- 派单 In #A 字面只点名 Renderer::allocate_glyph_slot, 但 render_headless 内有
  **同源 atlas 满路径** (anyhow Err, T-0403 起手)
- 不同改: In #D 集成测试灌 16000 CJK glyph 时 render_headless 必 anyhow Err 退出,
  **集成测试无法验"atlas 满后仍 render"** (派单 C 推荐选项之一)
- 两路径在派单允许的单文件 src/wl/render.rs 内, **不破 Out 段**
- writer 主动告知 (commit body 偏离段写明) + 注释引派单 acceptance, conventions §5
  "派单 vs 实装对照, 不静默偏离" 命中

### 集成测试覆盖度 (派单 In #C) ✅

`tests/atlas_clear_on_full.rs` (210 行新文件, 2 集成测试):

**`atlas_full_triggers_clear_no_panic`**:
- 灌 200×80 = 16000 不同 CJK glyph (U+4E00 起步) → atlas ~4400 槽位上限 → 必触
  clear-on-full 2-3 次
- 单次 render_headless 走完, expect 不 panic / 不 Err
- 验 RGBA 长度 = physical_w × physical_h × 4

**`atlas_clear_then_renders_visible_pixels`**:
- Pass 1: 灌 16000 CJK 触 clear (本身已 fn 1 覆盖, 这里走 don't panic)
- Pass 2: ASCII "hello world" 单行渲染, region 400×60 内非 clear-color 像素 >= 300
- 验"灌满 atlas 不污染 TextSystem 内部状态" (cosmic-text fontdb / SwashCache 不 corrupt)

**why 端到端而非 cfg(test) accessor** (writer 注释 line 1-19 + 偏离 #2 commit body):
- GlyphAtlas 模块私有 + 含 wgpu Texture/BindGroup (INV-010 类型隔离禁外露)
- cfg(test) accessor 需 `pub(crate)` 字段或 helper, 引入测试专属 API surface 不优
- 端到端"render 不 panic + RGBA 合法 + clear 后仍渲染像素" 间接锁住 clear 路径正确性

测试 `expect()` 在测试路径允许 (conventions §3 "真 IO 测试允许 std::thread::sleep +
expect/unwrap") ✅。

---

## 偏离审视 (3 项, writer 主动告知)

### 偏离 #1 render_headless 同改 (anyhow Err → clear-on-full)

**派单字面**: In #A 仅点名 `Renderer::allocate_glyph_slot` 的 panic 路径。

**writer 实装**: 顺手对齐 render_headless 内同源 atlas 满路径 (anyhow Err → clear)。

**独立判**: ✅ **接受** — 同文件内 (派单 Out 禁的是 src/text / src/pty / src/main.rs /
docs/invariants.md / Cargo.toml, render.rs 是派单 In 主战场)。两路径不对齐则集成测试
**无法验 In #C** (灌 16000 CJK 必走 render_headless 路径, anyhow Err 直接退出)。语义
对齐合理, writer 主动 commit body 告知 + 注释引派单 acceptance, conventions §5 命中。

### 偏离 #2 集成测试走端到端 render_headless (派单 C option 1)

**派单字面**: C "或单元测试形式 (在 src/wl/render.rs 内 #[cfg(test)] mod): 直接构造
GlyphAtlas + 灌满 + 验 clear 行为"。

**writer 实装**: 走端到端 render_headless (派单 C 推荐选项之一), 不走 cfg(test)
accessor。

**独立判**: ✅ **接受** — 派单 C 给两选项任选, writer 选端到端有论据:
- GlyphAtlas 模块私有 + 含 wgpu Texture/BindGroup, cfg(test) accessor 需暴露内部状态
- wgpu 资源构造昂贵 (request_adapter + device + queue + texture), 单测路径需 mock
- 端到端"render 不 panic + RGBA 合法 + clear 后仍渲染像素" 完整覆盖 clear 路径
- ROI 高于 cfg(test) accessor (后者维护成本 + INV-010 边界混淆)

### 偏离 #3 In #D 没跑独立 PNG (atlas 满后视觉一致)

**派单字面**: D (可选) "cargo run --release -- --headless-screenshot=/tmp/atlas_clear.png
跑 1 次 + 加 PtyHandle 喂大量字 → atlas 满 → clear, Lead Read PNG verify 字仍正常"。

**writer 实装**: 跑了基线 PNG (`/tmp/T-0406-baseline.png`) 确认 prompt 路径不破; 真"喂大
量字 → atlas 满 PNG 路径"需改 main.rs 加 `--headless-fill-atlas` flag (派单 Out 禁动
main.rs), 没做。集成测试 `atlas_clear_then_renders_visible_pixels` 端到端覆盖等价语义。

**独立判**: ✅ **接受** — 派单 D 标"(可选)", 派单 Out 禁动 main.rs 直接堵死 D 字面路径
(In #D 字面要求改 main 喂大量字)。writer 用集成测试覆盖等价语义 (clear 后仍渲染 >= 300
像素) 是合理替代; reviewer 第三方跑 baseline PNG 也确认 baseline 路径不破。如果要严格
跑 D 字面, 需先开新 ticket 改 main 加 flag, 不在本 ticket scope。

---

## 硬约束 verify

### INV-001..010 全维持 ✅

```
$ git diff main..HEAD -- docs/invariants.md
(零输出) ✅ INV 文件未动

$ git diff main..HEAD -- src/wl/window.rs
(零输出) ✅ INV-001 (State 字段顺序) 不破

$ git diff main..HEAD -- src/wl/render.rs | grep -E '^[+-]\s*(surface|cell_|glyph_|device|queue|config|clear|surface_is_srgb|instance|allocations|cursor_x|cursor_y|row_height|bind_group|texture|view|sampler):'
(零输出) ✅ INV-002 Renderer 14 字段顺序 + GlyphAtlas 内部 6+ 字段顺序未动

$ git diff main..HEAD -- src/pty/
(零输出) ✅ INV-008 + INV-009 PTY 不变式不破
```

**INV 详细**:
- INV-001 State 字段顺序 ✅ (T-0406 不改 src/wl/window.rs)
- INV-002 Renderer / GlyphAtlas 字段顺序 ✅ (T-0406 仅改 atlas 内部 allocations
  HashMap **元素** + cursor 数值 reset, 不改字段名 / 顺序 / 类型)
- INV-003..009 不涉及 ✅ (T-0406 不动 unsafe / forbid / calloop / WindowCore /
  resize_dirty / PtyHandle)
- INV-010 类型隔离 ✅ (5 grep 全零真命中, 详下)

### 派单 Out 不动 ✅

```
$ git diff main..HEAD -- src/text/mod.rs src/pty src/main.rs docs/invariants.md Cargo.toml Cargo.lock
(零输出) ✅
```

派单 Out 字面: "src/text/mod.rs / src/pty / src/main.rs / docs/invariants.md / Cargo.toml"
— 全部未动 ✅

### 不引新 crate ✅

```
$ git diff main..HEAD -- Cargo.toml Cargo.lock
(零输出) ✅
```

`tests/atlas_clear_on_full.rs` 仅 `use quill::term::{CellPos, CellRef, Color}; use
quill::text::TextSystem; use quill::wl::{render_headless, HIDPI_SCALE};` — 全 quill 公共
API 路径, 零新 dep ✅。

### INV-010 type isolation 维持 ✅

```
$ grep -nE 'pub use (alacritty|wgpu|wayland|cosmic)' src/wl/render.rs tests/atlas_clear_on_full.rs
(零命中) ✅

$ grep -nE 'impl From<(alacritty|wgpu|wayland|cosmic)' src/wl/render.rs tests/atlas_clear_on_full.rs
(零命中) ✅

$ grep -nE 'cosmic_text|wgpu::|fontdb|alacritty' tests/atlas_clear_on_full.rs
4://! struct (含 `wgpu::Texture` / `BindGroup` 等, INV-010 类型隔离禁外露)。  ← doc 反例
139:/// 验"灌满 atlas 不污染 TextSystem 内部状态" (cosmic-text fontdb / SwashCache 不
                                                       ↑ doc comment 描述
                                                       ✅ 0 真命中, 2 doc 假阳同 T-0407 P0-1
```

**INV-010 第 12 次应用零真违规** (T-0302 / 0303 / 0304 / 0305 / 0306 / 0399 / 0401 /
0402 / 0403 / 0407 / 0405 / **0406**) ✅

### INV-002 字段顺序 ✅

`src/wl/render.rs` diff 字段声明区零变化, `Renderer` / `GlyphAtlas` struct 字段顺序
14 + 6 维持 (详 INV verify 段)。**T-0406 仅改 atlas.allocations HashMap 内 K=>V 元素 +
cursor_x/y/row_height 数值 reset, 不改字段定义** — 字段顺序契约不涉及 ✅。

### 生产零 unwrap / expect / println / unsafe / thread::spawn ✅

```
$ grep -nE 'unwrap\(\)|\.expect\(|println!|thread::spawn' src/wl/render.rs
(零命中) ✅
```

`tests/atlas_clear_on_full.rs` 用 `.expect()` — test 路径允许 (conventions §3) ✅。

### Commit message 风格 (conventions §1) ✅

- `51f22a2` claim commit: 标题 ASCII writer name + ≤ 70 char ✅
- `e08e883` 主体 commit: HEREDOC 单引号 + What/Why/副作用/测试 4 段齐 + diff stat + 引
  派单 In #A/B/C/D + 主动告知 3 偏离 + INV-002 / INV-010 / 不引新 crate verify ✅
- `137cfa4` Status commit: 单文件 ±2 行 (Status + Assigned), 符合 conventions §4 Step 4
  "Status 变更独立 commit" 模板 ✅

---

## 派 P 列表

### P0 (阻塞)
- 无 ✅

### P1 (主合后跟进)
- 无 ✅

### P2 (低优观察, 不阻塞)

**[P2-1] clear-on-full 跨帧 cache 失效后的 50ms hiccup 实测**

writer 注释 line 1442-1443 估"~1920 个 ASCII 满屏在 17pt × HIDPI 下, ~50ms 量级 hiccup"
是理论估算, 未实测。Phase 4 atlas 容量 (2048² / 2700-10000 槽位 @ HiDPI) + 终端字符集
稳定性 (~ASCII 95 + 常用 CJK 几千) 让满几乎不发生, **不阻塞**, 但 Phase 6 soak 1h
测试若发现 clear 频繁触发 (RUST_LOG=warn 看 `glyph atlas full` 出现频率) 需重审是否升级
真 LRU; 当前 KISS 等价物在派单 acceptance 内。

### P3 (nice-to-have)

**[P3-1] In #D PNG verify atlas 满路径升级路径明确**

派单 In #D "(可选)" 字面要求改 main.rs 加 `--headless-fill-atlas` flag 跑独立 PNG, 派单
Out 禁动 main.rs 致 D 字面路径堵死。writer 用集成测试 `atlas_clear_then_renders_
visible_pixels` 端到端覆盖等价语义 (clear 后仍渲染 >= 300 非 clear-color 像素), 已通过。

未来若 Phase 6 想加 PNG baseline 比对 atlas 满路径视觉, 升级路径明确:
- 开新 ticket T-0506 (或类似) 加 main.rs `--headless-fill-atlas=N` flag (喂 N 个不同
  CJK char), 走独立 PNG → Lead Read 比对 baseline
- 当前 ticket scope 不涉及, **不阻塞**

---

## 写码偏离 vs 派单 总览 (conventions §5 主动告知 verify)

| # | writer 偏离 | 派单字面 | reviewer 独立判 |
|---|---|---|---|
| 1 | render_headless 同改 anyhow Err → clear-on-full | In #A 仅点名 Renderer::allocate_glyph_slot | 接受 — 派单 Out 不破 (同文件), 不改无法验 In #C |
| 2 | 集成测试走端到端 render_headless | C 选项 1 / 选项 2 任选 | 接受 — 派单 C 推荐路径之一, INV-010 边界更干净 |
| 3 | In #D 没跑独立 PNG | D "(可选)" 喂大字符 PNG | 接受 — 派单 Out 禁 main.rs 堵死字面路径, 集成测试覆盖等价语义 + reviewer baseline PNG 三源 verify atlas baseline 路径不破 |

**3 项偏离全合理, 主动告知 + 注释 traceability 完整** ✅

---

## 给后续 ticket 的前置信息

### 给 Phase 5 起步 (输入 / IME / scrollback 等)

- T-0406 关闭 Phase 4 最后一单, atlas 容量满路径 KISS 完结 (派单"clear-on-full 不是真
  LRU, ROADMAP 命名沿用历史" 已落地)
- atlas 容量监控点: RUST_LOG=warn 看 `glyph atlas full (allocations=N)` 出现频率, Phase
  6 soak 1h 若频繁 (>1次/小时) 重审升级真 LRU
- 集成测试覆盖度: tests/atlas_clear_on_full.rs 加在 124 tests 序列尾 (122 → 124),
  Phase 5 加新 ticket 时 baseline 124

### 给 mainline-audit 下次跑 (Phase 4 末)

- INV-010 类型隔离 SOP 第 12 次应用零真违规 (沿袭统计)
- INV-001..009 全维持
- 总测试 122 → 124 (T-0406 +2 集成测试 atlas_clear_on_full)
- T-0406 视觉验收: writer baseline + reviewer 第三方独立跑 PNG 三源一致, atlas baseline
  路径不破; 集成测试 atlas_clear_then_renders_visible_pixels 等价覆盖 atlas 满后视觉
  (clear 后 >= 300 非 clear-color 像素)

---

## 整体评价

T-0406 是 Phase 4 收尾最后一单 — **跨 1 文件改 + 1 文件加 / 2 commits / +69/-24 +210
行 diff** (符合 conventions §1 "diff < 300 行"; 派单 budget 50k 适当)。**派单 In #A
(allocate_glyph_slot panic → clear) + In #B (doc 同步) + In #C (集成测试 ≥1) 全落地,
In #D (可选) 用集成测试覆盖等价语义 + reviewer baseline PNG 三源 verify 替代字面 PNG
路径, Out 清单全维持, 4 门绿 124 tests, INV-010 第 12 次应用零真违规, INV-001..009 全
维持**。

**关键架构判断 — render_headless 同改 (writer 偏离 #1) 接受**: 派单 In #A 字面只点
allocate_glyph_slot, 但 render_headless 内有同源 atlas 满路径 (anyhow Err); 不同改则
集成测试 `atlas_full_triggers_clear_no_panic` 灌 16000 CJK glyph 时必 anyhow Err 退出,
**无法验 In #C**。两路径在派单允许的单文件 src/wl/render.rs 内, 不破 Out 段。语义对齐
合理, writer 主动 commit body 告知 + 注释引派单 acceptance, conventions §5 "派单 vs
实装对照, 不静默偏离" 命中。

**视觉验收三源齐**: writer 跑 baseline PNG (atlas baseline 路径不破) + Lead 已 Read
(派单上下文) + reviewer 第三方独立跑 `/tmp/T-0406-reviewer.png` (1600×1200 PNG, 第 1 行
`[user@userPC ~]$` 浅灰 fg + 深蓝 bg + cell black, 与 writer baseline 视觉完全一致),
T-0408 三源 PNG SOP 任一不一致即 P0 — 三源全一致, 通过 ✅。trace 数学 (cell 90 / glyph
90 / atlas 12) 与 T-0405 / T-0407 / T-0408 同源, 15 字符 prompt 路径不破。

**clear-on-full 实装审视**: 4 字段 reset 完整 (allocations / cursor_x / cursor_y /
row_height), fall through 到下方 shelf packing, 不 clear texture / 不动 bind_group /
view / sampler (zero GPU resource churn, 派单关键约束全守); render_headless 同源路径
字面对齐 (同 4 字段 reset, 同 tracing::warn! 模板); 注释 4 段 (why clear-on-full 不是
LRU / why 不 clear texture / 跨帧 cache 失效后果) traceability 完整。

**P3 跟进**: 1 项不阻塞 (In #D PNG 字面路径堵死, 升级路径明确). P2 观察 1 项 (Phase 6
soak 重审 clear 触发频率).

**Lead 决策**: ✅ **批准合并** (3 commits 保留 / Option C squash 单 commit, Lead 选)。
