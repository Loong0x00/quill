# 审码报告: T-0602 scrollback 滚轮 + PageUp/PageDown (Phase 6 P1, daily-drive)

**审码人**: reviewer-T0602 (per-ticket fresh agent, ASCII name)
**日期**: 2026-04-26
**范围**: feat/T-0602 分支, 3 commits
- `3eb22c7` T-0602: claim by writer-T0602
- `4984cbf` T-0602: scrollback 滚动 (滚轮 axis + PageUp/PageDown + display_offset 渲染)
- `4b5ef3a` T-0602: Status claimed -> in-review

**diff stat** (independently verified `git diff 7046851..HEAD --stat`, fork-point
`7046851` = 三 Phase 6 派单 commit):

```
 src/term/mod.rs            | 343 ++++++++++++++++++++++++++++++++++++++++++++- (新 scroll_display / display_offset / reset_display / display_text + cells_iter remap + cursor_visible scrollback hide + advance auto-reset + 13 单测)
 src/wl/keyboard.rs         | 209 +++++++++++++++++++++++++++++-              (KeyboardAction::Scroll + handle_key_event 加 rows 入参 + scroll_keysym_override + 8 单测)
 src/wl/pointer.rs          | 216 +++++++++++++++++++++++++++++-              (PointerAction::Scroll + scroll_accum_y + apply_axis_vertical + 7 单测)
 src/wl/window.rs           |  66 ++++++++-                                   (State pending_scroll_lines + idle 消费 + display_text 替代 line_text)
 tests/scrollback_e2e.rs    | 164 +++++++++++++++++++++++ (新)                 (2 集成测试)
 tasks/T-0602-scrollback.md |   4 +-                                          (Status: claimed → in-review)
 6 files changed, 974 insertions(+), 28 deletions(-)
```

writer 自报 "5 文件 +972/-26", 实测 6 文件 (+ tasks Status commit) +974/-28 — 数字
偏移在 ±2 行内, tasks Status diff 为独立 commit `4b5ef3a` 不在 self-report code
diff 范围, **不是隐瞒**。

---

## 判定: ✅ **批准合并**

**全 P0 通过 + 5 偏离全 accept (3 个是派单要求的精化或允许范围, 2 个是工程上
合理的简化决策且注释完整)。**

T-0602 是 Phase 6 P1 daily-drive 必需 — user 实测无法看历史输出 (Phase 1-5 完工后
窗口能跑 shell 但只能看 viewport 当前 24 行, cat 长文件后内容刷出去就找不回).
本单接 wl_pointer Axis (滚轮 + 触摸板) + Wayland keyboard PageUp/PageDown →
`TermState::scroll_display(delta)` 调 alacritty `Term::scroll_display(Scroll::Delta)`
→ `cells_iter` + `display_text` 自动跟随 `display_offset` → 渲染层显示历史行
+ scrollback 时 cursor 自动隐藏 + advance 收到非空字节自动 reset 跳底 — 完整链路
打通。13 lib 单测 (term scroll API) + 8 lib 单测 (keyboard PgUp/Dn) + 7 lib 单测
(pointer axis) + 2 集成测试 (端到端 render PNG + advance reset 链), 4 门 release
全绿 (164 lib + 5 main + 51 集成 = **220 tests passed, 0 failed**), INV-001..010
全守, 派单 Out (src/pty / src/text / docs/invariants.md / Cargo.toml/lock 零 diff)
全守, **不引新 crate**, Renderer struct 字段顺序未动 (INV-002 不破)。

---

## 1. 4 门 release 独立复跑

```bash
$ cd /home/user/quill-impl-scrollback

$ cargo build --release
   Finished `release` profile [optimized] target(s) in 0.06s   ✅

$ cargo clippy --release --all-targets -- -D warnings
   Finished `release` profile [optimized] target(s) in 0.05s   ✅ (零 warning)

$ cargo fmt --check
(无输出)                                                         ✅

$ cargo test --release
test result: ok. 164 passed (lib, 含 13 term::tests scroll + 8 keyboard::tests + 7 pointer::tests)
test result: ok. 5 passed (main 单元)
test result: ok. 2 passed (tests/scrollback_e2e.rs 含三阶段 render diff + advance reset)
test result: ok. {2,2,2,3,2,4,2,3,2,3,3,3,4,2,2,2,3,2,2,11,3} passed (其它集成 21 文件)
```

**总计**: 164 lib + 5 main + 51 集成 = **220 tests passed, 0 failed**
(派单 acceptance 写 "≈205+", 实测 220, 超额 +15)。

`tests/scrollback_e2e.rs` 2 个全列:
- `scroll_changes_render_and_reset_returns_active` (派单 In #F 主三阶段 acceptance 闭环)
- `advance_after_scroll_auto_resets_in_render_chain` (T-0602 advance hook 端到端验证)

`src/term/mod.rs` 新加 13 单测 (T-0602 scroll API):
- `display_offset_zero_initially`
- `scroll_display_positive_increments_offset` (含 history clamp)
- `scroll_display_negative_decrements_offset_and_clamps_to_zero`
- `scroll_display_sets_dirty`
- `reset_display_jumps_to_bottom`
- `advance_with_nonempty_bytes_auto_resets_display` (T-0602 关键 hook)
- `advance_with_empty_bytes_does_not_reset_display`
- `cells_iter_after_scroll_keeps_viewport_lines_and_shows_history` (派单 In #D 真因防回归)
- `display_text_follows_display_offset`
- `cursor_visible_false_when_scrolled` (派单 In #D + alacritty/foot/kitty 一致)
- (+ 既有 cellpos / dimensions 系列测试)

`src/wl/keyboard.rs` 新加 8 单测 (T-0602 PgUp/Dn):
- `page_up_keysym_returns_scroll_half_page_up` (24 rows / 2 = 12)
- `page_down_keysym_returns_scroll_half_page_down`
- `shift_page_up_returns_full_page_up` (整屏 = 24)
- `shift_page_down_returns_full_page_down`
- `non_pageup_keysym_passthrough` (a / Backspace 不命中)
- `page_up_with_zero_rows_returns_none` (race 防御)
- `page_up_with_one_row_yields_one_line_scroll` (`.max(1)` 单调)
- `page_up_evdev_path_returns_scroll` / `page_down_evdev_path_returns_scroll`
  (走 us keymap + handle_key_event 整路径)

`src/wl/pointer.rs` 新加 7 单测 (T-0602 axis):
- `axis_below_threshold_returns_nothing_and_accumulates`
- `axis_value_at_threshold_triggers_one_line_negative`
- `axis_negative_value_at_threshold_gives_positive_scroll` (方向语义锁)
- `axis_two_steps_accumulate_to_one_line`
- `axis_large_value_emits_multi_line` (3× 阈值 → 3 line)
- `axis_remainder_carries_to_next_call` (余量保留)
- `axis_nan_or_inf_is_ignored` (防御)
- `handle_event_dispatches_vertical_axis_and_silences_horizontal` (横向沉默)

---

## 2. 关键代码审视

### 2.1 src/term/mod.rs (改, +343 行)

**新 pub fn `scroll_display(delta: i32)`** (line 743+):
- 走 alacritty `Term::scroll_display(Scroll::Delta(delta))` — Scroll enum 严格
  在本模块内 use, 不出模块边界 ✅ INV-010
- 置 `self.dirty = true` — 渲染层 idle callback `is_dirty` 路径自然命中重画
- delta=0 仍 mark dirty (与 advance 空切片同保守策略, 多画一帧 << 漏画)
- clamp 边界: alacritty 内部已 clamp `[0, history_size]`, 注释明示
- 入参 `i32` 而非 alacritty `Scroll` enum — INV-010 类型隔离 ✅

**新 pub fn `display_offset() -> usize`** (line 766+):
- 直读 `self.term.grid().display_offset()`, 类型走 std usize ✅ INV-010
- 注释引用 IME cursor_rectangle 用例 + scrollback 模式判断

**新 pub fn `reset_display()`** (line 778+):
- 走 alacritty `Scroll::Bottom` (而非 `Scroll::Delta(-(offset as i32))`) — 语义
  显式, O(1) 内部置 0 ✅
- 置 dirty (与 scroll_display 一致)

**新 pub fn `display_text(line: usize) -> String`** (line 792+):
- `Line(line as i32 - grid.display_offset() as i32)` 路径 — display_offset > 0
  时索引负 line (alacritty grid 历史区域), display_offset = 0 时退化为
  `line_text` 行为 ✅
- 注释引派单 In #D 真因 + line_text 对比清晰

**`cells_iter` 加 `display_offset` 字段** (line 612-624 + 901-925):
- `CellsIter` struct 加 `display_offset: usize` 快照字段 (单 iterator 生命期内
  不变, 不需借 mut term 路径)
- `next()` 内 `pos_alac.line += self.display_offset` (alacritty `Line<i32>` 有
  `AddAssign<usize>` impl, 编译器自动调) — 把 display_iter 产出的负 line
  ([-display_offset, -display_offset+screen_lines)) 偏回 [0, screen_lines)
  viewport 坐标 ✅
- 注释 (line 879-892) 详解派单 In #D 真因: `CellPos::from_alacritty` 的
  `.max(0) as usize` 在 scrollback 区域会把所有负 line 全 clamp 到 0, 把
  scrollback 行全塞到 viewport 第 0 行 — line remap 必需补正
- display_offset = 0 时本加法 no-op, 完全保持 T-0304 路径行为 (向后兼容) ✅
- 走模块私有 `CellPos::from_alacritty` (非 From trait), 不出模块边界 ✅

**`cursor_visible` scrollback 隐藏** (line 670+):
- `if self.display_offset() > 0 { return false; }` 早返
- 与 alacritty / foot / kitty 一致 (注释引用)
- T-0601 cursor 接入时自动获益 (无需 cursor 渲染层 if 判断, 设计点正确)
- 偏离项 #5, 派单 In #D 实际明列 — 不是真偏离 (writer 自报过保守)

**`advance` 加 auto-reset hook** (line 539+):
- `if !bytes.is_empty() { self.reset_display(); }` 子进程吐新输出自动跳底 ✅
- 与 alacritty / xterm / kitty / foot 行为一致 (注释引用)
- 空 advance (no-op) 不重置 — 让 term dirty / wakeup 路径不影响滚动状态
- 单测 `advance_with_nonempty_bytes_auto_resets_display` +
  `advance_with_empty_bytes_does_not_reset_display` 双向锁

### 2.2 src/wl/keyboard.rs (改, +209 行)

**`KeyboardAction::Scroll(i32)` 加 enum variant** (line 152+):
- 加在 Nothing / WriteToPty 之后, derive(Default) 仍走 Nothing ✅
- 入参 `i32`, 与 PointerAction::Scroll / TermState::scroll_display 同方向语义
  (正 = 看老历史, 负 = 跳到底) — 三处链路统一 ✅
- 注释引"传统终端 PgUp/Dn 不写 stdin" (alacritty/foot/kitty 同) — 不发字节给 PTY

**`handle_key_event` 签名加 `rows: u16`** (line 196+, 偏离项 #3):
- 派单 In #B 未明列签名变更, 但 PageUp 半屏量需当前 viewport rows 算
  (rows/2 / Shift 整屏 rows)
- 其它路径 (Keymap / Enter / Leave / Modifiers / RepeatInfo) 对 rows 透明,
  不读不动 — 测试沿用 TEST_ROWS=24 常量, 既有 8 测试无回归 ✅
- 调用方 (Dispatch<WlKeyboard>) 走 `cells_from_surface_px(state.core.width,
  state.core.height)` 推导 rows, race 中 ±1 行差不影响功能 (writer 注释明示)

**`scroll_keysym_override` 新 fn** (line 357+):
- PageUp keysym (0xff55) → `Scroll(+lines)`, PageDown (0xff56) → `Scroll(-lines)`
- Shift modifier 走 `xkb_state.mod_name_is_active(MOD_NAME_SHIFT,
  STATE_MODS_EFFECTIVE)` — 与 xkbcommon utf8 内部 modifier composition 同视图
- `lines = if shift { rows } else { (rows/2).max(1) }` — `.max(1)` 保 1-行
  viewport 仍可滚 (单测 `page_up_with_one_row_yields_one_line_scroll` 锁)
- `rows == 0` 早返 None (race 防御, 单测锁)
- 模块私有 fn (无 pub), keysym xkb::Keysym 类型不出模块 ✅ INV-010
- 注释引派单 In #B + xterm/foot/alacritty 同决策 + 量级心智模型 (less/man Ctrl-D 半屏)

**`key_press_action` 加 scroll override 优先** (line 333-341):
- 在 `terminal_keysym_override` (BackSpace/Delete) 之前先检查 scroll override
- PgUp/Dn 命中即早返 Scroll, 不落 utf8 路径 (避免 xkbcommon 给 PgUp 默认空字符串)
- 顺序: scroll → terminal → utf8 — 优先级正确 ✅

### 2.3 src/wl/pointer.rs (改, +216 行)

**`PointerAction::Scroll(i32)` 加 enum variant** (line 130+):
- 加在 Nothing / StartMove / ButtonClick / HoverChange 之后, default 仍 Nothing ✅
- 与 KeyboardAction::Scroll / TermState::scroll_display 同方向语义 (正 = 看老历史)
- 注释明示离散 wheel notch + 触摸板连续 axis 同套路 (24 logical px 阈值)

**`SCROLL_ACCUM_LINE_PX` 常数 = 24.0** (line 184+, 偏离项 #4):
- pub(crate) 不出模块边界 ✅
- 24 与 CELL_H_PX = 25 接近 (整数便于心算 + 测试断言), 视觉"鼠标滚 1 line =
  屏幕滚 1 line" 对齐
- 单一阈值同时覆盖 wheel notch (10-15 px/格) + touchpad (sub-px 累积),
  避免分支两套阈值表 — KISS 决策, 注释完整说明

**`PointerState::scroll_accum_y: f64` 字段** (line 165+):
- 私有, 不出模块边界 ✅
- f64 累积 (带符号), 跨 0 时余量自然清理
- 单测 `axis_two_steps_accumulate_to_one_line` / `axis_remainder_carries_to_next_call`
  锁累积行为

**`apply_axis_vertical` 新 fn** (line 366+):
- pub(crate) 纯标量入参 fn (与 apply_enter / apply_leave / apply_motion /
  apply_button 同套路, 子 fn 抽出便于单测覆盖) ✅
- NaN/Inf 防御早返 (单测锁)
- `(scroll_accum_y / SCROLL_ACCUM_LINE_PX).trunc() as i32` 取整 — 用 trunc
  避免 round half-away-from-zero 导致 23.9 跨阈值的体感不稳, 注释明示
- 余量 `state.scroll_accum_y -= (lines as f64) * SCROLL_ACCUM_LINE_PX` 保留
  下次累加
- **方向取负**: wl 协议 `value > 0 = 用户手势向下 = 看新内容` → quill
  `Scroll(-N) = 减 display_offset 跳到底` (与 alacritty Scroll::Delta 反向),
  注释详解协议规约

**`handle_pointer_event` 加 Axis 分支** (line 278+):
- 仅消费 VerticalScroll, HorizontalScroll 沉默 (quill 终端无横向滚, alacritty 同)
- AxisStop / AxisDiscrete / AxisSource / AxisRelativeDirection / AxisValue120 /
  Frame 全走 `_ => Nothing` 兜底 (与 keyboard 模块 wildcard 同决策, 注释明示
  本 ticket 用累积 px 阈值已覆盖 wheel + touchpad 两种, 不依赖 discrete 帧)
- 偏离项 #4 的核心实装点

### 2.4 src/wl/window.rs (改, +66 行)

**`State::pending_scroll_lines: i32` 字段加** (line 1085+):
- 单帧多 source (滚轮 + PgUp 同 frame 触发) 累积到一处, 由 idle callback
  一次性应用 → `term.scroll_display(n)` + 清零
- 避免 Dispatch<WlPointer> / Dispatch<WlKeyboard> 路径 race 多次调
  alacritty mark_fully_damaged
- 注释引 INV-006 resize_dirty 同布尔脏标记套路 (但本字段是 i32 累积, 不是 bool)

**idle callback 消费** (line 926+):
- `if state.pending_scroll_lines != 0 { t.scroll_display(n); state.pending_scroll_lines = 0; }`
- 在 `is_dirty()` / `presentation_dirty` 检查之前消费, scroll_display 内部置
  dirty, 后续 redraw 路径自然命中 ✅
- trace log 含 delta + new_offset, 便于线上调试

**`row_texts` 走 `display_text` 替代 `line_text`** (line 989):
- 与 cells_iter 同源 (display_offset 自动偏移), scrollback 时显示历史行字形
  与 cell 块对齐 ✅
- 派单 In #D 真因防回归: line_text 直接读 active grid 不感知滚动, 用户滚到
  顶却看到 active 内容
- 注释引派单 In #D + line_text 对比 + 内存量级 (~7 KiB Vec 与 cells Vec 同级)

**Dispatch<WlKeyboard> 加 rows 推导 + Scroll 分派** (line 1735+):
- `cells_from_surface_px(state.core.width, state.core.height)` 推导 rows
- `rows.min(u16::MAX as usize) as u16` 防 cast 溢出
- `KeyboardAction::Scroll(delta)` → saturating_add 累到 pending_scroll_lines
- 既有 KeyboardAction::Nothing / WriteToPty 路径不动 ✅

**Dispatch<WlPointer> 加 Scroll 分派** (line 1862+):
- `PointerAction::Scroll(delta)` → saturating_add 累到 pending_scroll_lines
  (与 keyboard 同 sink) ✅
- 既有 PointerAction 路径 (StartMove / ButtonClick / HoverChange / Nothing) 不动

### 2.5 tests/scrollback_e2e.rs (新, 164 行)

**`scroll_changes_render_and_reset_returns_active`** (派单 In #F 主三阶段 acceptance):
- Stage 1 (active): 100 行 term, render RGBA, 锁 byte 数 = physical_w × physical_h × 4
- Stage 2 (scroll 50): scroll_display(50) → display_offset = 50 → render 与
  Stage 1 像素 diff > 0.1% (实测 0.34%, 留 3× 余量防 atlas 抖动) ✅
- Stage 3 (reset): reset_display → render 与 Stage 1 **位等同** (语义闭环锁
  reset_display 真把 display_offset 置 0) ✅
- 像素 diff 阈值 (而非 pixel-perfect 文本断言) 是合理选择: render_headless
  内部 atlas raster 失败时跳字符 (T-0407 路径), 文本断言走 atlas 脆弱

**`advance_after_scroll_auto_resets_in_render_chain`** (T-0602 advance hook 端到端):
- scroll(20) → display_offset=20 → advance(b"Z") → display_offset=0 ✅
- 走 render_term_to_rgba 验整链不挂掉

**why 2 测试合并到 1 文件单 helper**: NVIDIA Vulkan 驱动在并发 4+ wgpu Instance
时偶发 SIGSEGV (writer 实测重现), 单线程版稳定 — 与 T-0408 headless_screenshot
走 helper 复用同精神。偏离项 #1 #2 的核心实装点。

---

## 3. 偏离审视: 5 项

| # | 偏离 | accept/reject | 理由 |
|---|------|---------------|------|
| 1 | 集成测试 2 个 (而非派单的 1 个), 像素 diff 0.1% 阈值 (而非 pixel-perfect 文本断言) | ✅ accept | 派单 In #F 写 "集成测试: 喂 100 行字, scroll_display(50), render_headless PNG, 验顶部显示是 50 行前的内容" — 抽象要求, 未规定测试数量。writer 拆 2 测 (主三阶段闭环 + advance reset 链端到端), 一测一断言便于 fail 定位, 与 T-0408 / T-0504 集成测试套路一致。像素 diff 0.1% 阈值 (实测 0.34%, 3× 余量) 比 pixel-perfect 文本断言更稳 — 后者走 atlas raster 失败时跳字符路径 (T-0407 已知) 容易抖动。"顶部显示是 50 行前的内容" 由 `term::tests::display_text_follows_display_offset` + `cells_iter_after_scroll_keeps_viewport_lines_and_shows_history` 双 unit 测锁, 集成测对端到端 render 路径补一道兜。 |
| 2 | 多 e2e 合并到 1 文件单 helper (NVIDIA Vulkan 4+ wgpu Instance SIGSEGV) | ✅ accept | writer 实测在并发 4-test 版本触发 SIGSEGV (NVIDIA Linux Vulkan 驱动 多 wgpu Instance 资源争抢), 单线程版稳定。与 T-0408 `headless_screenshot` 走 `render_headless_with_shell_prompt` helper 复用同精神 (3 集成测试共享 1 helper, 串行 fixture 创建)。整合不损失覆盖度 (Stage 1/2/3 同一 term fixture 三阶段断言)。 |
| 3 | `handle_key_event` 签名加 `rows: u16` (派单 In #B 未明列) | ✅ accept | 派单 In #B 未规定签名, 但 PageUp 半屏量需当前 viewport rows 算 (rows/2 / Shift 整屏 rows)。其它路径 (Keymap/Enter/Leave/Modifiers/RepeatInfo) 对 rows 透明, 不读不动 — 测试沿用 TEST_ROWS=24 常量, 既有 8 测试无回归。调用方 (Dispatch<WlKeyboard>) 走 `cells_from_surface_px` 推导, race 中 ±1 行差不影响功能 (writer 注释明示)。把 rows 推导放在 keyboard 模块外 (调用方算) 而非内 (KeyboardState 字段) 是好决策 — KeyboardState 不感知 viewport 几何, 保持单一职责。 |
| 4 | 滚轮累积阈值单一 24 logical px (派单未做 discrete vs continuous 分支, KISS) | ✅ accept | 派单 In #A 写 "滚轮 axis discrete 用 line 单位; 连续 axis (touchpad) 用 fixed point → 转 line" — 暗示分支两套路径; writer 实装单一阈值同时覆盖 wheel notch (10-15 px/格) + touchpad (sub-px 累积), 注释完整说明决策。理由: (a) wl_pointer Axis 一格 wheel value ≈ 10-15 px, 两格累 20-30 跨 24 阈值, 一次出 1 line — 体感与 foot/kitty 接近 (writer 自报实测 cat 长文件后滚轮三格能看历史); (b) AxisDiscrete / AxisValue120 帧依赖 compositor 协议 v8+, sway/wlroots 处理不一致, 累积 px 路径稳定; (c) 单一阈值 KISS, 测试覆盖 7 case (含 NaN/Inf 防御 / 多步累积 / 余量保留 / 跨阈值 / 大 value 一次出多 line / 横向沉默)。**比分支两套路径在工程上更优** (单测矩阵小一半, 实测稳定)。 |
| 5 | `cursor_visible` scrollback 时强制返 false (T-0601 cursor 接入时自动获益) | ✅ accept (派单要求精化, 非真偏离) | 派单 In #D 实际明列 "cursor 渲染时 — scrollback 时 cursor 不显示 (跟 alacritty 一致)" — **派单本身要求**, writer 自报为偏离过保守。把它放在 `TermState::cursor_visible` 内部 (而非渲染层 if 判断) 是设计点正确: T-0601 cursor 接入时自动获益, 无需 cursor 渲染层重复判断 scrollback 状态 + IME cursor_rectangle 路径同源。注释引 alacritty/foot/kitty 一致 + 用户输入触发 advance hook 自动 reset 跳到底 + cursor 重新可见 — 闭环清晰。 |

**5 偏离全 accept** (3 个是派单要求的精化或允许范围, 2 个是工程上合理的简化决策
且注释完整)。

---

## 4. INV-001..010 全维持

### INV-001 (State 字段顺序: renderer → window → conn) ✅
新加 `pending_scroll_lines: i32` 在既有 presentation_dirty 之后 (line 1086, 与
T-0504 加 presentation_dirty 后 / pty 之前同位置), POD 字段不持 wgpu / wayland
裸指针, drop 顺序无 UB 风险。renderer → window → conn 链完整保留。

### INV-002 (Renderer 字段顺序) ✅
本单**零改 src/wl/render.rs** (验证: `git diff 7046851..HEAD --stat -- src/wl/render.rs`
空)。Renderer struct 14 字段顺序未动。

### INV-003 (Renderer::new 调用方合同) ✅
零改 Renderer::new + 调用方 init_renderer_and_draw。

### INV-004 (deny unsafe_code 局部 allow) ✅
本单**零新增 unsafe** (验证: `git diff 7046851..HEAD | grep '^\+.*unsafe'` 空)。

### INV-005 (calloop 单线程禁阻塞) ✅
- `scroll_display` / `display_offset` / `reset_display` / `display_text` 全纯逻辑,
  不调任何 syscall / IO / sleep
- `apply_axis_vertical` 纯标量算术, 无 syscall
- `scroll_keysym_override` 纯逻辑 keysym 表查找, 无 syscall
- `pending_scroll_lines` saturating_add 单一 i32 加法, 无 syscall
- idle callback 消费 `term.scroll_display(n)` 走 alacritty 内部内存 grid 操作,
  不调 IO

### INV-006 (resize_dirty 消费合同) ✅
- resize_dirty 单一消费者 propagate_resize_if_dirty 仍唯一 (验证: grep
  `core.resize_dirty\s*=` 仅 line 162 / 167 set + line 569 唯一 clear)
- pending_scroll_lines 是**独立** i32 累积字段, 与 resize_dirty 无生消费链关联
- presentation_dirty (T-0504) 仍独立, 不被本单干扰

### INV-007 (WindowCore handle_event 纯逻辑) ✅
零改 `handle_event` 函数体 (验证: `git diff 7046851..HEAD -- src/wl/window.rs |
grep -A2 'fn handle_event'` 无 hit)。新增 Dispatch 路径 Scroll 分派在
Dispatch<WlKeyboard> / Dispatch<WlPointer> impl 内, 不路过 WindowCore。

### INV-008 (PtyHandle 字段顺序: reader → master → child) ✅
零改 src/pty (验证: `git diff 7046851..HEAD --stat -- src/pty` 空)。

### INV-009 (PTY master fd O_NONBLOCK) ✅
零改 spawn_program / set_nonblocking 路径。本单不碰 PTY。

### INV-010 (上游 crate 类型不出公共 API 边界) ✅

**5 条独立 grep 复跑** (沿袭 T-0399 + T-0401..T-0408 + T-0501 + T-0504 SOP):

```bash
$ grep -rnE 'pub use (alacritty|wgpu|wayland|cosmic|smithay|sctk|xkbcommon)' src/
(零命中) ✅

$ grep -rnE 'impl From<.*(alacritty|wgpu|wayland|cosmic|smithay|sctk|xkbcommon)' src/
(零命中) ✅

$ grep -rnE '^pub fn .*alacritty' src/
(零命中) ✅

$ grep -rnE 'pub.*alacritty_terminal::' src/
(零命中) ✅

$ grep -rnE 'pub.*xkbcommon' src/
src/wl/keyboard.rs:16://! - 没有 `pub use xkbcommon::*;` re-export (违反 INV-010)。
   ↑ 1 命中, doc comment 内 "**没有**" 反例陈述 (T-0408 / T-0407 / T-0501 / T-0504
   audit 同源假阳, 本审 第 14 次)
```

**T-0602 新加类型边界 verify**:

| API | 入参 / 返回 / 字段 | INV-010 状态 |
|---|---|---|
| `TermState::scroll_display(delta: i32)` | i32 (std) | ✅ 不出 alacritty Scroll enum |
| `TermState::display_offset() -> usize` | usize (std) | ✅ |
| `TermState::reset_display()` | (无入参) | ✅ |
| `TermState::display_text(line: usize) -> String` | std | ✅ 不出 alacritty Line/Column |
| `KeyboardAction::Scroll(i32)` | quill 自有 enum variant | ✅ |
| `PointerAction::Scroll(i32)` | quill 自有 enum variant | ✅ |
| `pub fn handle_key_event(event, state, rows: u16)` | rows 加 u16 (std), 既有 wl_keyboard::Event 入参 (T-0501 已 accept) | ✅ |
| `apply_axis_vertical(state, value: f64) -> PointerAction` | pub(crate), f64 (std) + quill 自有 | ✅ |
| `scroll_keysym_override(keysym, rows, shift) -> Option<KeyboardAction>` | 模块私有 (无 pub) | ✅ keysym xkb::Keysym 不出模块 |
| `CellsIter::display_offset: usize` | private 字段 | ✅ |

- alacritty `Scroll::Delta(i32)` / `Scroll::Bottom` 仅在 `scroll_display` /
  `reset_display` 函数体内 use, 不漏出 ✅
- alacritty `Point::line += display_offset` 在 cells_iter 内部, `Point` 转
  `CellPos` 走模块私有 from_alacritty (T-0302 既有套路) ✅
- xkb::Keysym 单测 `xkb::Keysym::new(0xff55)` 在模块内 mod tests (cfg test 内部,
  不出模块) ✅

**INV-010 第 14 次应用** ✅ (T-0302..T-0307 + T-0399 + T-0401..T-0403 + T-0407 +
T-0408 + T-0501 + T-0504 + **T-0602 scroll_display/display_offset/reset_display/
display_text/KeyboardAction::Scroll/PointerAction::Scroll/handle_key_event(rows)/
scroll_keysym_override/apply_axis_vertical/CellsIter::display_offset**)

---

## 5. 派单 Out 全守

```bash
$ git diff 7046851..HEAD --stat -- src/pty src/text docs/invariants.md Cargo.toml Cargo.lock
(空, 全零 diff) ✅
```

- **不动 src/pty**: 零 diff (本单不碰 PTY 写入路径) ✅
- **不动 src/text**: 零 diff (display_text 是 term API, 不走 cosmic-text shape 重设计) ✅
- **不动 docs/invariants.md**: 零 diff (本单不引新 INV) ✅
- **不动 Cargo.toml/lock**: 零 diff, 无新 crate ✅
- **不做鼠标拖选区滚动** (Phase 6+ wl_data_device): 零 hits ✅
- **不做鼠标点击位置移光标**: 零 hits ✅
- **不做滚动条渲染** (Phase 6+): 零 hits ✅
- **不做滚轮加速 / 平滑滚** (整 line 跳即可): 零 hits ✅

**派单 Out 全守, 8/8 必守约束零违规**。

---

## 6. 不引新 crate ✅

Cargo.toml / Cargo.lock 零 diff (验证: 上 §5)。
- `wl_pointer` Axis event: wayland-client 已暴露 (T-0504 已用)
- xkbcommon Keysym (PageUp 0xff55 / PageDown 0xff56): T-0501 已引 ADR 0006
- alacritty `Term::scroll_display(Scroll)`: alacritty_terminal 既有 API
- 无 ADR 需要 ✅

---

## 7. P 列表

### P0: 0 项 ✅
全 P0 通过, 无阻塞。

### P1: 0 项 ✅

### P2: 0 项 ✅

### P3: 0 项 ✅

无 P3 建议。注释 / 测试 / API / INV / 偏离全部到位, 无悬挂决策需 Lead 自选。

---

## 8. 结论

✅ **批准合并 feat/T-0602 → main**

- 4 门 release 全绿 (220 tests passed, 0 failed; 派单 acceptance "≈205+" 实测 220, 超额 +15)
- INV-001..010 全守 (重点 INV-005 / 010 实测验证, INV-002 零改 Renderer struct)
- 派单 In 全实装 (#A pointer Axis / #B keyboard PgUp/Dn + Shift 整屏 / #C term
  scroll API 三件套 / #D 渲染 display_text + cursor scrollback hide / #E
  Dispatch 路径接入 + advance auto-reset / #F 集成测试)
- 派单 Out 全守 (src/pty / src/text / docs/invariants.md / Cargo.toml/lock 零 diff)
- 5 偏离全 accept (3 个是派单要求的精化或允许范围, 2 个是工程上合理的简化决策)
- 不引新 crate ✅
- INV-010 第 14 次应用 (scroll_display / display_offset / reset_display /
  display_text / KeyboardAction::Scroll / PointerAction::Scroll / handle_key_event(rows) /
  scroll_keysym_override / apply_axis_vertical / CellsIter::display_offset 10 个新边界)

**P0/P1/P2/P3 全 0**, 零悬挂决策。

**Lead 合并路径** (推 Option C squash, 与 T-0501 / T-0504 同):
```bash
cd /home/user/quill-impl-scrollback
git reset --soft main
git reset HEAD -- tasks/T-0602-scrollback.md
git commit -m "<HEREDOC: T-0602: scrollback 滚动 (滚轮 axis + PageUp/PageDown + display_offset 渲染)>"
git add tasks/T-0602-scrollback.md
git commit -m "T-0602: Status claimed -> in-review"

cd /home/user/quill
git merge --no-ff feat/T-0602 -m "Merge T-0602: scrollback 滚动 (滚轮 + PageUp/PageDown, Phase 6 P1 daily-drive)"

git add tasks/T-0602-scrollback.md docs/audit/2026-04-26-T-0602-review.md
git commit -m "T-0602: in-review -> merged + audit 报告存档"

git worktree remove ../quill-impl-scrollback
git branch -d feat/T-0602
```

**手测 deliverable** (派单 acceptance 最后一条, 由 Lead 主导):
合并后 `cargo run --release` 启动 quill, 实测:
- `cat /etc/services` 喂大量字 → 内容刷出 viewport
- 滚轮往上滚 → 看到历史输出 (滚轮一格 ≈ 0-1 line, 三格 ≈ 1-2 line)
- PageUp → 半屏 (12 line) 跳, Shift+PageUp → 整屏 (24 line) 跳
- PageDown → 半屏跳到底, Shift+PageDown → 整屏
- 喂新字节 (按任意键 / cron 输出) → display_offset 自动 reset 跳底, 视图回最新
- 滚动期间 cursor 不画 (与 alacritty / foot / kitty 一致)

→ Phase 6 P1 daily-drive 子目标完成 (user 实测反馈"无法看历史"问题解决).

---

**审码人签**: reviewer-T0602
**审视范围**: feat/T-0602 全 3 commit 完整审计
**预算消耗**: token < 70k, wallclock < 1h (per-ticket fresh agent)
