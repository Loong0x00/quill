# 审码报告: T-0206 集成测试 spawn echo hello (Phase 2 收官)

**审码人**: 审码-opus (quill-phase2 team, Opus 4.7 1M, general-purpose)
**日期**: 2026-04-25
**范围**: feat/T-0206, 2 commits
- `12ec0c2` T-0206: tests/pty_echo.rs 端到端 PTY 字节通路冒烟
- `f6e2bae` T-0206: Status claimed -> in-review

**diff stat**: 2 files, +123
- tests/pty_echo.rs +122 新 (2 integration tests)
- tasks/T-0206 Status (+1/-1)
- **src/ 零改动** — 端到端冒烟, 无生产回归风险

**判定**: ✅ 批准合并 (Phase 2 6/6 收官)

## P0 全部通过

### [P0-1] 零 src 改动 = 零生产回归 ✅
INV-001/002/005/008/009 全部自动维持, 无需复核。

### [P0-2] 合并标记 0 ✅

### [P0-3] 四门 ✅
59 tests pass (36 unit + 2 event_loop_smoke + 3 frame_stats_smoke + 2 pty_calloop_smoke + **2 pty_echo 新** + 3 pty_resize + 11 state_machine)

## P1 违规: 零

- 集成测试允许 expect/panic (CLAUDE.md 例外)
- 零 println / unsafe / thread::spawn 生产
- 注释全 why 非 what (CRLF 不敏感 / 600.5 防撞 / 500ms 裕量 / init 收养兜底 / zombie cmdline /proc 空)

## P2 架构 + 测试质量

### [P2-1] 测试 1 captures_echo_hello_output 端到端闭环 ✅
- `windows(5).any(|w| w == b"hello")` 子串搜索, CRLF-agnostic (比 T-0203 `starts_with` 宽松合理)
- 错误处理全分支 (Ok(0) / Ok(n) / WouldBlock / EIO / 其它)
- 2 秒 deadline 200× 裕量

### [P2-2] 测试 2 drop_cleans_up_long_running_child ✅

**(a) 600.5 小数秒 trick 防撞**: pgrep -f cmdline 子串匹配, "sleep 600" / "sleep 6000.5" / "sleep 60.5" 全都不与 "sleep 600.5" 构成连续子串。系统里正好跑 "sleep 600.5" 概率极小。

**(b) 前置断言守门**: 没前置就可能假 pass (sleep 根本没启动)。前置命中才保证 drop 路径真被测。加分。

**(c) INV-008 drop chain 端到端验证**: reader (dup fd 关) → master (close → slave 收 SIGHUP) → child (句柄 drop 不 wait), sleep 默认 SIGHUP handler = Term, zombie 被 init 收养, /proc cmdline 清空 → pgrep 不命中。

**(d) 500ms 窗口 scope tightening 合理**: ticket 字面 100ms, 写码放宽 500ms + 20ms 轮询。CI 慢机 / WSL / VM 下裕量必要, 实测 120ms 完成 (5× 裕量)。

### [P2-3] ticket Acceptance 全对齐 ✅
| Acceptance | 状态 |
|---|---|
| cargo build/test --test pty_echo / test all / clippy / fmt | ✅ |
| 审码 放行 | ✅ |
| drop-cleanup 证 SIGHUP | ✅ |

### [P2-4] Phase 2 闭环综合盘点 ✅
- T-0201 spawn_shell/spawn_program + O_NONBLOCK
- T-0202 calloop Generic + POLLHUP (T-0205 修)
- T-0203 read + escape_ascii + 8 pty_readable_action 单测
- T-0204 resize API + cols/rows 防回归
- T-0205 try_wait + Arc<AtomicBool> + POLLHUP fix
- T-0206 端到端冒烟 (本 ticket)

PtyHandle 5 方法全实装, calloop 接入, 端到端测试到位。

## 附带 cleanup commit a2c280a 审

- `git rm --cached` 移除 logs/ 下 2 个 log 文件 (保留磁盘副本)
- `.gitignore` 追加 `logs/` (目录形式精确匹配)
- commit message 诚实交代违反 `git add -A` 禁令

复核: `.gitignore` 的 `logs/` 形式 + `git rm --cached` 保留本地 debug 能力 + 单 commit < 30 行。防复发链条完整, 写码-close 认错 + 自修姿态符合 CLAUDE.md 事后补救标准。

## P3 建议 (不阻塞, 归档 TD-014/015/016)

### [P3-1] 600.5 常量 + 小数秒防 pgrep 撞技巧入 CLAUDE.md
建议把这条 trick 写成"测试模式 tips"小节, 未来 pkill 类测试可复用。**归档, 非本 ticket scope**。

### [P3-2] T-0203 vs T-0206 echo 测试重叠
T-0203 字节前缀 `starts_with("hi\n"/"hi\r\n")` 验字节顺序, T-0206 contains `windows(5)` 验端到端送达。**语义不同, 非冗余, 不合并**。

### [P3-3] zombie init 收养注释顺序微小校正
严格讲"sleep → SIGHUP → zombie → portable-pty drop 不 wait → zombie 持续 → 测试进程退出 → init re-parent → init reap"。注释意思对, 顺序可更明确。**不阻塞**。

## 给后续 Phase 3 的前置信息

### Phase 2 产出 handoff
- PtyHandle 公共 API 完备 (6 方法)
- calloop 接入 (pump_once 5a + POLLHUP 正确检)
- 统一退出 Arc<AtomicBool> should_exit (T-0104+T-0205 共享)
- INV-001..009 全齐
- 端到端测试 16 tests (pty_calloop / pty_resize / pty_echo / pty_readable_action)

### Phase 3 T-0301 alacritty_terminal 集成建议
- 入口: `pty_read_tick` 里 Ok(n) 分支把 `&buf[..n]` 喂 `Term::process_byte_slice()`, 取代单纯 tracing::trace!
- **callback 要升级**: Core<PtyHandle> → Core<&mut (PtyHandle, Term)> 或者 Core<&mut State>, 两条路建议与 **T-0105 refactor 一起做**
- 当前审码建议: **T-0301 与 T-0105 refactor 合并**, 一次性升级 Data type 到 Core<&mut State>, 消除双 poll 并把 Term 塞进 state, Lead 派单时定夺

## 整体评价

Phase 2 收官的纯测试 ticket, 无过度工程, 2 条端到端冒烟把 PtyHandle read 路径 + Drop 路径焊死。`600.5` 防撞 + 前置断言守门 + 500ms 轮询裕量 + CRLF-agnostic 断言都是成熟测试工程细节。cleanup commit 认错 + 修复方式彻底。

**Phase 2 完整闭环, 6/6 合并, PtyHandle API 完备, 端到端测试到位**。可直接 `git merge --no-ff feat/T-0206` 到 main。
