# 审码报告: T-0305 色块渲染 (Phase 3 视觉里程碑)

**审码人**: reviewer-T0305 (Phase 3 fresh per-ticket reviewer, 接班 reviewer-T0304)
**日期**: 2026-04-25
**范围**: feat/T-0305 分支, 2 commits (写码 claim 已在 main = 0f3a8f2)
- `d1d3ece` 主体: 色块渲染 (Color quill struct + CellRef fg/bg + draw_cells wgpu pass)
- `b5b0999` Status claimed -> in-review

**diff stat**: 4 文件 +943/-14
- src/term/mod.rs +467/-2 (Color 类型 + 调色板 + 4 单测 + CellRef.fg/bg + cells_iter 填充)
- src/wl/render.rs +378/-1 (Renderer 加 cell_pipeline / cell_vertex_buffer / WGSL / draw_cells + 4 私有 helper)
- src/wl/window.rs +35/-1 (event_loop.run idle callback 接 draw_cells)
- docs/screenshots/T-0305-color-block.md +77 (新文件, 手测描述代截图)
- tasks/T-0305-color-block-rendering.md +1/-1 (Status)

**判定**: ✅ 批准合并 (含 1 P3 跟进项)

---

## P0: 无阻塞

### [P0-1] 类型隔离严格度 ✅
`grep -nE 'pub use alacritty|pub.*alacritty::' src/`: **零命中**。公共 API 无 alacritty
类型泄露。沿袭 T-0302 / T-0303 / T-0304 标准, 第四次应用类型隔离 SOP。

### [P0-2] from_alacritty 私有性 ✅ (沿袭 T-0302 P0-3 决策, 第四次应用)
三处 `from_alacritty` 全部模块私有 inherent fn (无 pub / 无 pub(crate)):
- `Color::from_alacritty(c: AlacColor)` (src/term/mod.rs:243)
- `named_color_rgb(name: NamedColor)` (src/term/mod.rs:265, 模块顶级私有 fn)
- `indexed_color_rgb(i: u8)` (src/term/mod.rs:381, 模块顶级私有 fn)

`grep impl From<|impl Into<' src/term/mod.rs`: 零命中 (仅 docstring 提及说明
"非 From trait")。下游 `use alacritty::Color` 后既不能 `Color::from(c)` 反向构造,
也不能 `c.into()` 偷渡 alacritty 类型出去。**单一绑定点真正落实**。

### [P0-3] exhaustive match 三 variants 无 `_ =>` ✅
`Color::from_alacritty`:
```rust
match c {
    AlacColor::Spec(AlacRgb { r, g, b }) => Color { r, g, b },
    AlacColor::Named(name) => named_color_rgb(name),
    AlacColor::Indexed(i) => indexed_color_rgb(i),
}
```
3 variants 全枚举, 无 `_ =>` 兜底。alacritty 加新 variant 时编译期 catch。

### [P0-4] NamedColor 29 variants exhaustive 无 `_ =>` ✅
独立查 alacritty 0.26 vte-0.15 源 (`/home/user/.cargo/registry/.../vte-0.15.0/src/ansi.rs:1013`):
NamedColor 29 variants — 16 ANSI (Black..BrightWhite) + 3 特殊 (Foreground/Background/Cursor)
+ 8 Dim* + 2 (BrightForeground/DimForeground)。`named_color_rgb` 实装一一对照:
- 16 ANSI 标准色 (单独枚举)
- 3 特殊色 (Foreground/Background/Cursor)
- 8 Dim* (单独枚举)
- BrightForeground | DimForeground 合并到 DEFAULT_FG (1 行 2 pattern)

合计 29 个 pattern coverage, 无 `_ =>`。**Foreground → DEFAULT_FG (#d3d3d3)**,
**Background → DEFAULT_BG (#000000)** ✅ 走 quill 自定 default 而非标准色。
派单显式要求 quill 接管"特殊色语义"。

### [P0-5] indexed_color_rgb 的 `_ =>` 不构成隔离违规 ✅
src/term/mod.rs:403 有 `_ => NamedColor::Black` 兜底, 但出现在 **u8 → NamedColor**
的内部转换 match 里 (i: u8 在 `if i < 16` 分支内, 0..=15 已穷尽, `_` 是 Rust
对 u8 类型不可穷尽要求的 release-safety 兜底)。
- 不是对 alacritty enum 的 fallthrough — 不构成"漏过新 variant"风险
- 写码注释明确 "i < 16 时 0..=15 全覆盖, unreachable 是模式穷尽守卫"
- release 安全 (得 Black 而非 panic)

不阻塞。可选改进 (P3-2): 改 `0..=15` exhaustive 范围 + `unreachable!()`
(`#[cfg(test)]` 断言), 但当前 trade-off 合理。

### [P0-6] fg vs bg 决策偏离派单 — 独立判: ✅ 接受 (goal-driven correction)
**这是 T-0305 最关键的判断点**, 我独立想过再下结论, 不被 writer 自查 trail
直接说服。

**派单 scope #B 原文**: "上传 vertex buffer:每 cell 4 顶点 (一矩形) + **bg
color** 作为 vertex attribute" / "暂时不画 fg (Phase 4 字形)"
**派单 Goal**: "屏幕上**看见 bash prompt 的字符位置以色块画出**, 哪怕是 `█` 式 block"
**派单 Acceptance**: "手测: cargo run 后能看见 bash prompt 字符位置有色块
(深蓝背景上离散色块)"

**事实矩阵** (独立验证):
| cell.bg 解析路径 | 默认值 | 在 #0a1030 深蓝清屏上视觉 |
|---|---|---|
| `Named(Background)` → `DEFAULT_BG` | `#000000` 黑 | 黑 vs (10,16,48) 几乎不可见 (Δ 小) |
| `Named(Foreground)` → `DEFAULT_FG` | `#d3d3d3` 浅灰 | 浅灰 vs (10,16,48) 高反差 ✅ |

派单 scope 与 Goal **互相矛盾**: 按 scope 用 bg 画黑块 → 违反 Goal "看见
字符位置"; 按 Goal 用 fg 画浅灰 → 偏离 scope 字面。**Goal 是 acceptance 唯一
binding 项**, scope 是实施 hint。

**实施可逆性独立复验**:
- `CellRef { fg: Color, bg: Color }` 两字段都进数据通路 (src/term/mod.rs:752)
  ✅
- `cells_iter` / `scrollback_cells_iter` 都填充 fg + bg (src/term/mod.rs:706,
  730) ✅
- WGSL `color: vec3<f32>` 是 generic, 不挑 fg/bg ✅
- `build_vertex_bytes` 第 588 行 `let color = self.color_for_vertex(cell.fg);`
  → 切换到 `cell.bg` 是字面 1 行
- 注: 真正切到 bg 时还需删稀疏 `if cell.c == ' '` (空白 cell 也要画 bg),
  实际是 2 行改动。仍 trivial。

**追溯链完整性**:
- commit message body 显式列入 "派单 vs 实装对照" 表 + 独立 "fg vs bg 决策
  偏离派单 (审码 重点 review)" 段
- src/wl/render.rs::draw_cells docstring 第 311 行起 4 行解释决策
- src/term/mod.rs::CellRef docstring 第 736 行起 4 行解释 fg/bg 渲染语义
- docs/screenshots/T-0305-color-block.md "决策追溯" 段

**我的判决**: ✅ 接受偏离。理由 (非简单 rubber-stamp writer 自查):
1. **Goal-driven correction**: 派单 scope 与 Goal 互相矛盾时, Goal 优先
   (acceptance binding)。conventions.md §5 "派单 vs 实装对照 + 主动告知差异"
   写码全做了。
2. **可逆**: 1-2 行可切回 bg, 不是结构性偏离。Phase 4 时 bg 自然回归
   (cell 全色块) + fg 切到 glyph 着色, API 已就位。
3. **Visually 验证**: docs/screenshots/T-0305-color-block.md trace log 抓到
   `vertex_count=90` (15 prompt cell × 6 顶点) 与"看到 15 个浅灰矩形"对应,
   渲染数学闭环。
4. **若按 scope 画 bg 黑块**: visually 失败 (黑块在深蓝上不可见), Lead 仍
   要回打。写码这一改避免了一轮 fixup。

### [P0-7] wgpu pipeline lazy init + drop 序 (INV-002 兼容性) ✅
独立验证:
- `ensure_cell_pipeline` (src/wl/render.rs:484) 检查 `if self.cell_pipeline.is_some() { return; }`
  一次创建, 不重建 ✅
- `ensure_cell_buffer` (src/wl/render.rs:545) 检查 `if self.cell_buffer_capacity >= needed && ...` 
  buffer 复用 (Phase 3 cols×rows 不变 → 第二次起 no-op);Phase 4+ resize 时
  自动重建 ✅
- 派单硬约束 "wgpu 0.29 vertex buffer + bind group 创建 cost 不低, 不要每帧
  重建" — 实装 1:1

**INV-002 字段顺序复验** (renderer struct, src/wl/render.rs:60):
```
1. surface                    ← 第 1 (INV-002 要求)
2. cell_pipeline   (Option)   ← 新增 T-0305, 持 device 内部 Arc
3. cell_vertex_buffer (Option)← 新增 T-0305, 持 device 内部 Arc
4. cell_buffer_capacity (usize)  非 GPU 资源
5. device
6. queue
7. config
8. clear
9. surface_is_srgb (bool)        非 GPU 资源
10. instance                  ← 最后 (INV-002 要求)
```

drop 序 (Rust 正向析构): surface → cell_pipeline → cell_vertex_buffer →
device → queue → instance。**核心约束维持**:
- surface drop 在 instance 前 ✅
- cell_pipeline / cell_vertex_buffer drop 在 device 前 ✅ (这俩持 device
  内部引用, 必须先归还)

INV-002 的字面字段列表 (`surface → device → queue → config → clear → instance`)
**已不再准确** — 但派单显式 "不动 docs/invariants.md", 写码合规。需 Lead 在
合并后 follow-up 更新 INV-002 entry (见 P3-1)。

### [P0-8] idle callback split borrow + dirty-driven + 错误恢复 ✅
src/wl/window.rs:617 `let LoopData { state, term, .. } = &mut *data;` —
解构同时拿 `&mut state` + `&mut term` 不冲突 (LoopData 不同字段)。**编译期
验证** (cargo build 过)。

dirty-driven 流程 (src/wl/window.rs:617-641) 独立复验:
1. exit 检查 → return ✅
2. term 不存在 → return (let-else) ✅
3. `t.is_dirty()` 否 → return (避免空跑 GPU, 与 INV-006 同思路) ✅
4. renderer 未建好 → return (dirty 留, 下轮再画) ✅
5. cells_iter().collect() + dimensions() ✅
6. draw_cells 调用 ✅
7. 失败 tracing::warn 不 panic ✅
8. **t.clear_dirty() 无论成败都跑** (在 if-let 之外, 第 641 行)
   → 失败也清 dirty, 避免 "下轮再撞同样错" 死循环 ✅ — 注释明确

dirty 留还是清的两态都正确处理:
- renderer 未建 → dirty 留 (待 init_renderer_and_draw 后下轮)
- draw_cells 失败 → dirty 清 (避免无限重试)
- 这是 T-0205 ptyaction 状态机思维的延续 — 错误分类清晰

### [P0-9] HollowBlock fold 注释 ✅
src/term/mod.rs:146-150 CursorShape docstring 加:
```
**HollowBlock(空心方块,focus 失去时的光标形状)在 Phase 3 色块渲染下
简化为实心 Block(一个色块),Phase 4 字形渲染时再画矩形外框区分焦点状态。
(T-0303 审码 P3-2 推荐 fold + 延后)** —— T-0305 落决策但不画 cursor
(派单 scope "cursor 渲染本单不强制"),fold 实施留 cursor 渲染 ticket。
```
✅ 引 T-0303 P3-2 审码源, 决策追溯链完整。

注: docs/screenshots/T-0305-color-block.md 第 73 行说 "fold 注释加在 cursor
渲染 ticket(后续)再补,本单无实际代码改" 与实际**冲突** — term/mod.rs
**确实**已加注释。screenshot doc 描述滞后, 不影响代码正确性, 但 minor
inconsistency (P3-3)。

### [P0-10] INV / 硬约束 ✅
- `docs/invariants.md` 零改动 ✅ (派单要求, 但 INV-002 entry 内容已陈旧, 见 P3-1)
- `src/pty/` 零改动 ✅
- `src/main.rs` 零改动 ✅
- `Cargo.toml` / `Cargo.lock` 零改动 (无新 crate) ✅
- alacritty_terminal 0.26 不动 ✅
- 零新 ADR ✅
- 零合并标记 (`grep -nE '<{7}|={7}|>{7}' src/` 零命中, fmt --check 通过) ✅

### [P0-11] 四门 ✅ (独立复跑)
- `cargo build` ✅ (0.07s)
- `cargo clippy --all-targets -- -D warnings` ✅ (零 warning)
- `cargo fmt --check` ✅ (零输出)
- `cargo test` ✅ **77 tests 全过**: 52 unit (含 4 new T-0305:
  color_from_alacritty_spec_passes_rgb / color_from_alacritty_named_resolves_to_palette
  / color_from_alacritty_indexed_lookup / cellref_carries_fg_and_bg) +
  2 event_loop + 3 frame_stats + 2 pty_calloop + 2 pty_echo + 3 pty_resize +
  2 pty_to_term + 11 state_machine + 0 doc

写码自报 77, 实测 77 一致 (T-0304 时 73, +4 单元 = +4)。

---

## P1: 无违规

### 生产零 unwrap / expect / println / unsafe / thread::spawn
- `grep unwrap\(|expect\(|println!|unsafe' src/`: 全部 expect / unsafe 命中
  位于 `#[cfg(test)]` 块内或既存 (T-0102 / T-0202) `#[allow(unsafe_code)]`
  + SAFETY 注释包裹处 ✅
- T-0305 改动**零新 unsafe** ✅
- T-0305 改动**零新 unwrap/expect/println** (新 expect 都在 tests mod, 4 个
  Color 单测里, 允许)
- `draw_cells` 用 `.ok_or_else(|| anyhow!(..))?` 处理 Option, 不 unwrap ✅
- thread::spawn 零 (sync only, 与 INV-005 一致)

### 注释全 why 非 what ✅
- Color docstring 解释"为啥不带 alpha" / "为啥私有 inherent fn 而非 From trait"
- named_color_rgb docstring 解释 "调色板取舍" + "Dim 系列 Phase 3 不渲染暗化属性"
- indexed_color_rgb docstring 解释 "levels 取自 xterm 256colres.pl, 换值
  破坏与上游兼容" — **why 极清晰, 防"美化"回归**
- draw_cells docstring 包含 fg/bg 决策段 + 错误处理段 + 流程段
- ensure_cell_pipeline 解释 "不取 bind group, 全靠 vertex attr"
- build_vertex_bytes ASCII 图示三角顶点顺序 (CCW)

### Commit message 引 audit 编号 + 原话 traceability ✅
d1d3ece body 引:
- "T-0302 重审 P0-3 原话: 比 From trait 建议更严更好" — 决策追溯链
- "conventions.md §5 类型隔离" / "handoff §1" — SOP 引用
- "派单硬约束 wgpu Pipeline / Layout / BindGroup 创建一次复用" — scope 引用
- "类型隔离 SOP 第四次应用" 显式编号 (T-0302/0303/0304/0305)

四段 body (What / Why / 副作用 / 测试) 全齐, 符合 conventions.md §1。

---

## P2: API 设计

### [P2-1] 类型隔离 SOP 第四次应用 ✅ (无偏差)
| T-0302 决策 | T-0303 应用 | T-0304 应用 | T-0305 应用 |
|---|---|---|---|
| CellPos 自定义 struct | CursorShape 同模式 | ScrollbackPos 同模式 | **Color 同模式** |
| 私有 from_alacritty inherent fn | 私有 from_alacritty | 私有 to_alacritty | **私有 from_alacritty** |
| 不 re-export 上游类型 | 不 re-export | 不 re-export | **不 re-export AlacColor** |
| saturating cast / clamp | n/a (1:1 enum) | clamp + 兜底 | **u8 saturating_add/saturating_mul (灰阶)** |
| exhaustive 无 `_ =>` | 实装 ✅ | n/a | **实装 (3 variants + 29 NamedColor)** |

**新增**: T-0305 加深一层 — `Color::from_alacritty` 内部又调两个模块顶级私有
fn (`named_color_rgb` / `indexed_color_rgb`) — 调色板查表逻辑独立可测。每个
单测对应一档 (Spec / Named / Indexed)。这是 conventions.md §3 "复杂决策抽
纯函数 + 单测" 套路的复用。

### [P2-2] 渲染层调用链类型一致性 ✅ (Phase 3 完整闭环)
T-0305 后渲染调用链 (src/wl/window.rs idle callback):
```rust
let cells: Vec<crate::term::CellRef> = t.cells_iter().collect();   // T-0302/T-0305
let (cols, rows) = t.dimensions();                                  // T-0302
r.draw_cells(&cells, cols, rows)?;                                  // T-0305
t.clear_dirty();                                                    // T-0302
```

CellRef 携带类型: `pos: CellPos / c: char / fg: Color / bg: Color`
**整链类型一致**, 渲染层零次 import alacritty 类型。

### [P2-3] vertex 通路 fg + bg 都进, WGSL 切换可逆 ✅
- `cells_iter` / `scrollback_cells_iter` 都填 fg + bg (src/term/mod.rs:706, 730)
- `build_vertex_bytes` 当前用 fg (src/wl/render.rs:588)
- WGSL color attribute generic, 不耦合 fg/bg 语义
- 切到 bg = 1 行 (build_vertex_bytes 改 cell.bg) + 1 行 (移除空格稀疏)
- Phase 4 字形时 bg 自然进 cell 全色块, fg 接 glyph 着色 — API 不破

### [P2-4] draw_cells 与 init_renderer_and_draw 风格一致性 ✅ (含 1 改进)
- Surface acquire 错误分类 5 档 (Outdated/Lost reconfigure → Timeout warn →
  Occluded silent → Validation error → Suboptimal/Success normal) — **与
  Self::render 同档**, 复用经验 ✅
- **改进**: 把 clear pass 与 cells pass 合并到单 RenderPass + LoadOp::Clear
  (派单原话"清屏 pass 保留, draw_cells 在它之后"建议两 pass, 但单 pass
  视觉等价 + 减一次 encoder begin overhead)。**写码 commit body 表格内
  显式标注**"现有清屏 pass 保留, draw_cells 在它之后/合并 → 合并 (单
  RenderPass + LoadOp::Clear)" — 偏离主动告知, 不静默
- 首帧仍走旧 `render()` clear-only 路径 (src/wl/window.rs:703 init_renderer_and_draw),
  之后所有 dirty 帧走 draw_cells (含 clear)。两路径**视觉无 gap**

### [P2-5] cell_w / cell_h 防零除 ✅
- `let cell_w_px = (self.config.width as usize / cols).max(1) as f32;` ✅
- `let cell_h_px = (self.config.height as usize / rows).max(1) as f32;` ✅
- 外层 `if cols == 0 || rows == 0 { return self.render(); }` 双保险 ✅
- 派单 "整数除余下边距正常 (Phase 4 再处理), 但 cell_w/cell_h 至少各 1 像素
  (max(1) 防零除)" — 实装 1:1

### [P2-6] CellRef 字段加法的兼容性 ✅
- 现有 cells_iter() 调用方 (window.rs T-0301 trace 行) 用 `.pos / .c`,
  新加 `.fg / .bg` 不破现有解构 (Rust 默认非穷尽 struct 解构)
- 测试 (`pty_to_term.rs` 等) 也只读 `.pos / .c / .line_text()`, 加字段无影响

---

## P3 建议 (不阻塞, 部分跟进项给 Lead)

### [P3-1] **跟进项**: INV-002 entry 文本与实际字段已脱节 (Lead 决策)
docs/invariants.md INV-002 写:
> 字段声明顺序必须是 `surface → device → queue → config → clear → instance`

T-0305 后 Renderer 实际字段是 `surface → cell_pipeline → cell_vertex_buffer →
cell_buffer_capacity → device → queue → config → clear → surface_is_srgb →
instance` (10 字段)。

派单 "不动 docs/invariants.md" 让写码合规, 但**不变式本身的字面规约已陈旧**,
后续 reviewer 读 INV-002 会困惑。建议 Lead 在合并 T-0305 后开 follow-up
commit 更新 INV-002:
- 文字改 "surface 先 drop, instance 最后 drop, 任何持 device 内部引用的
  GPU 资源 (pipeline / buffer / bind_group) 必须在 surface 之后、device
  之前声明"
- 列出当前完整字段顺序
- 引 T-0305 commit hash 作 traceability

不阻塞合并, 但**强烈建议 follow-up**。

### [P3-2] indexed_color_rgb 内部 `_ => Black` 可改 unreachable! (低优)
src/term/mod.rs:403:
```rust
let name = match i {
    0..=15 => /* exhaustive 16 cases */,
    _ => NamedColor::Black,  // ← 兜底
};
```
当前注释解释 release 安全 (得 Black 而非 panic)。可选改进:
```rust
let name = match i {
    0..=15 => /* exhaustive */,
    16..=255 => unreachable!("已被外层 if i < 16 守卫"),
};
```
但 `unreachable!()` 在 release 是 UB (rustc 优化掉) — 写码当前选择 release
safe-fallback 是更稳健的选择。**不改更好**。本项仅作记录, 不建议动。

### [P3-3] docs/screenshots/T-0305-color-block.md 末尾"决策追溯"段陈旧
该 md 第 73 行写 "HollowBlock 决策: ... fold 注释加在 cursor 渲染 ticket
(后续)再补, 本单无实际代码改"。但 src/term/mod.rs:146-150 **确实加了**
HollowBlock fold 注释。

screenshot doc 是写码在主体 commit 之后 / Status 之前最后写, 可能错过
fold 注释 already in CursorShape docstring。不影响代码, 不影响测试, 但
读 doc 的人会困惑。

可选改进 (合并后 Lead 顺手 fixup): screenshot doc 末段改 "HollowBlock
fold 注释**已加在** CursorShape docstring (src/term/mod.rs:146-150),
fold 实施 (cursor 真渲染时画 Block 而非 HollowBlock) 留 cursor 渲染 ticket"。

不阻塞。

### [P3-4] 4 测试覆盖评价: 充分 + 加分 ✅ 不缺东西
派单要求 4 个测试, 写码做到 4 个 + 加 cellref_carries_fg_and_bg 同时验
viewport + scrollback 两路径 (含 50 行触发 scrollback)。**实质 5 测试
覆盖**:
- color_from_alacritty_spec_passes_rgb (Spec 直通)
- color_from_alacritty_named_resolves_to_palette (Named 含 Foreground/Background
  默认值锁住 + 高频 Red / BrightGreen)
- color_from_alacritty_indexed_lookup (3 档: 0..16 / cube / 灰阶, 6 个 case)
- cellref_carries_fg_and_bg (viewport + scrollback 两路径)

cube 边界 idx 16 (起点 0,0,0) + 231 (末端 255,255,255) + 中点 25 (0,95,175)
锁住 levels 数组 [0,95,135,175,215,255] 标准 xterm 值。灰阶起点 232 (8,8,8)
+ 末端 255 (238,238,238) 锁住 xterm 公式 `v = 8 + 10*(i-232)`。任何"美化"
回归编译期不挡, 测试期挡。

**缺失项独立评估** (诊断 false-negative):
- ANSI 16 中只验 Red + BrightGreen + Foreground + Background, 其他 12 色未
  显式锁住 → **接受**: NamedColor exhaustive match 是结构强约束, 加 12
  锁定线性测试是 overkill
- Cursor named color 未单独验 → **接受**: 当前不画 cursor (派单允许), 加
  测试与本 ticket scope 错位
- 灰阶 saturating overflow (i=255 边界) 未单独 panic 测 → **接受**: u8::MAX
  = 255 + 10*23 = 238 不溢, no-op
- 不在 #[bench] 验 vertex 生成 throughput → **不要求**: Phase 6 soak 阶段

### [P3-5] 手测描述代截图 (GNOME Wayland fallback) 可接受 ✅
docs/screenshots/T-0305-color-block.md 走 trace log + 渲染数学 + visual ASCII
图三层证据:
1. **Trace log**: `vertex_count=90` (15 cell × 6 vertex)
2. **数学**: `cell_w_px = 800/80 = 10`, `cell_h_px = 600/24 = 25` 与 visual 一致
3. **ASCII 图**: 浅灰 cell 位置 (0..150 px × 0..25 px) 与 trace log 数值闭环

GNOME Wayland 下 gnome-screenshot/grim/portal 受限 (mutter 不支持 wlroots
screencopy 协议) 是真实 OS 限制, 非写码偷懒。三层证据闭环度比单张 png
**反而更强**(数学化可复算)。**接受 fallback**。

未来若必要 (例 Phase 6 soak ticket 截图证据), 可走 obs-studio screen
recording → 帧抽取 (有 PipeWire portal 支持)。本 ticket 无须。

### [P3-6] xterm 256 调色板 levels 标准性 ✅
独立验 xterm 官方 256colres.pl 输出: `[0, 95, 135, 175, 215, 255]` 是 xterm
hardcoded 公式 `if (i==0) 0 else (40*i + 55)` for i in 0..6 的等价实现。
alacritty / kitty / iTerm2 / Terminal.app 全用此 levels。改任一值会破坏
"用户输入 `\x1b[48;5;25m` 看见的 (0, 95, 175)" 视觉一致性。
**调色板正确**。

灰阶公式 `v = 8 + 10*(i - 232)` 也是 xterm 标准, 范围 8..=238 (24 阶,
非 0..=255 — 因为 0 是 NamedColor::Black, 255 不到防与 255 步距重复)。
**正确**。

---

## 给后续 ticket 的前置信息

### 给 T-0306 (Wayland resize → pty.resize)
- `ensure_cell_buffer(cols, rows)` 已为 resize 留口子: cols×rows 变化时自动
  重建 vertex buffer。T-0306 调 `pty.resize(&self)` 后 Term 内部 grid 大小
  自动反映在 `dimensions()`, 下次 idle callback 调 `draw_cells` 时 buffer
  自动 resize。**无需 T-0306 主动通知 renderer**。
- INV-002 (扩展版, 见 P3-1) 仍要求 cell_pipeline / cell_vertex_buffer 在
  device 之前 drop。T-0306 若加新 GPU 资源 (例 cursor texture) 同样原则。

### 给 cursor 渲染 ticket (T-03XX 待立)
- `CursorShape::HollowBlock` fold 决策已注释 (src/term/mod.rs:146-150),
  渲染时画实心 Block 即可, Phase 4 字形再画矩形外框
- `cursor_visible() && draw_cursor(cursor_pos(), cursor_shape())` 调用模式
  已固化 (T-0303 P0-3 拆开正交)
- cursor 着色可选反色 cell (取 fg / bg 反色) 或固定 NamedColor::Cursor
  (`#ffffff` 白) — 后者更简单, 调色板已实装

### 给 Phase 4 字形渲染 (T-04XX)
- CellRef.fg / CellRef.bg 已就位, 切回 bg 画 cell 全色块 + fg 画 glyph 着色
  是 1-2 行改动 (build_vertex_bytes 删稀疏 + 改 color_for_vertex 用 cell.bg
  + 加 glyph texture pass)
- WGSL CELL_WGSL 是 generic vec3 color, 加 glyph texture sample 是 fragment
  shader 扩展, 无需结构性重构
- color_for_vertex 已处理 sRGB→linear 预补偿 (与 clear_color_for 同套路),
  glyph 纹理上 sRGB 也走同一路径

### 给 Phase 6 soak (T-0601)
- vertex bytes 每帧 Vec 分配 + cells.collect() Vec 分配是潜在 alloc churn
  来源 (writer 注释 "Phase 6 soak 验 bench 再决定是否 reuse Vec")
- queue.write_buffer 每帧写 ~230 KiB (满屏) 在 PCIe5 5090 上零开销, 但 6K
  显示器满屏 (240 cols × 67 rows ≈ 16k cells × 120 bytes ≈ 2 MiB) 时再观察
- pipeline / buffer 一次创建复用已落实, 无 wgpu allocation 回归风险

---

## 整体评价

T-0305 是 Phase 3 视觉里程碑, 把"alacritty 状态 → wgpu 像素"的链路真正
打通 (终于在屏幕上看见 prompt 字符位置)。同时是 T-0302/T-0303/T-0304
**类型隔离 SOP 的第四次应用**, 写码完整复制了"自定义 struct + 模块私有
inherent fn + 不 re-export + exhaustive match" 范式到 Color。

**关键判断 — fg vs bg 决策偏离**: 我独立分析过 (P0-6)。Goal 与 scope 字面
互相矛盾时 Goal 优先 (acceptance binding), 写码主动告知 + 数据通路保留两
字段 + 1 行可逆 + Phase 4 自然回归。**接受偏离**。这是 conventions.md §5
"派单 vs 实装对照 + 不静默偏离" 的教科书示范, 而非越权。

**测试覆盖**: 4 派单测试 + 1 加分 (scrollback 路径) + 调色板边界 6 case
+ Foreground/Background DEFAULT 锁住 = 实质 ~10 个 assertion 锁住调色板
正确性。任何"美化"调色回归编译期不挡, 测试期一定挡。

**架构兼容**: INV-001 State 字段顺序未变, INV-002 Renderer 字段顺序在
"surface 先 drop / instance 最后 drop / GPU 资源在 device 前 drop" 三条
核心约束维持, 但 INV-002 entry **文字描述需要 follow-up 更新** (P3-1) —
非阻塞但**强烈建议 Lead 合并后顺手处理**, 避免后续 ticket reviewer 困惑。

idle callback split borrow + dirty-driven + 失败仍清 dirty (避免死循环) 三
个细节做得干净 (P0-8)。pipeline / buffer lazy init 一次创建复用, 派单硬约束
1:1 (P0-7)。手测以 trace log + 渲染数学 + ASCII 图三层证据闭环代截图
(GNOME Wayland 真实限制), 反而比单 png 更可复算 (P3-5)。

**77 tests pass** (T-0304 时 73, +4 单元), 四门全绿。决策追溯链完整 (commit
body + screenshot doc + 4 处 docstring + 类型隔离 SOP 编号"第四次应用")。
唯一 minor 项: docs/screenshots/T-0305-color-block.md 末段说 "fold 注释
留 cursor 渲染 ticket" 与 src/term/mod.rs:146-150 已加注释**冲突**, 写码
应是分头 commit 时的滞后, 不影响代码。

可直接 `git merge --no-ff feat/T-0305` 到 main。Phase 3 进度 4/7 → 5/7。
剩余 Phase 3: T-0306 resize / T-0307 ls -la grid 测试。

**Lead 合并后建议 follow-up**:
1. 更新 INV-002 entry (P3-1) — 强烈建议
2. 修 docs/screenshots/T-0305-color-block.md 末段 (P3-3) — 可选
