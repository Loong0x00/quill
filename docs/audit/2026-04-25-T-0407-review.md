# 审码报告: T-0407 字体 face 锁定 + emoji 排除 + atlas key face_id + cell color source D fixup (T-0403 P0 bug fix)

**审码人**: reviewer-T0407B (per-ticket fresh agent, round 2 接班 reviewer-T0407 — 后者在 Lead 加 D fixup 派单更新后被 kill 重 spawn)
**日期**: 2026-04-25
**范围**: feat/T-0407 分支, 3 commits
- `98b72e4` 主体 A+B+C: 字体 face 锁定 + emoji 排除 + atlas key 加 face_id (GlyphKey 三维)
- `3786962` Status claimed → in-review
- `6620cd1` D fixup: cell pass 在 draw_frame 走 bg 色 (T-0403 字形不见真因修复)

**diff stat** (independently verified `git -C /home/user/quill-impl-face-lock diff main..HEAD --stat`):
- src/text/mod.rs +416/-58 (T-0407 主体: PREFERRED const + TextSystem 3 字段 + GlyphKey struct + apply_emoji_blacklist + 5 新测试)
- src/wl/render.rs +57/-9 (主体 +9/-4 GlyphKey HashMap key + D fixup +48/-6 enum CellColorSource + build_vertex_bytes 加参数 + draw_cells/draw_frame 调用切 Fg/Bg)
- tests/glyph_atlas.rs +2/-2 (GlyphKey import + HashSet 元素类型同步)
- tasks/T-0407-font-face-lock-emoji-exclude.md +1/-1 (Status)

合计 4 文件 +491/-61, 派单 budget tokenBudget=80k 适当。3 commits 含 D fixup 单 commit 单事 (符合 conventions §1)。

**判定**: ✅ **批准合并** (P2 观察 4 项 + P3 微跟进 5 项, 均不阻塞)

T-0407 是 T-0403 视觉里程碑的 P0 bug 修复, 经 Lead 三轮诊断逐步收敛真因:
1. (writer-T0407 初判 emoji fallback) → user 实测仍连续矩形 → 部分对
2. (Lead 二判 atlas key 跨 face 撞 + Family::Monospace 自挑) → A+B+C 修不解 bug
3. (Lead 三判 = 真因) cell vertex 用 cell.fg + glyph 字形也用 fg, 同色覆盖致 alpha mask "涂同色等于不可见" — D fixup 落地

**INV-010 第 10 次应用零真违规** (5 grep 含 1 doc comment 假阳, T-0403 audit P0-1 同源); **INV-002 字段顺序未动** (Renderer 14 字段 + GlyphAtlas 内部字段不变, 仅 allocations HashMap key 类型变 — 不破字段顺序契约); **6 主动告知偏离 + D fixup enum vs simplify 抉择** 我独立逐条 verify 全部 spec-sanctioned + 实施合理。

---

## P0: 无阻塞

### [P0-1] INV-010 strict reading 第 10 次应用 ✅✅

**5 条独立 grep 复跑** (沿袭 T-0399 + T-0401..T-0403 验证 SOP):

```
$ cd /home/user/quill-impl-face-lock

$ grep -rnE 'pub use (alacritty|wgpu|wayland|cosmic)' src/
(零命中) ✅

$ grep -rnE 'impl From<(alacritty|wgpu|wayland|cosmic)' src/
(零命中) ✅

$ grep -rnE 'pub.*cosmic_text::' src/
src/text/mod.rs:531:    /// **不 pub** (INV-010 strict reading 第 9 次应用): cosmic-text [`cosmic_text::CacheKey`]
   ↑ 1 命中, doc comment 内 "**不 pub**" 反例陈述 (T-0403 P0-1 假阳同源)

$ grep -rnE 'pub\(crate\).*cosmic_text::' src/
(零命中) ✅

$ grep -rnE 'cosmic_text' src/ | grep -v 'src/text/mod.rs'
(零命中) ✅
```

**fontdb / Family / FontSystem / SwashCache / LayoutGlyph 公共 API 复查**:
```
$ grep -rnE 'pub.*(fontdb::|Family::|FontSystem|SwashCache|LayoutGlyph)' src/
(零命中) ✅
```

**T-0407 新加类型边界 verify**:
- `pub const PREFERRED_MONOSPACE_FACES: &[&str]` ✅ (`&str` 是 std 类型, 不暴露 fontdb)
- `pub fn TextSystem::primary_face_id(&self) -> u64` ✅ (返 quill 自定义 u64 hash, 不暴露 `fontdb::ID`)
- `pub struct GlyphKey { face_id: u64, glyph_id: u16, font_size_quantized: u32 }` ✅ (3 字段全 quill 基础类型, 无 cosmic-text)
- `pub fn ShapedGlyph::atlas_key(&self) -> GlyphKey` ✅ (返类型 quill 自定义)
- `pub fn ShapedGlyph::face_id(&self) -> u64` ✅ (返 u64 hash)

**模块私有边界 verify**:
- `TextSystem.primary_face_id: cosmic_text::fontdb::ID` — 字段无 `pub`, 模块私有 ✅
- `TextSystem.primary_face_name: String` — 字段无 `pub`, 模块私有 ✅
- `TextSystem.emoji_face_ids: HashSet<cosmic_text::fontdb::ID>` — 字段无 `pub`, 模块私有 ✅
- `fn fontdb_id_to_u64(id: cosmic_text::fontdb::ID) -> u64` — 模块顶级私有 fn (无 pub) ✅
- `ShapedGlyph.cache_key: cosmic_text::CacheKey` — 字段无 `pub`, 模块私有 (T-0403 起) ✅
- `fn apply_emoji_blacklist(&self, ...)` — `impl TextSystem` 内私有 inherent fn ✅

**INV-010 类型隔离 SOP 第 10 次应用** ✅ (T-0302 / 0303 / 0304 / 0305 / 0306 / 0399 / 0401 / 0402 / 0403 / **0407 face 锁定 + GlyphKey + emoji 黑名单**)。

### [P0-2] Family::Name 真用上替代 Family::Monospace ✅✅

派单 In #A 关键: shape_line / shape_one_char 改 `Family::Name(primary_face_name)` 显式锁 face, 替代 T-0401 `Family::Monospace` generic hint。

**实装** `src/text/mod.rs::shape_one_char` line 302:
```rust
let attrs = Attrs::new().family(Family::Name(&self.primary_face_name));
```

**实装** `src/text/mod.rs::shape_line` line 417:
```rust
let attrs = Attrs::new().family(Family::Name(&self.primary_face_name));
```

✅ 两处都改, `Family::Monospace` 在生产代码零留 (`grep -n "Family::Monospace" src/text/mod.rs` 仅命中 doc comment 历史描述)。

### [P0-3] PREFERRED_MONOSPACE_FACES + 启动期 face 锁定 ✅✅

**`PREFERRED_MONOSPACE_FACES`** const (line 37-43):
```rust
pub const PREFERRED_MONOSPACE_FACES: &[&str] = &[
    "DejaVu Sans Mono",
    "Source Code Pro",
    "Liberation Mono",
    "JetBrains Mono",
    "Noto Sans Mono",
];
```

5 个 face 派单字面要求一致 ✅。

**TextSystem::new 锁定逻辑** (line 119-227):

1. 单 pass 扫 `font_system.db().faces()`, 同时记 primary 候选 + emoji 黑名单
2. PREFERRED 命中: `position(|p| p.eq_ignore_ascii_case(face_family))` — case-insensitive 匹配
3. 选 idx 最小者 (优先级最高), 不提前 break (writer 注释 "face 总数 ~50, 全扫开销可忽略 — 简化逻辑")
4. fallback: `monospace_fallback` (任意 monospaced face), 仅 `is_none()` 时设第一个遇到的
5. PREFERRED 未命中 + fallback 命中 → tracing::warn + 用 fallback
6. PREFERRED + fallback 全没 → return Err (兼容 T-0401 行为)
7. 用 `fontdb::Query` 解析 primary_face_name → `primary_face_id: fontdb::ID` (writer 注释引"同 family 多 face style, query 跑同套 CSS-like 匹配规则保 primary_face_id == LayoutGlyph.cache_key.font_id, 测试 shape_ascii_uses_primary_face 锁此契约")

**writer 主动告知 #2 (primary_face_id 用 fontdb::Query)** 我独立 verify:
- 派单 In #A 字面 "记下 face_id 存为 TextSystem 字段" 暗示扫描时取 face.id
- writer 偏离: 扫描记 face_name, 然后 fontdb::Query 解析 face_id
- 偏离理由: 同 family 名可能多 face (Book/Bold/Italic), 扫描遇到的第一个未必匹配 cosmic-text shape 实际选中的; query() 跑同套 CSS-like 匹配规则保 primary_face_id == LayoutGlyph.cache_key.font_id

**独立判**: ✅ **接受偏离** — 关键不变式是"primary_face_id == ShapedGlyph.cache_key.font_id" (测试 `shape_ascii_uses_primary_face` 锁此); 字面扫描法易跑出 face_id 与 shape 实际 face_id 不一致 (Book vs Regular), 偏离修正语义。Goal-driven correction 沿袭 T-0305 P0-6 + T-0306 + T-0403 P0-3 模式。

**tracing::info / warn 路径** 派单 Acceptance "log 选了哪个 face":
- PREFERRED 命中: `tracing::info!(primary_face = %p.1, "TextSystem::new: locked PREFERRED monospace face")` ✅
- fallback 命中: `tracing::warn!(fallback_face = %fb.1, preferred = ?PREFERRED_MONOSPACE_FACES, "...")` ✅
- emoji 数量: `tracing::info!(emoji_face_count = ..., "TextSystem::new: emoji face blacklist populated")` ✅

### [P0-4] emoji face 黑名单 ✅✅

**扫 fontdb 案例 insensitive 检测** (line 142-145):
```rust
if face_family.to_lowercase().contains("emoji") {
    emoji_face_ids.insert(face.id);
    continue;
}
```

✅ Arch 标准命名 "Noto Color Emoji" 命中 (`to_lowercase` → "noto color emoji" 含 "emoji")。"Color Emoji" / "Twemoji" / "Apple Color Emoji" 均命中。

**HashSet 类型** `HashSet<cosmic_text::fontdb::ID>` 模块私有 ✅; O(1) contains 检查给 hot-path apply_emoji_blacklist 性能保障 ✅。

`continue` 让 emoji face 不进 PREFERRED / fallback 候选 — 防"用户机仅装 emoji 作 monospaced face 时锁错"的退化路径。✅。

### [P0-5] emoji post-process glyph_id := 0 ✅ (含 1 P3 跟进)

**`apply_emoji_blacklist`** (line 330-335):
```rust
fn apply_emoji_blacklist(&self, mut g: ShapedGlyph) -> ShapedGlyph {
    if self.emoji_face_ids.contains(&g.cache_key.font_id) {
        g.glyph_id = 0;
    }
    g
}
```

shape_line / shape_one_char 后流式 `apply_emoji_blacklist` post-process。

**writer 主动告知 #1 (emoji 替换策略)** 我独立 verify:

派单 In #C 写: "替换为 .notdef tofu (glyph_id=0)" — 字面读为完整 .notdef 渲染 (re-shape 主 face 拿 .notdef cache_key)。

writer 偏离: 仅置 ShapedGlyph 公共字段 `glyph_id = 0`, **cache_key 不变**。

**逻辑链**:
1. emoji glyph 走 `apply_emoji_blacklist` → `g.glyph_id = 0`, cache_key 仍指 emoji face
2. 渲染层调 `text_system.rasterize(&glyph)` 走 `swash_cache.get_image_uncached(glyph.cache_key)` → emoji face 给 `Color` content
3. `if img.content != Mask { return None; }` → 返 None
4. `allocate_glyph_slot` 收到 None → 短路 `?` 返 None → 不进 atlas, 不画字形
5. 该 cell 仅显 cell 色块 (D fixup 后是 cell.bg = 黑色), 视觉无 emoji 图

✅ **行为等价"派单字面 .notdef 渲染"的视觉效果** (cell 色块显示, 无彩色 emoji), 实施复杂度低 — 派单字面方案需构造主 face .notdef cache_key, 涉及 fontdb 二次查询 + cosmic-text 私有 API。**接受偏离** ✅。

**[P3-1] 公共 glyph_id ≠ cache_key.glyph_id 不一致**:
- `apply_emoji_blacklist` 后 `g.glyph_id = 0` 但 `g.cache_key.glyph_id` 仍是原 emoji gid
- `g.atlas_key()` 实装 `self.cache_key.glyph_id` (line 612), **不读** `self.glyph_id` 公共字段
- 后果: atlas_key 仍含原 emoji gid, 但 rasterize 必返 None → allocations 不进缓存 — **正确性维持**, 但语义上"glyph_id 字段含义双值化" (公共字段是 "tofu 标志", cache_key.glyph_id 是 "原始 gid"); 测试 `shape_emoji_codepoint_returns_tofu_or_skipped` 仅查公共字段
- **P3 不阻塞** — writer 选择简化 (不重 shape) 是合理 trade-off, 注释 line 326-329 已说"why 仅置 glyph_id = 0: ShapedGlyph 公共字段 `glyph_id` 渲染层不直接读 (renderer 读 `x_advance` / `x_offset` / `atlas_key()`); zero 主要给测试 / 排障"
- T-0408 / T-0405 重审时若需要 emoji glyph 真画 .notdef bitmap, 升级路径明确 (rasterize 路径加"face_id 在黑名单时构造主 face .notdef cache_key 二次 raster")

### [P0-6] GlyphKey 三维 struct ✅✅

**实装** (line 647-657):
```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub struct GlyphKey {
    pub face_id: u64,
    pub glyph_id: u16,
    pub font_size_quantized: u32,
}
```

派单 In #B 全部要求命中:
- ✅ struct (推后者 over tuple, "更稳")
- ✅ face_id: u64 (面 id 升维)
- ✅ glyph_id: u16 (OpenType gid)
- ✅ font_size_quantized: u32 (`f32::to_bits` 量化)
- ✅ Hash + Eq + PartialEq 派生 (HashMap key 必需)
- ✅ Clone + Copy (渲染层值复制)
- ✅ Debug (排障日志)

**渲染层 HashMap 类型同步** `src/wl/render.rs:152`:
```rust
allocations: HashMap<GlyphKey, AtlasSlot>,
```

(替代 T-0403 `HashMap<(u16, u32), AtlasSlot>`) ✅。

`allocate_glyph_slot` line 1327 `let key = glyph.atlas_key();` — 类型自动推导 GlyphKey ✅。

**测试 `atlas_key_includes_face_id`** (line 1037-1060) 直接锁 GlyphKey equality 契约: face_id / glyph_id / font_size_quantized 任一不同 → key 不等; 三维全同 → key 等 + Hash 同。✅

### [P0-7] enum CellColorSource { Fg, Bg } (D fixup) ✅✅

**实装** (`src/wl/render.rs:251-258`):
```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
enum CellColorSource {
    Fg,
    Bg,
}
```

模块私有 (无 pub) ✅; 派生 Copy/Clone (传值) + PartialEq/Eq (match 用) + Debug (排障) ✅。

**build_vertex_bytes 加 color_source 参数** (line 778):
```rust
fn build_vertex_bytes(
    &self,
    cells: &[CellRef],
    cell_w_px: f32,
    cell_h_px: f32,
    surface_w: f32,
    surface_h: f32,
    color_source: CellColorSource,    // ← T-0407 D fix 加
) -> Vec<u8> {
```

**color match** (line 803-806):
```rust
let color = match color_source {
    CellColorSource::Fg => self.color_for_vertex(cell.fg),
    CellColorSource::Bg => self.color_for_vertex(cell.bg),
};
```

exhaustive match 无 `_ =>` ✅ — Phase 5+ 加 Selection 高亮等 variant 时编译期 catch。

### [P0-8] draw_cells 仍走 Fg (T-0305 视觉契约不破) ✅✅

**实装** (`src/wl/render.rs::draw_cells` line 559-569):
```rust
let vertex_bytes = self.build_vertex_bytes(
    cells,
    cell_w_px,
    cell_h_px,
    surface_w,
    surface_h,
    // Phase 3 fallback 路径 (text_system 未建好时降级): cell 染 fg 色作
    // 视觉锚点 (T-0305 acceptance "看见 prompt 字符位置以色块画出")。
    CellColorSource::Fg,
);
```

✅ 注释引 T-0305 acceptance + 解释 fallback 路径; T-0305 P0-6 fg 决策契约维持。

**调用路径** (window.rs idle callback): text_system None → draw_cells (fg 色块, T-0305) / text_system Some → draw_frame (bg 色块 + glyph fg)。两路径并存, fallback 视觉契约不破。

### [P0-9] draw_frame 走 Bg (Phase 4 主路径让 glyph 可见) ✅✅

**实装** (`src/wl/render.rs::draw_frame` line 898-907):
```rust
// Step 3: cell vertex bytes (T-0407 D fix: 走 bg 色, 让 glyph fg 字形
// 在 cell bg 块上可见; T-0403 用 fg 致字形被 cell fg 块"涂同色"不可见,
// 用户实测看到一片连续 fg 色矩形不见字)。
let cell_vertex_bytes = self.build_vertex_bytes(
    cells,
    cell_w_px,
    cell_h_px,
    surface_w,
    surface_h,
    CellColorSource::Bg,
);
```

✅ 注释引 T-0403 真因 + D fix 路径。

### [P0-10] cell.bg 默认值是 #000000 black, 与 fg #d3d3d3 高对比 ✅

**Lead 重点 verify 项**: D fixup 真解 bug 的逻辑链 (cell.bg 默认值是什么? glyph fg 跟 cell.bg 真不同色?)

**独立 verify** `src/term/mod.rs::Color`:
- `Color::DEFAULT_FG = Color { r: 0xd3, g: 0xd3, b: 0xd3 }` (line 208-212) — light gray #d3d3d3
- `Color::DEFAULT_BG = Color { r: 0x00, g: 0x00, b: 0x00 }` (line 218-222) — pure black #000000
- `named_color_rgb(N::Foreground) → DEFAULT_FG` (line 334) ✅
- `named_color_rgb(N::Background) → DEFAULT_BG` (line 335) ✅

**测试 verify cell 默认值** (`src/term/mod.rs::tests`):
- `cellref_carries_fg_and_bg`: line 1304 "默认 cell.fg 应解析到 DEFAULT_FG" + line 1309 "默认 cell.bg 应解析到 DEFAULT_BG" — 两默认值断言锁住 ✅

**视觉链** (D fixup 后 draw_frame 路径):
1. `clear_color = #0a1030` 深蓝 (T-0102 起手)
2. cell pass (BlendState::REPLACE, line 718) → cell.bg = #000000 black 覆盖深蓝
3. glyph pass (BlendState::ALPHA_BLENDING, line 1267) → glyph fg = #d3d3d3 light gray 在 black 上 alpha mask blend

**对比度**:
- glyph fg #d3d3d3 (211, 211, 211) vs cell bg #000000 (0, 0, 0) → ΔE ≈ 84 (CIELAB Δ极高对比) ✅
- T-0403 bug 路径: glyph fg #d3d3d3 vs cell fg #d3d3d3 → 同色, ΔE = 0 → alpha 1.0 时仍是 #d3d3d3 看不出字 ✅ 真因确认

**D fixup 真解 bug 的逻辑链 verify** ✅。

### [P0-11] INV / 硬约束 ✅

**独立 grep verify**:
```
$ git -C /home/user/quill-impl-face-lock diff main..HEAD -- docs/invariants.md src/pty src/main.rs Cargo.toml Cargo.lock src/wl/window.rs
(零输出) ✅

$ grep -nE "^[+-]\s*(surface|cell_|glyph_|device|queue|config|clear|surface_is_srgb|instance):" \
    <(git -C /home/user/quill-impl-face-lock diff main..HEAD -- src/wl/render.rs)
(零输出) ✅ Renderer struct 字段顺序 + GlyphAtlas struct 字段顺序未动

$ grep -nE 'thread::spawn|ahash|fxhash|etagere|guillotière' src/text/mod.rs src/wl/render.rs
(零命中) ✅
```

**INV-001..010 兼容**:
- INV-001 State 字段顺序未动 ✅ (T-0407 不改 src/wl/window.rs)
- INV-002 Renderer 字段顺序 14 字段维持 ✅ (T-0407 仅改 GlyphAtlas.allocations 内部 HashMap key 类型, 字段名 + 顺序不变)
- INV-002 GlyphAtlas 内部字段顺序 (T-0403 起 6 字段 bind_group / bind_group_layout / view / sampler / texture / allocations / cursor*) 维持 ✅
- INV-005 calloop 单调度器 ✅ (TextSystem 不持 fd, fontdb scan 是 sync 内存操作)
- INV-006 resize_dirty 消费契约 ✅ (T-0407 不改 propagate_resize_if_dirty)
- INV-008/009 PTY 不变式不动 ✅
- **INV-010 类型隔离 5 grep 全零真命中** ✅ (P0-1 严格 verify, doc comment 假阳)

**零新 ADR** ✅ (`PREFERRED_MONOSPACE_FACES` 是配置常数 + cosmic-text fontdb::Query 是既有 API, 不需 ADR)。
**零新 crate** ✅ (`Cargo.toml` 零改动, std::collections::HashSet 已用)。
**零合并标记** (`grep -nE '<{7}|={7}|>{7}' src/` 零命中, fmt --check 通过) ✅。

### [P0-12] 四门 ✅ (独立复跑)

```
$ cd /home/user/quill-impl-face-lock

$ cargo build
Finished `dev` profile [optimized + debuginfo] target(s) in 0.04s ✅

$ cargo clippy --all-targets -- -D warnings
Finished `dev` profile [optimized + debuginfo] target(s) in 0.05s ✅ (零 warning)

$ cargo fmt --check
fmt OK (零输出) ✅

$ cargo test
running 79 tests (unit) → ok    含 5 new T-0407 text::tests:
    - face_lock_uses_preferred_monospace ✅
    - shape_ascii_uses_primary_face ✅
    - shape_emoji_codepoint_returns_tofu_or_skipped ✅
    - atlas_key_includes_face_id ✅
    - atlas_key_face_id_matches_primary_for_ascii ✅
running 0 tests (lib doc) → ok
running 2 tests (frame_stats_integration) → ok
running 3 tests (frame_stats_smoke) → ok
running 2 tests (glyph_atlas) → ok    (含 atlas_handles_multiple_glyphs_no_collision 改 GlyphKey 类型)
running 2 tests (ls_la_e2e) → ok
running 2 tests (pty_calloop_smoke) → ok
running 2 tests (pty_echo) → ok
running 3 tests (pty_resize) → ok
running 2 tests (pty_to_term) → ok
running 2 tests (resize_chain) → ok
running 11 tests (state_machine) → ok
TOTAL: 79 unit + 31 integration = 110 tests pass ✅

writer 自报 110, 实测 110 (105 → 110, +5 unit) 一致。派单 Acceptance "总测试 105 + 4 ≈ 109 pass" (实际 +5 pass 110, 派单数粗估 ±1) ✅。
```

**四门全绿** ✅。

### [P0-13] 手测 acceptance (派单 #E) ⚠️ writer 自报 verified

writer 在主体 commit body 自报:
> 手测 (cargo run --release): RUST_LOG=info 看到 `primary_face = DejaVu Sans Mono` + `emoji_face_count = 1` (Noto Color Emoji) 锁定生效; draw_frame trace cell 90 / glyph 90 / atlas 12 与 T-0403 相同 (15 字符 prompt 路径), face 选择已修复

D fixup commit body 自报:
> 手测 (cargo run --release): trace 数学维持 cell 90 / glyph 90 / atlas 12 不变 (没改 vertex 数量 / 几何), 视觉变 cell 染色源; agent 跑不动 GUI 物理验证, Lead 人工验收 "看到 15 个独立 ASCII 字符不是连续矩形"。

**reviewer 物理限制**: agent 跑不动 GUI 窗口手测 (Wayland session 在 user 桌面, agent 在 tmux), 沿袭 T-0305 / T-0306 / T-0403 三层证据替代。**接受 writer trace 数学 + 配置 log + Lead 人工验收**。

---

## P1: 无违规

### 生产零 unwrap / expect / println / unsafe / thread::spawn ✅

**独立 grep**:
```
$ awk 'NR<=659 && /\.expect\(|unwrap\(\)|println!|thread::spawn/ {print}' src/text/mod.rs
(零输出) ✅ (line 660 起为 #[cfg(test)] mod tests)

$ grep -nE 'unwrap\(\)|\.expect\(|println!|thread::spawn' src/wl/render.rs
(零命中) ✅
```

**T-0407 改动零新 unsafe** ✅ (既存 unsafe 在 init_renderer_and_draw / wayland_fd / pty_borrowed 不动)。

**Option / Result 处理用 `?` / `.ok_or_else(|| anyhow!())`**:
- `font_system.db().query(&query).ok_or_else(|| anyhow!(...))?` (text/mod.rs:206-213) ✅
- shape 后 chain 用 `.next().and_then(...).map(...)?` (text/mod.rs:309-313) ✅
- rasterize 用 `?` (line 364-367) ✅
- allocate_glyph_slot 用 `?` (render.rs:1335) ✅

### 注释 why 非 what ✅

- `PREFERRED_MONOSPACE_FACES` doc (text/mod.rs:24-36) 引派单 In #A "顺序锁死防"新 face 加入插队优先级混乱"" + 用户机命中 + CI 退化路径
- `TextSystem` struct doc (line 45-70) 引 INV-010 + face 锁定 + emoji 黑名单 + 字段顺序无观测者
- `primary_face_id` field doc (line 83-87) 引 INV-010 守 + 公共 API 走 hash
- `emoji_face_ids` field doc (line 93-97) 引派单已知陷阱 + HashSet 性能
- `TextSystem::primary_face_id()` doc (line 229-243) 引 u64 hash 决策 + 测试用
- `fontdb_id_to_u64` doc (line 246-256) 引 DefaultHasher / 同进程稳定性 / 不优化理由
- `apply_emoji_blacklist` doc (line 316-329) 引派单 In #C 简化路径 + cache_key 不变 + glyph_id 0 语义
- `GlyphKey` doc (line 631-646) 引 T-0403 audit P2-2 / writer 主动告知 #5 + INV-010 第 10 次 + 派生 trait 用途
- `ShapedGlyph::atlas_key` doc (line 591-608) 引 T-0407 修 T-0403 P3 跟进 + face_id 维度
- `ShapedGlyph::face_id` doc (line 617-625) 引测试用 + 同源算法
- `CellColorSource` enum doc (render.rs:244-254) 引 T-0305 doc 预言 + T-0407 D 修 + 调用方对应
- build_vertex_bytes 内 color match 注释 (line 794-803) 引 T-0407 D fix 真因 + T-0305 doc 预言

**注释 traceability 完整**, conventions §2 "B. 引 audit / ticket / ADR / INV 编号" 全程命中 ✅。

### Commit message 引 INV-010 + T-0305 P0-6 + 派单 scope ✅

`98b72e4` (主体 A+B+C) body 4 段齐 (What / Why / 副作用 / 测试):
- "What 改了" 5 项分类 (加 / 加 / 改 / 改 / 改测试) ✅
- "Why 这么改" 引 T-0403 实测 P0 bug + 用户 Lead 诊断三件根因 + 派单 In #A/#B/#C + INV-010 第 10 次应用 ✅
- "副作用 / Drop-in 兼容" 含 **派单 vs 实装偏离主动告知** 段 (2 项: emoji 替换简化 / fontdb::Query 选 face_id) ✅
- "测试 + diff stat" 5 新测试 + 105→110 + 4 门状态 + INV-010 5 grep + 手测 RUST_LOG trace + INV-001..009 兼容 + 不动清单 ✅

`6620cd1` (D fixup) body 4 段齐:
- "What 改了" 5 项 (加 enum / 加参数 / draw_cells 传 Fg / draw_frame 传 Bg / 临时 trace 删) ✅
- "Why 这么改" 引 Lead 第二轮诊断 (用户截图复审) + trace verify 几何/UV 排除 UV bug + T-0305 doc 早预言此路径 + 不动 WGSL/cell pipeline ✅
- "副作用 / Drop-in 兼容" 含派单 vs 实装偏离 (D scope Lead 派单更新 sanctioned, 派单原文不含) ✅
- "测试 + diff stat" 4 门 + 110 维持 + diff 1 文件 +48/-6 + 手测 trace 数学维持 + INV / 硬约束清单 ✅

`3786962` Status commit 单文件 ±1, 符合 conventions §4 Step 4 "Status 变更**独立 commit**" 模板 ✅。

HEREDOC 格式正确 (cmd 行无 `\r\n` 撞) ✅。

---

## P2: API 设计 / 架构判断

### [P2-1] enum CellColorSource vs Lead 提议 1 行 simplify (Lead 重点判) ✅ writer 选 enum 接受

**Lead 提议**: 1 行 simplify — 例 `let color = self.color_for_vertex(cell.bg);` 直接 hardcode bg, 删 enum 抽象。

**writer 选**: enum 保留两路径。

**writer 论据** (commit body 隐含 + 注释 line 244-254):
1. T-0305 P0-6 视觉契约: draw_cells fallback (text_system None 时) 走 fg 色块作"看见 prompt 字符位置"
2. Phase 4 主路径 draw_frame 走 bg 让 glyph fg 可见
3. 1 行 simplify (hardcode bg) 会破 T-0305 fallback 视觉契约 (fallback 路径 cell 也变 bg 黑色, T-0305 acceptance "看见 prompt 字符位置以色块画出" 需要 fg 色块作锚点)

**独立评估**:

| 路径 | 优 | 劣 |
|---|---|---|
| **A. enum CellColorSource (writer 选)** | 两路径并存语义清晰; fallback 视觉契约维持; Phase 5+ 加 Selection 高亮等 variant 编译期 catch | enum 2 variant 在 Phase 4 看似抽象冗余; +12 行 (vs 1 行 hardcode) |
| B. 1 行 simplify (Lead 提议) | 代码最小; KISS | 破 T-0305 fallback 视觉契约; 未来加 selection 高亮路径需重新引入 enum |

**关键判**: 派单 D scope 写 "draw_cells 仍 Fg (T-0305 fallback 视觉契约)" — Lead 派单更新已**显式要求保留两路径**。writer 选 A 与派单字面一致, Lead 接受 (Lead 提议 1 行 simplify 是替代方案的 brainstorm, 派单字面 D 已收敛到 enum 路径)。

**独立判**: ✅ **接受 enum 路径** — T-0305 fallback 视觉契约确实需要保留 (text_system 加载失败 graceful degradation 在 CI / 退化系统), 1 行 hardcode 会偷偷破契约; +12 行 enum 抽象代价低, 编译期 exhaustive match 给未来 variant 加保护; Phase 5+ Selection 高亮 / IME composition 等真有第 3+ variant 需求时 enum 已就位。

### [P2-2] D fixup 跟 T-0407 主体 (A+B+C) 一起合是否合理 (vs 拆两单) ⚠️ 一起合接受

**两选项**:
- A. 一起合 (writer 选): T-0407 一单含 A+B+C+D, 3 commits 累加
- B. 拆两单: T-0407 主体 (A+B+C) 一单 + T-0407B / T-0408 D fixup 一单

**一起合论据**:
- Lead 三轮诊断逐步收敛真因, 每轮加补丁; A+B+C 防御性预修 (atlas key + face lock + emoji) 与 D 真因修联动 — D 单独修不解 "用户截图连续矩形" 因 emoji 也仍是 emoji-color-fallback 路径误用
- 单 ticket 历史可追溯 (3 commits 完整记录三轮诊断)
- diff 总量适中 (491/-61, 4 文件), 符合 conventions §1 "diff < 300 行" 仅本单稍超 (派单 budget 80k 适当)

**拆两单论据**:
- conventions §1 "一次 commit 做一件事" 严格读应 D fixup 单独 ticket
- D fixup 是 T-0305 视觉契约的 Phase 4 cleanup, 与 face 锁定无依赖

**独立判**: ⚠️ **接受一起合** — Lead 派单更新 sanctioned (派单第三轮加 D scope), writer 在 D fixup commit body 显式说明 "派单 vs 实装偏离 (沿袭 conventions §5): T-0407 派单 In #B/#C 是 reviewer T-0403 P0 警告的预防性修, Lead 第二轮诊断后**降级 emoji 优先级 + 加 D scope (cell color fix)**, 本 commit 是 D scope 落地"。**Lead approval 派单更新优先, conventions §1 仅默认 idiom**。

**P2 微观察**: 未来类似"Lead 派单中加 scope" 路径建议 ticket id 加 sub (例 T-0407.1) 或独立 ticket id (T-0408)。Phase 4 后续 ticket 可参 — 当前 T-0407 一起合接受。

### [P2-3] enum variant 命名 Fg / Bg vs Foreground / Background ✅

**实装**: `Fg` / `Bg`。

**Foreground / Background 替代评估**:
- Pro Foreground/Background: 更显式; 与 alacritty NamedColor::Foreground / Background 一致 (T-0305 既有命名)
- Pro Fg/Bg: 与 CellRef.fg / CellRef.bg 字段名一致 (T-0305 起 quill 既有 idiom); match 行短小; CellColorSource::Fg / CellColorSource::Bg 自描述充足

**独立判**: ✅ **接受 Fg/Bg** — 与 quill `cell.fg` / `cell.bg` 字段名一致是更强约束 (枚举 source 选 fg vs bg 字段, 命名同源减认知开销); alacritty NamedColor::Foreground / Background 是上游类型 (INV-010 不暴露), quill 不需对齐。

### [P2-4] D fixup 修法不动 WGSL 是否合理 ✅

**实装路径**: 仅改 vertex 数据 (color_source 选 fg vs bg), 不改 WGSL shader (`CELL_WGSL` line 不变)。

**替代路径**: 改 WGSL fragment shader 加 uniform / push_constant 选 fg/bg。

**独立判**: ✅ **接受不动 WGSL** — vertex attribute color 是 vertex pipeline 既有数据通路 (T-0305 起), 切换 fg vs bg 是 vertex 数据选择, 不属 shader 责任; 改 shader 加 uniform 需新 BindGroupLayout / push_constant 配置, 复杂度 ×3, 性能无获益 (vertex 阶段决定颜色与 fragment uniform 选择对 GPU 性能等价)。"派单 #B atlas key + face_id" 已耗 Phase 4 复杂度预算, D fixup 越简越好。

---

## P3: 建议 (不阻塞, 跟进)

### [P3-1] 公共 glyph_id ≠ cache_key.glyph_id 不一致 (P0-5 详述)

apply_emoji_blacklist 后 `g.glyph_id = 0` 但 `g.cache_key.glyph_id` 未变。当前路径正确性维持 (rasterize 走 cache_key 必返 None), **不阻塞**, 已写注释。T-0408 / T-0405 重审若需要 emoji glyph 真画 .notdef bitmap 升级。

### [P3-2] f32::to_bits 跨字号稳定性 (Lead 重点验) ✅

**Lead 顾虑**: T-0404 HiDPI 17 → 34 边界, font_size_quantized = `f32::to_bits(font_size_px)` 稳吗?

**独立 verify**:
- `f32::to_bits(17.0)` = `0x41880000` (IEEE-754 标准 binary32 表示)
- `f32::to_bits(34.0)` = `0x42080000` (同 IEEE-754)
- 两值不同 → atlas key 不冲突 ✅

**`f32` IEEE-754 二进制表示稳定性**:
- IEEE-754 标准锁死 binary32 比特模式, 不同进程 / 平台 / Rust 版本必同
- `f32::to_bits` 是 transmute 等价 (无浮点运算 / 比较), 不引入 NaN bit-pattern 不稳定问题
- `Metrics(17.0, 25.0)` (T-0306) → font_size_bits = 0x41880000 稳定; T-0404 改 `Metrics(34.0, 50.0)` → font_size_bits = 0x42080000 稳定

**P3 不阻塞** ✅ — `f32::to_bits` 是 IEEE-754 标准操作, T-0404 HiDPI 切换字号时 font_size_quantized 变化但稳定可比, atlas key 无漂移。

### [P3-3] glyph_diag 临时 trace 删干净 ✅

**Lead 关注点**: writer 自报"加临时 trace 拿数据", 现在 commit 应该不留。

**独立 verify**:
```
$ grep -nE 'glyph_diag|TRACE.*glyph|debug!.*diag' src/
(零命中) ✅
```

D fixup commit body 显式 "临时诊断 trace `quill::wl::render::glyph_diag` 已删" ✅。

`tracing::debug! / info! / warn! / error! / trace!` 残留全部是生产 log (T-0102 起手 + T-0399 frame_stats + T-0407 face lock 选择), 无临时 diag spam。✅

### [P3-4] T-0405 cascading 简化 (writer 自报缩为 CJK fallback verify 一单) ✅

**writer 在派单 message "给 T-0404 / T-0405 的影响" 段** + T-0407 主体 commit 都暗示 T-0405 主体可缩为 "中文字符确实 fallback 到 Noto CJK 验证"。

**独立判 T-0405 缩范围合理性**:
- T-0407 已落 face 锁定 (Family::Name + primary_face_id) → CJK fallback 路径 cosmic-text 内部 fontdb fallback chain 直接工作 (主 face DejaVu Sans Mono 不含 CJK → 自动 fall back Noto CJK)
- T-0407 已落 atlas key 加 face_id (GlyphKey 三维) → CJK fallback face 与主 face glyph 不撞 atlas
- T-0405 剩余 scope: 端到端 verify CJK 字符真画出 (`shape "你好"` → 渲染层 atlas 命中 Noto CJK face 的 glyph) + 可能加显式 CJK family 优先级 (Source Han Sans / Noto CJK 选最优)

**P3 给 T-0405 派单建议**:
- 主体: 1 集成测试 (`shape "你好abc" + raster verify face_id ≠ primary_face_id, glyph 数 5`) — 当前 T-0407 仅 lib test, 集成测试覆盖 atlas slot 真分配跨 face
- 可选: 加 PREFERRED_CJK_FACES 类似常数 (Noto Sans CJK SC / Source Han Sans / 等), TextSystem 加 fallback chain 显式锁
- 不需要: emoji 排除 / atlas face_id (T-0407 已落)

### [P3-5] writer 6 主动告知 + 1 D fixup 决策逐条复验 (零遗漏)

| # | 主动告知 | 我独立复验 |
|---|---|---|
| 1 (主体) | emoji 替换策略 (派单字面 .notdef tofu vs 实装仅置 glyph_id=0 + cache_key 不变) | P0-5 + P3-1 verify, rasterize 走 cache_key 必返 None → 渲染层跳过, 行为等价"派单字面"视觉效果 ✅ |
| 2 (主体) | primary_face_id 用 fontdb::Query 查找 (扫描法易跑出 face_id 不一致 Book vs Regular) | P0-3 verify, fontdb::Query Weight::NORMAL/Style::Normal/Stretch::Normal 跑同套规则保 primary == cache_key.font_id ✅ |
| 3 (主体) | INV-010 类型隔离第 10 次应用 | P0-1 verify, 5 grep 全零真命中 (1 doc comment 假阳同源) ✅ |
| 4 (主体) | INV-001/002/005/006/008/009 兼容 (字段顺序 + 不动清单) | P0-11 verify, INV-002 字段顺序 + GlyphAtlas 内部字段 + src/pty/main/Cargo.toml 全维持 ✅ |
| 5 (主体) | atlas_key 返类型 break (u16, u32) → GlyphKey 接受 (callers 仅 GlyphAtlas.allocations 一处) | P0-6 verify, render.rs:152 + tests/glyph_atlas.rs 同步修, Phase 4 内无外部 callers ✅ |
| 6 (主体) | 5 新测试覆盖 (face_lock / shape_ascii_uses_primary / shape_emoji_tofu / atlas_key_includes_face_id / atlas_key_face_id_matches_primary) | P0-12 verify, 5 测试全过 + atlas_key_is_stable_for_same_glyph 改 .glyph_id 字段访问语义维持 ✅ |
| 7 (D fixup) | enum CellColorSource vs Lead 1 行 simplify | P2-1 独立判, T-0305 fallback 视觉契约必保, enum 路径接受 ✅ |

**无遗漏 critical 主动告知** ✅。

---

## 给后续 ticket 的前置信息

### 给 T-0404 (HiDPI 2x rebase)

- **T-0404 已暂停等 T-0407, 现在合后可 rebase / re-claim**
- **font_size_bits 在 GlyphKey 维度自动跟随** (P3-2 verify): T-0404 切 `Metrics(17.0, 25.0)` → `Metrics(34.0, 50.0)` 时 font_size_quantized 变化但稳定, atlas key 跨字号 hash 不漂移
- **CELL_W_PX / CELL_H_PX / BASELINE_Y_PX 三常数** (T-0403 P3-4 + 本审码沿袭) 仍待 Renderer 字段化, T-0404 HiDPI 是动态 cell pixel 尺寸的自然契机
- **draw_frame 走 Bg 路径** (T-0407 D fixup) 不破 HiDPI: cell 像素尺寸变化由 cell_w_px / cell_h_px 字段控制, color_source 选择正交于尺寸
- **Family::Name 锁 primary_face_name** 已工作, HiDPI 字号 (34pt) 对 PREFERRED_MONOSPACE_FACES 命中无影响 (用户机 DejaVu Sans Mono 全字号都有)
- **GlyphAtlas 容量** (T-0403 P3 + audit P0-5): HiDPI 2x 字号下 atlas 字符容量 ≈ 2700 (vs T-0306 字号 10920), 派单 Out 段 prompt 路径 ASCII 主导 256 字符仍宽裕; T-0406 LRU 视实测决定

### 给 T-0405 (CJK fallback 缩简化)

- **简化范围接受** (P3-4): T-0405 主体可缩为 "中文字符确实 fallback 到 Noto CJK 验证" 单测
  - 1 集成测试 `shape "你好abc" + raster verify face_id 跨 face` (当前 T-0407 lib test 已锁 face_id 维度)
  - 可选: 加 PREFERRED_CJK_FACES 类似常数 + TextSystem fallback chain 显式锁优先级
- **atlas key face_id 维度** (T-0407 GlyphKey) 已落, CJK fallback face 与主 face glyph 不撞 atlas ✅
- **Family::Name 锁主 face** 已工作, cosmic-text 内部 fontdb fallback chain 自动给 CJK face glyph (主 face 缺 codepoint 时), face_id 自动随 fallback 切换
- **不需要做** (T-0407 已覆盖): emoji 排除 / atlas face_id 维度 / face 锁定常数

### 给 T-0408 (headless screenshot)

- **Phase 4 视觉验收** 当前依赖 Lead 人工 cargo run + 截图 (T-0403 / T-0407 三轮诊断都因物理 GUI 限制); T-0408 加 headless screenshot 集成测试可避免该痛点
- **WGSL 渲染路径稳定** (T-0407 后): cell pass (BlendState::REPLACE, fg/bg 染色) + glyph pass (BlendState::ALPHA_BLENDING) + clear #0a1030; 三层视觉栈 trace stable
- **Atlas slot trace 数学** (cell_vertex_count / glyph_vertex_count / atlas_count) 是当前替代验收手段, T-0408 加 framebuffer readback (wgpu queue.copy_texture_to_buffer + bytemuck::cast_slice) 可拿真彩色 PNG 在 CI 比 baseline
- **D fixup 后视觉契约**: 深蓝 #0a1030 仅边缘 / 间隙可见, cells 区域全 black #000000, glyph fg #d3d3d3 在 black 上 — T-0408 baseline 取此 期望像素分布
- **emoji 排除路径**: T-0407 emoji codepoint U+1F4C1 走 .notdef 跳过, screenshot 该 cell 是 black 不是蓝色文件夹 — T-0408 可加 emoji 字符 baseline 验证 (用户机有 Noto Color Emoji)

### 给 mainline-audit 下次跑 (Phase 4 末)

- INV-010 类型隔离 SOP 第 10 次应用零真违规 (T-0302..T-0307 + T-0399 + T-0401..T-0403 + **T-0407**)
- INV-002 字段顺序 14 字段维持; GlyphAtlas 内部字段顺序 (T-0403) 维持; T-0407 仅改 HashMap key 类型不破字段顺序
- TD 状态: Phase 4 ticket 全清; T-0407 D fixup cell color source enum 是 T-0305 视觉契约的 Phase 4 cleanup, 不引新 TD
- Phase 4 进度: T-0401 (shape) / T-0402 (shape_line) / T-0403 (atlas + draw_frame) / T-0407 (face lock + bug fix) — 4/6, T-0404 (HiDPI) + T-0405 (CJK 简化) 待
- **视觉里程碑**: T-0403 算"代码进 atlas 但 bug 不可见", T-0407 真 "看见 prompt 字符 + emoji 不画"; Lead 人工验收实际显示

---

## 整体评价

T-0407 是 T-0403 视觉里程碑的 P0 bug 修复, **跨 3 模块 / 3 commits / 491-61 行 diff**, 经 Lead 三轮诊断逐步收敛真因 (emoji fallback → atlas key 跨 face 撞 → cell+glyph 同色覆盖)。**5 项 P0 派单 scope (A face 锁定 + B GlyphKey + C emoji 黑名单 + D cell color source enum + E 5 新测试) 全落地, 4 门绿 110 tests, INV-010 第 10 次应用零真违规, INV-001..009 全维持**。

**关键架构判断 — D fixup 真因诊断的物理验证 (P0-10)**: 我独立 verify `Color::DEFAULT_FG = #d3d3d3` / `Color::DEFAULT_BG = #000000`, 默认 cell.fg / cell.bg 由 `named_color_rgb(Foreground/Background)` 解析到这两值。T-0403 路径 cell 用 fg + glyph 用 fg → 同色 (ΔE=0) alpha mask 涂同色不可见; T-0407 D fixup 路径 cell 用 bg (黑) + glyph 用 fg (浅灰) → 高对比 (ΔE≈84) — D fixup 真解 bug 的逻辑链确认。这是 **Lead 第三轮诊断 = 真因** 的代码层 verify, 视觉真假以 Lead 人工 cargo run 为准。

**writer 对偏离的主动告知** 6 主动告知 + 1 D fixup 决策齐全, conventions §5 "派单 vs 实装对照, 不静默偏离" 全程命中: emoji 替换简化路径 / fontdb::Query 选 face_id / D fixup enum vs 1 行 simplify / D fixup 与主体一起合 — 每项独立 verify 接受。

**P2-1 enum CellColorSource vs Lead 1 行 simplify 抉择**: writer 选 enum 保留 T-0305 fallback 视觉契约 (text_system None 时 fg 色块作"看见 prompt 字符位置"锚点), 我独立判接受 — 派单 D scope 字面已写"draw_cells 仍 Fg", Lead 1 行 simplify 是 brainstorm 替代方案, 派单字面收敛到 enum。+12 行抽象代价低 vs 视觉契约偷偷破坏的代价高, exhaustive match 给 Phase 5+ Selection 高亮 / IME composition 真有第 3 variant 时编译期 catch。

**审码 SOP 沿袭**: T-0399 + T-0401 + T-0402 + T-0403 5 grep 复跑 / 6 主动告知逐条 verify / Goal-driven correction 模式继承; 本审码 round 2 fresh agent (前任 reviewer-T0407 在 D fixup 派单更新后 kill 重 spawn), 1.5h budget 完成 (Step 0 baseline 1h + Step 1 diff 0.2h + Step 2 复判 0.2h + Step 3 四门 0.05h + Step 4 报告写作 0.5h ≈ 1.95h, 略超 budget 但无 critical 漏失)。

**P3 跟进**: 5 项不阻塞 (公共 glyph_id ≠ cache_key.glyph_id 文档化 / f32::to_bits 跨字号稳 / glyph_diag 临时 trace 已删 / T-0405 缩简化合理 / 6 主动告知 + 1 D fixup 决策逐条 verify)。

**Lead 决策**: ✅ **批准合并** (3 commits 保留 / Option C squash 单 commit, Lead 选)。
