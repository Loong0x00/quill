# 审码报告: T-0504 CSD 自画 titlebar + 3 按钮 + wl_pointer 接入 (Phase 5, GNOME daily-drive)

**审码人**: reviewer-T0504 (per-ticket fresh agent, ASCII name)
**日期**: 2026-04-25
**范围**: feat/T-0504 分支, 3 commits
- `907c5f6` T-0504: claim by writer-T0504
- `9b51bfc` T-0504: CSD 自画 titlebar + 3 按钮 + wl_pointer 接入
- `623f939` T-0504: Status claimed -> in-review

**diff stat** (independently verified `git diff c2cfc28..HEAD --stat`, fork-point `c2cfc28` = T-0504 派单 commit):

```
 src/pty/mod.rs                       |   6 +-      (rustfmt only, 行为零变化)
 src/term/mod.rs                      |  12 +       (新 pub fn mark_dirty, forward-compat 未使用)
 src/wl/mod.rs                        |   1 +       (mod pointer)
 src/wl/pointer.rs                    | 590 +++++   (新文件)
 src/wl/render.rs                     | 395 ++++    (titlebar pipeline + hover 入参 + cells y_offset)
 src/wl/window.rs                     | 216 +++     (Dispatch<WlPointer> + SeatHandler Pointer + State 字段)
 tasks/T-0504-csd-titlebar-pointer.md |   4 +/-     (Status)
 tests/csd_e2e.rs                     | 240 +++++   (新文件, 4 集成测试)
 8 files changed, 1433 insertions(+), 31 deletions(-)
```

writer 自报数与实测**完全一致** (8 文件 +1433/-31).

---

## 判定: ✅ **批准合并**

**全 P0 通过 + 6 偏离全 accept (理由充分, 派单 In scope 内精化或派单允许)。**

T-0504 是 Phase 5 GNOME daily-drive 解锁 P1 — T-0503 已确证 GNOME mutter 政策性
不支持 SSD, user 主桌面没自画 titlebar 拖不动 / 关不掉只能 SIGTERM。本单加
`wl_pointer` 协议 + 自画 CSD (titlebar 灰色矩形 + 3 按钮色块/icon + hover 高亮) +
hit-test 决策 (含 12 单测) + xdg_toplevel.move/set_minimized/set_maximized/close
触发路径完整打通。24 个 lib 单测 (12 hit_test + 12 apply_*) + 4 个集成测试覆盖
核心路径, 4 门 release 全绿 (176 tests passed, 0 failed), INV-001..010 全守, 派单
Out 全守 (src/text / docs/invariants.md / Cargo.toml/lock 零 diff; src/pty 仅
rustfmt 行为零变化), **不引新 crate** (sctk 0.19 / wayland-client 已封装 wl_pointer
+ xdg_toplevel.move/set_minimized/set_maximized, ADR 0007 不需)。

**三源 PNG verify 全过** (writer + Lead + reviewer 三方独立 Read 视觉一致):
- `/tmp/csd_test.png` (47 KiB, 1600×1200 RGBA8): titlebar 顶部深灰 #2c2c2c +
  右上角三按钮 (− Min / □ Max / × Close) 清晰 + bash prompt 在 titlebar 下方
  正常 + 深蓝 #0a1030 clear bg
- `/tmp/csd_reviewer.png` (独立 CLI flag 路径, 1600×1200, 同视觉): 三按钮
  icon / titlebar 灰 / prompt 全一致, 双源 sanity 通过

---

## 1. 4 门 release 独立复跑

```bash
$ cd /home/user/quill-impl-csd

$ cargo build --release
   Finished `release` profile [optimized] target(s) in 0.07s   ✅

$ cargo clippy --release --all-targets -- -D warnings
   Finished `release` profile [optimized] target(s) in 0.05s   ✅ (零 warning)

$ cargo fmt --check
(无输出)                                                         ✅

$ cargo test --release
running 121 tests (lib unit) → ok
running 5 tests (main unit) → ok
running 2+2+2+4+2+3+2+3+4+2+2+2+3+2+2+11+3 tests (各 integration files) → ok all
```

**总计**: 121 lib + 5 main + 50 integration = **176 tests passed, 0 failed**
(T-0501 是 138, T-0504 +38, 含 24 wl::pointer::tests + 4 csd_e2e + 已合 keyboard_e2e
+ buffer_scale 等)。

`wl::pointer::tests` 24 个全列 (完成派单 In #G "≥6 单测 (titlebar / 3 按钮 /
text area / 边界)" 实际超额到 24):

**hit_test 12 个**:
- hit_test_titlebar_left_returns_titlebar (派单 In #F titlebar 区)
- hit_test_close_button_top_right (派单 In #F Close 按钮)
- hit_test_maximize_button (派单 In #F Maximize 按钮)
- hit_test_minimize_button (派单 In #F Minimize 按钮)
- hit_test_text_area_below_titlebar (派单 In #F text area)
- hit_test_text_area_at_titlebar_boundary (边界 y=28)
- hit_test_titlebar_just_above_boundary (边界 y=27.9)
- hit_test_outside_surface_negative (负坐标防御)
- hit_test_outside_surface_overflow (x>=w / y>=h 防御)
- hit_test_close_right_edge_inclusive (右边界)
- hit_test_button_y_below_button_height_falls_to_titlebar (按钮底 y=24)
- hit_test_extremely_narrow_surface_button_overflow_falls_to_titlebar (50×600)

**apply_* 子 fn 12 个** (绕开 wl_surface 构造难, 抽纯标量子 fn):
- enter_records_position_and_hovers_titlebar
- motion_within_same_region_returns_nothing (避免 redraw 风暴)
- motion_crosses_to_close_button_emits_hover_change (跨区)
- leave_clears_hover
- left_button_press_on_titlebar_starts_move (StartMove)
- left_button_press_on_close_emits_button_click (Close click)
- left_button_press_on_minimize_emits_click
- left_button_press_on_maximize_emits_click
- right_button_press_does_nothing (BTN_RIGHT 0x111 忽略)
- release_does_not_trigger_action (release 不触发, 派单简化)
- left_button_press_on_text_area_does_nothing
- set_surface_size_updates_hover_recomputation (resize 同步 hit_test)

`tests/csd_e2e.rs` 4 个集成测试全列 (派单 In #G):
- csd_renders_titlebar_to_png (PNG 文件存在 + 尺寸正确 + PNG signature)
- csd_titlebar_has_non_clear_pixels (titlebar 中段 80%+ 灰像素)
- csd_buttons_have_icon_pixels (按钮区域 ≥200 icon 浅灰像素)
- csd_writes_deliverable_png_to_fixed_path (/tmp/csd_test.png 给三源 verify)

---

## 2. 三源 PNG verify (T-0408 SOP)

### 写码-T0504 自报 (commit 9b51bfc body / 派单完工 SendMessage)
- /tmp/csd_test.png 47 KiB, 1600×1200 RGBA8, titlebar 灰 #2c2c2c +
  右上角 3 按钮 + bash prompt + 深蓝 clear

### Lead 已 Read /tmp/csd_test.png 自报 (本派单 baseline 段)
"titlebar 深灰 #2c2c2c + 右上角 3 按钮 (− / □ / ×) 清晰 + bash prompt 在 titlebar
下方正常" ✅

### reviewer 第三方 Read /tmp/csd_test.png (本审码独立复跑)

```
$ ls -la /tmp/csd_test.png
.rw-r--r-- user wheel 47 KB Sat Apr 25 22:36:52 2026 /tmp/csd_test.png
```

Read 结果:
- 1600×1200 PNG (HIDPI×2 × 800×600 logical, 与 T-0408 SOP 物理尺寸一致)
- **顶部 titlebar 深灰 #2c2c2c** 清晰可见 (跨整 width)
- **右上角三按钮 icon** 清晰可辨:
  - Minimize: 横线 `−`
  - Maximize: 矩形框 `□`
  - Close: × 阶梯对角 (派单 #D 接受"色块组合"近似)
- **bash prompt** `[user@userPC ~]$` 在 titlebar 之下 (cell y_offset 验证)
- 浅灰 fg #d3d3d3 + cell black bg + 深蓝 clear bg #0a1030 (与 T-0407 D fixup 路径一致)

**三源一致**: writer ✅ Lead ✅ reviewer ✅ — T-0408 三源 PNG verify SOP 任一不一致即 P0,
三源全一致, 通过 ✅

### 第二源 PNG (CLI flag 独立复跑)

```
$ cargo run --release -- --headless-screenshot=/tmp/csd_reviewer.png
... headless screenshot 写出完成, 退出 path=/tmp/csd_reviewer.png ...
```

Read /tmp/csd_reviewer.png:
- 1600×1200 PNG, 视觉与 /tmp/csd_test.png **完全一致**
- titlebar 灰 / 三按钮 icon (− □ ×) / bash prompt / 深蓝 clear 全同
- 双源 sanity 通过 ✅ (说明 render_headless 路径在 csd_e2e fixture 与 main CLI flag
  两条入口产出像素级相同 — CSD 渲染稳定, 非测试 fixture 偶发)

---

## 3. 关键代码审视

### 3.1 src/wl/pointer.rs (新, 590 行)

**模块边界 (INV-010 类型隔离)** ✅:
- 对外 `pub` 仅 5 项: `WindowButton` enum / `HoverRegion` enum / `PointerAction` enum /
  `PointerState` struct / `handle_pointer_event` fn / `hit_test` fn (全 quill 自有)
- `PointerState` 字段**全私有** (`pos` / `hover` / `last_button_serial` /
  `surface_w_logical` / `surface_h_logical`), 下游构造不出来
- `handle_pointer_event` 入参 `wl_pointer::Event` (raw wayland-client 协议类型,
  与 Dispatch<WlPointer> 边界一致, 无可避免 — 与 keyboard 模块 wl_keyboard::Event
  入参同决策)
- 无 `pub use wl_pointer::*` re-export (grep 验过)
- 不实现 `From<wl_pointer::Event>` (grep 验过, 防下游 .into() 偷渡)
- WlSurface 字段在 Enter/Leave 用 `..` 解构丢弃, 不出 pointer.rs 模块边界 ✅

**handle_pointer_event 决策抽 PointerAction enum** (line 226-254):
- 抽 `PointerAction` enum (Nothing / StartMove{serial} / ButtonClick(WindowButton) /
  HoverChange(HoverRegion)) 是 conventions §3 抽状态机模式 (与 KeyboardAction /
  PtyAction / WindowAction 同套路) ✅
- match 6 个 wl_pointer::Event variant (Enter / Leave / Motion / Button /
  Axis* / Frame), 末尾 `_ => Nothing` 兜底 (与 keyboard 模块 wildcard 同决策, 见
  T-0501 P3-1 "wayland 协议层加 variant 概率低", 维持现状)

**子 fn 抽出**: `apply_enter` / `apply_leave` / `apply_motion` / `apply_button`
是 `pub(crate)` 纯标量入参 fn (line 258-313):
- **why**: wl_pointer::Event::Enter 含 `WlSurface` 字段, 单测构造 WlSurface 需
  真 Connection (无可行 mock 路径), 抽纯标量子 fn 让 24 单测覆盖决策矩阵
- 与 conventions §3 套路一致, writer 自标偏离项 #2 (其实是必须精化, 非真偏离)

**hit_test 纯逻辑** (line 332-378):
- 三常数 `TITLEBAR_H_LOGICAL_PX` / `BUTTON_W_LOGICAL_PX` / `BUTTON_H_LOGICAL_PX`
  从 `render.rs` import — **单一来源**, 改一处即视觉与 hit-test 同步 (派单 In #F)
- 极小 surface (50×600) 防御: min_x_min < 0 时 Min 落 titlebar 兜底 (单测覆盖)
- 边界用 `<` / `>=` 不等 (与 NDC / 像素坐标系一致)

**字段 `last_button_serial` `#[allow(dead_code)]`** (line 151):
- forward-compat: Phase 6+ show_window_menu / resize 协议要读最近 button serial
- 注释引用 KeyboardState::repeat_info 同 forward-compat 决策 — 一致, 接受 ✅

### 3.2 src/wl/render.rs (改, +395 行)

**TITLEBAR 常数** (line 290-303):
- `TITLEBAR_H_LOGICAL_PX = 28` ✅ (派单 In #D 硬编码 28, ×HIDPI=56 physical)
- `BUTTON_W_LOGICAL_PX = 24` / `BUTTON_H_LOGICAL_PX = 24` ✅ (派单 #D, GNOME-like)
- `TITLEBAR_BG = #2c2c2c` / `BUTTON_BG_HOVER = #4a4a4a` /
  `BUTTON_BG_CLOSE_HOVER = #e53935` / `BUTTON_ICON = #d3d3d3` (与 cell.fg 默认色同源)

**append_titlebar_vertices** (line 432-540):
- 走 cell pipeline 同 vertex 格式 (`pos[2 f32] + color[3 f32]`), 一次 draw
  同时画 cell + titlebar (无独立 pipeline 的 setup 开销)
- titlebar bg + 3 button bg + Close × (12 stair-stepped 段对角) + Maximize □ (4 边) +
  Minimize − (1 横) ~ 33 quads 总
- HiDPI 适配: 内部 logical × HIDPI_SCALE 算 physical (与 cell px × HIDPI_SCALE 同套路)
- hover 入参驱动按钮 bg 颜色 (Close hover 红, Min/Max hover 深灰)
- icon 走 `color_for_vertex_with_srgb` (sRGB-aware shader 预补偿, 与 cell 同)

**TITLEBAR_RESERVED_QUAD_ROWS = 64** (line 347, 偏离项 #5):
- 派单 #D 未指定容量, writer 实测 ~33 quads + 2× 安全余量 = 64
- ensure_cell_buffer 入参加 64 (实占 ~7680 byte, cells 主体 ~230 KiB, 可忽略)

**draw_frame 入参加 hover** (line 1281, 偏离项 - 派单 In #D 直接要求):
- `hover: super::pointer::HoverRegion` — quill 自有 enum (INV-010), Phase 5 之前
  调用方传 `HoverRegion::None` 等价无高亮

**build_vertex_bytes 入参加 y_offset_px** (line 1175):
- cells 区域起 y 偏移 `TITLEBAR_H_LOGICAL_PX × HIDPI_SCALE` (= 56 physical)
- glyph y_top 同步加 titlebar_y_offset_px (与 cell 视觉对齐)
- `#[allow(clippy::too_many_arguments)]` 注释引派单 + append_quad_px 同决策 ✅
- draw_cells fallback 走 y_offset=0 (Phase 3 兼容, 无 titlebar)

**render_headless 同步加 titlebar** (line 2086-2136):
- 同 Renderer::draw_frame 套路: cells 偏 titlebar_y_offset_px + glyph y 同步偏 +
  append_titlebar_vertices(hover=None) — 三源 PNG verify 走此路径

### 3.3 src/wl/window.rs (改, +216 行)

**State 字段顺序** (line 952-1006, INV-001 守约):
```
renderer  ← INV-001 第 1 (持 wgpu Surface 含 wl 裸指针)
window
conn      ← INV-001 第 3
core
seat_state            (T-0501)
keyboard_state        (T-0501)
keyboard              (T-0501, Option<WlKeyboard>)
pointer_state         (T-0504, PointerState)
pointer               (T-0504, Option<WlPointer>)
pointer_seat          (T-0504, Option<WlSeat>, 用于 xdg_toplevel.move)
is_maximized          (T-0504, bool, Maximize toggle 跟踪)
presentation_dirty    (T-0504, bool, hover 变化触发重画 dirty 标志)
pty                   (Lead T-0202 拍板放最后)
```
- INV-001 链 (renderer → window → conn) **完整保留** ✅
- 新加 5 字段 (pointer_state / pointer / pointer_seat / is_maximized /
  presentation_dirty) 全在 conn 之后 / pty 之前, 不持 wgpu 裸指针, drop 顺序无 UB
- 注释 (line 977-999) 明示 INV-001 链不破 + presentation_dirty 与 INV-006
  resize_dirty 同布尔脏标记套路

**SeatHandler::new_capability(Pointer) 分支** (line 1321-1326, 派单 In #C):
- 与 keyboard 同套路: 仅当 `self.pointer.is_none()` 才 bind (防重复)
- 记 `pointer_seat = Some(seat)` 给 StartMove 路径用
- 注释 (line 1317-1320) 引派单 #C + INV-010

**Dispatch<WlPointer, ()> impl** (line 1458-1529):
- user_data = () (与 keyboard 同, 不需 per-pointer 数据)
- 调用 `handle_pointer_event(event, &mut state.pointer_state)` → match PointerAction
- StartMove → `state.window.move_(seat, serial)` (sctk 已封装 xdg_toplevel.move)
- ButtonClick(Min) → `state.window.set_minimized()`
- ButtonClick(Max) → toggle `is_maximized` + `set_maximized` / `unset_maximized`
- ButtonClick(Close) → `handle_event(&mut state.core, WindowEvent::Close)` 走
  既有 `core.exit = true` → drive_wayland → loop_signal.stop() 路径 (与 xdg
  close request 同出口, INV-007 不破)
- HoverChange → `state.presentation_dirty = true` 触发下一次 idle redraw

**idle callback 检查 presentation_dirty** (line 902):
```rust
if !t.is_dirty() && !state.presentation_dirty { return; }
```
- 任一为真即重画, 重画后 `state.presentation_dirty = false` (line 944)
- 与 term.dirty 解耦避免污染 cell 渲染节奏 ✅

**propagate_resize_if_dirty 同步 PointerState 尺寸** (line 562):
- `state.pointer_state.set_surface_size(width, height)` 让 hit_test 用最新尺寸
  算按钮位置 (按钮在右上角, 拖窗口时按钮跟着移)

**cells_from_surface_px 减 TITLEBAR_H** (line 483-488):
- `usable_h = height.saturating_sub(TITLEBAR_H_LOGICAL_PX)` ✅ (派单 In #E)
- saturating_sub 防 underflow (height < 28 时落 max(1) 兜底)
- 单测覆盖 5 个 (default / grows / clamps / partial / titlebar boundary)

**T-0503 decoration log 更新** (line 1240, 派单 In #H):
- 旧: "no titlebar, quill 不自画 CSD"
- 新: "quill 自画 CSD (titlebar + 最小化/最大化/关闭按钮)" ✅

### 3.4 src/term/mod.rs (改, +12 行)

**新 pub fn `mark_dirty`** (line 644-646, 偏离项追加 #7 见下文):
- 注释说明 forward-compat 用途, 实装路径未使用 (window.rs 走独立的
  `state.presentation_dirty` bool 不走 `t.mark_dirty()`)

### 3.5 src/wl/mod.rs (改, +1 行)

`mod pointer;` 一行 (与 keyboard / render 同), 不 re-export 内部类型 ✅

### 3.6 tests/csd_e2e.rs (新, 240 行)

走 `render_headless` 离屏路径 (T-0408 SOP), 4 个测试覆盖派单 In #G 全部要求 +
固定路径 deliverable PNG (给三源 verify 用) ✅

---

## 4. 偏离审视: 6 + 1 = 7 项

| # | 偏离 | accept/reject | 理由 |
|---|------|---------------|------|
| 1 | `last_button_serial` 字段未消费 (Phase 6+ show_window_menu/resize 预留) | ✅ accept | 与 KeyboardState::repeat_info 同 forward-compat 决策 (T-0501 audit 偏离项 #4 已 accept), 派单未否决保留 forward-compat hook, 注释引用同决策路径 (line 145-150). `#[allow(dead_code)]` 防 clippy 挡, 移除会破坏 Phase 6+ show_window_menu/resize 路径前向兼容. |
| 2 | 拆 apply_enter / apply_leave / apply_motion / apply_button 子 fn (派单未明列) | ✅ accept | wl_pointer::Event::Enter/Leave 含 WlSurface 字段, 单测构造 WlSurface 需真 Connection (无可行 mock). conventions §3 抽决策模式硬要求, T-0107 / T-0205 / T-0302 同套路 — **不抽就无法单测覆盖决策矩阵**. handle_pointer_event 自身仅做协议字段拆解 + 子 fn 转发, 行为薄, 单测对子 fn 等价覆盖. 子 fn 走 `pub(crate)` 不出模块边界. |
| 3 | presentation_dirty 字段加在 State (而非复用 INV-006 resize_dirty) | ✅ accept | 派单 In #C 原文 "redraw 触发 resize_dirty 类似的标志" — **是派单要求的同套路**, 非真偏离. 语义不同 (resize_dirty 触发 renderer/term/pty 三方 resize chain; presentation_dirty 仅触发 idle redraw 重画 hover 高亮), 复用 resize_dirty 会触发 ioctl(TIOCSWINSZ) 浪费. INV-006 守约 (resize_dirty 单一消费者 propagate_resize_if_dirty 仍唯一) ✅. |
| 4 | Close icon 走 12 段阶梯对角矩形模拟 × (cell pipeline 无 rotation transform) | ✅ accept | 派单 #D 原文 "色块组合 line, e.g. ... 关闭 = 叉" — **接受色块组合近似**. cell pipeline 走 NDC pos+color 顶点格式, 无 rotation transform 无法画真斜线, 阶梯近似 (12 段每段 stroke_w × stroke_w) 是该约束下唯一可行路径. 视觉 verify 三源 Read 都读为 "× 关闭" 形态 (writer + Lead + reviewer 三方一致), 视觉契约达成. |
| 5 | TITLEBAR_RESERVED_QUAD_ROWS=64 (派单未指定容量, 实测 ~33 quads + 2× 安全) | ✅ accept | 派单 #D 未指定 cell buffer 容量增量. 实测 quad 数 = 1 bg + 3 button bg + 24 close-X + 4 max-rect + 1 min-line = 33 quads, 64 给 2× 安全系数 + Phase 6+ 加按钮预留. 占用 64 × 6 × 20 = 7680 字节, cells 主体 ~230 KiB 相比可忽略. 注释 (render.rs line 326-345) 详估算清晰. |
| 6 | tests/csd_e2e.rs 4 测试 (派单 #G 未明确数量) | ✅ accept | 派单 #G 仅 "render_headless 写 PNG, 验 titlebar 像素 + 按钮像素" 抽象要求. writer 拆 4 测试 (PNG signature / titlebar gray ratio / button icon ratio / fixed-path deliverable) 是 T-0408 集成测试同套路细化 (T-0408 也是 3 测试), 一测一断言便于 fail 定位. |
| 7 | src/term/mod.rs 加 `pub fn mark_dirty()` (派单 Out 未列 src/term, 但实装未使用) | ✅ accept (有 P3 注释问题) | 派单 Out 仅列 "src/text / src/pty / docs/invariants.md", **未列 src/term** — 派单未禁此处加 pub fn. 加为 forward-compat (与 last_button_serial 同决策). **但** 函数 doc 注释 (line 637-640) 写 "why 不开新 presentation_dirty 标志: term.dirty 唯一作用是触发 idle callback 重画" — 与实装**事实矛盾** (实装走 `state.presentation_dirty` 独立 bool 不走 `t.mark_dirty()`). **本审码记 P3-1 注释清理建议**, 不阻塞合并. |

**6 (writer 自报) + 1 (审码新发) = 7 偏离全 accept** (P3-1 是注释 vs 实装不一致, 非
功能问题).

---

## 5. INV-001..010 + 派单 Out + 不引新 crate 全维持

### INV-001 (State 字段顺序: renderer → window → conn) ✅
新加 5 字段 (pointer_state / pointer / pointer_seat / is_maximized /
presentation_dirty) 全在 `conn` 之后, 不破 wgpu Surface 持 wayland 裸指针的
drop 链. 注释 (window.rs line 977-999) 明示 INV-001 链不破.

### INV-002 (Renderer 字段顺序) ✅
本单零改 Renderer struct 字段 (验证: `git diff c2cfc28..HEAD src/wl/render.rs |
grep 'struct Renderer'` 空). 仅加 titlebar 常数 + append_titlebar_vertices fn +
draw_frame 入参加 hover + build_vertex_bytes 入参加 y_offset_px, struct 字段顺序
未动.

### INV-003 (Renderer::new 调用方合同) ✅
零改 Renderer::new + 调用方 init_renderer_and_draw.

### INV-004 (deny unsafe_code 局部 allow) ✅
本单**零新增 unsafe** (验证: pointer.rs 零 unsafe, render.rs 仅 1 处 Renderer::new
既有). conventions §2 SAFETY 注释要求不触发 (无新 unsafe).

### INV-005 (calloop 单线程禁阻塞) ✅
- handle_pointer_event 纯逻辑, 不调任何 wl request / IO / sleep
- Dispatch<WlPointer> impl 内调 `state.window.move_(seat, serial)` /
  `set_minimized()` / `set_maximized()` 等都是 sctk 已封装的 wayland-client
  request (内部 `event_queue.send_request`, 非阻塞)
- presentation_dirty bool 切换无 syscall
- idle callback 检查 dirty + redraw 路径既有 (T-0305 已守 INV-005)

### INV-006 (resize_dirty 消费合同) ✅
- resize_dirty 单一消费者 propagate_resize_if_dirty 仍唯一 (验证: grep 全文)
- presentation_dirty 是**独立**布尔脏标记 (idle callback 消费), 不与 resize_dirty
  共生消费链
- propagate_resize_if_dirty 末尾新加 `state.pointer_state.set_surface_size(width,
  height)` 同步 PointerState 尺寸, **不**碰 resize_dirty 自身 (set_dirty=false 仍
  在 line 564 唯一处) ✅

### INV-007 (WindowCore handle_event 纯逻辑) ✅
零改 handle_event 函数体. Dispatch<WlPointer> 是新 trait impl, 走 SeatHandler /
Dispatch 协议路径, 不路过 WindowCore (除 Close 按钮调
`handle_event(&mut state.core, WindowEvent::Close)` 走既有 Close 事件路径, 与
xdg close request 同出口, 不破纯逻辑契约).

### INV-008 (PtyHandle 字段顺序: reader → master → child) ✅
零改字段顺序 (验证: `git diff c2cfc28..HEAD src/pty/mod.rs` 仅 rustfmt 4 行变 5
行调用参数换行, 字段定义未动).

### INV-009 (PTY master fd O_NONBLOCK) ✅
零改 spawn_program / set_nonblocking 路径. 本单不碰 PTY (除 rustfmt). T-0501
PtyHandle::write 已守.

### INV-010 (上游 crate 类型不出公共 API 边界) ✅

**5 条独立 grep 复跑**:

```bash
$ grep -rnE 'pub use (alacritty|wgpu|wayland|cosmic|smithay|sctk)' src/
(零命中) ✅

$ grep -rnE 'impl From<.*(wl_pointer|wayland|smithay|sctk)' src/
(零命中) ✅

$ grep -rnE 'pub.*wl_pointer' src/
src/wl/pointer.rs:18://!   类型路径, 与 keyboard 模块同决策).
src/wl/pointer.rs:20://!   [`PointerAction`] (quill 自有). 没有 `pub use wl_pointer::*` re-export.
src/wl/pointer.rs:106:/// 调用方拿到的全是 quill 自有类型, 不暴露 wl_pointer::Event 字段.
src/wl/pointer.rs:226:pub fn handle_pointer_event(event: wl_pointer::Event, state: &mut PointerState) -> PointerAction {
   ↑ 4 命中: 3 doc comment 反例陈述 + 1 必须接 wl_pointer::Event 入参 (与
   handle_key_event 同决策, 协议层不可避免, 注释 line 18-20 明示边界)

$ grep -rnE 'pub use .*pointer' src/
(零命中) ✅
```

**T-0504 新加类型边界 verify**:
- `pub struct PointerState { pos / hover / last_button_serial / surface_w_logical /
  surface_h_logical }` — 字段**全私有** ✅
- `pub fn handle_pointer_event(event: wl_pointer::Event, state: &mut PointerState) -> PointerAction`
  - 入参 `wl_pointer::Event`: wayland-client 协议类型, 与 Dispatch<WlPointer> impl
    边界一致 (与 handle_key_event 入参 wl_keyboard::Event 同, T-0501 audit 已 accept)
  - 入参 `&mut PointerState`: quill 自有 ✅
  - 返 `PointerAction`: quill 自有 enum (Nothing / StartMove / ButtonClick / HoverChange) ✅
- `pub enum PointerAction` / `pub enum HoverRegion` / `pub enum WindowButton` —
  全 quill 自有, 无上游漏出 ✅
- `pub fn hit_test(x: f64, y: f64, w: u32, h: u32) -> HoverRegion` — 全 std 类型 + quill 自有 ✅
- `apply_enter` / `apply_leave` / `apply_motion` / `apply_button` 是 `pub(crate)`
  纯标量子 fn, 仅本 crate 测试可见, 不出公共 API 边界 ✅
- WlSurface 字段在 wl_pointer::Event::Enter/Leave variant 用 `..` 解构丢弃,
  不出 pointer.rs 模块边界 ✅

**INV-010 第 13 次应用** ✅ (T-0302..T-0307 + T-0399 + T-0401..T-0403 + T-0407 +
T-0408 + T-0501 + **T-0504 PointerState/PointerAction/HoverRegion/WindowButton/
handle_pointer_event/hit_test**)

### 派单 Out 全守 ✅

```bash
$ git diff c2cfc28..HEAD --stat -- src/text docs/invariants.md Cargo.toml Cargo.lock
(空, 全零 diff)

$ git diff c2cfc28..HEAD --stat -- src/pty
src/pty/mod.rs | 6 +/-  (rustfmt only, libc::write 调用参数换行格式调整,
                         &参数顺序未变, 行为零变化)
```

- 不做 titlebar 自定义颜色 / 字体 (硬编码 GNOME-like) ✅
- 不做双击 titlebar 切最大化 ✅ (Phase 6+)
- 不做 titlebar 显示窗口标题 ✅ (Phase 6+)
- 不做边框拖动 resize ✅ (Phase 6+)
- 不做鼠标滚轮选区滚动 ✅ (Phase 6+, Axis 事件走 Nothing wildcard)
- 不做鼠标光标样式切换 ✅ (Phase 6+, set_cursor 未调)
- 不动 src/text / docs/invariants.md ✅ (实测零 diff)
- 不动 src/pty 行为 ✅ (rustfmt only)
- src/term 加 forward-compat pub fn mark_dirty (派单 Out 未列 src/term, 不违规) ⚠️ 有 P3-1 注释建议

### 不引新 crate ✅

Cargo.toml / Cargo.lock 零 diff. sctk 0.19 / wayland-client 已封装:
- `wl_pointer` 协议 (wayland-client 协议层)
- `xdg_toplevel.move` (sctk Window::move_)
- `xdg_toplevel.set_minimized` (sctk Window::set_minimized)
- `xdg_toplevel.set_maximized` / `unset_maximized` (sctk Window::set/unset_maximized)
- SeatHandler / Capability::Pointer (sctk 0.19 已暴露)

ADR 0007 不需 ✅.

---

## 6. P 列表

### P0: 0 项 ✅
全 P0 通过, 无阻塞.

### P1: 0 项 ✅

### P2: 0 项 ✅

### P3: 1 项 (建议级, 非阻塞)

#### [P3-1] `TermState::mark_dirty` 注释与实装事实不一致 (建议)

**事实链**:
- src/term/mod.rs line 637-640 注释:
  > **why 不开新 "presentation_dirty" 标志**: term.dirty 唯一作用是触发
  > idle callback 重画, hover 变化也走重画路径, 语义等价 — 复用既有标志
  > 不破 INV-006 / INV-007
- 实装 (src/wl/window.rs):
  - line 999: `presentation_dirty: bool` 字段加在 State (**已开新标志**)
  - line 902: `if !t.is_dirty() && !state.presentation_dirty { return; }` (走独立标志)
  - line 1475: `state.presentation_dirty = true;` (HoverChange 时置位)
  - **未** 出现任何 `t.mark_dirty()` 调用 (grep 全 src 仅 mark_dirty 定义点 1 处命中)

**结论**: 注释陈述的"复用 term.dirty 不开新标志"与实装事实相反. mark_dirty 函数
本身存在 (forward-compat hook 与 last_button_serial 同决策), 但注释解释错了
"why" — 实装走的是 presentation_dirty 独立标志方向.

**为啥仅 P3 (建议级而非 P2)**:
- 函数本身正确 (无副作用, 单纯 `self.dirty = true`)
- 加为 forward-compat 路径有效 (Phase 6+ 接 IME preedit 高亮等"cell 内容未变
  但需重画"场景可直接调)
- 仅是 doc comment 误导, 不影响编译 / 测试 / 行为
- 修复路径短: 改 doc comment "why 不开新 presentation_dirty 标志" → "与
  presentation_dirty 同 forward-compat 套路, Phase 6+ 备用"

**建议** (Lead 自选, 非合并阻塞):
方案 A (维持现状): 合并后 Phase 6 ticket 接入 mark_dirty 实际使用时顺手清理注释
方案 B (本单 fixup): writer 跑一个 fixup commit 改 doc comment 一句话, audit
  签字保留, Lead 直接合
方案 C (本单 squash 时 Lead 顺手改): Option C squash 路径下 Lead 在合并 commit
  里顺手改 src/term/mod.rs 4 行注释

**裁决**: **方案 A 推荐** — 注释问题极轻, 不值得本单返工; Phase 6+ 接入
mark_dirty 时一并清理. Lead 自决.

---

## 7. 结论

✅ **批准合并 feat/T-0504 → main**

- 4 门 release 全绿 (176 tests passed, 0 failed; 派单 Acceptance "≥159" 实测 176)
- INV-001..010 全守 (重点 INV-005 / 006 / 007 / 010 实测验证)
- 派单 In 全实装 (8 必须 #A-#H, 6/6 acceptance 检查项)
- 派单 Out 全守 (src/text / docs/invariants.md / Cargo.toml/lock 零 diff;
  src/pty 仅 rustfmt 行为零变化; src/term 加 forward-compat pub fn 派单未禁)
- 6 (writer 自报) + 1 (审码新发) = 7 偏离全 accept (5 个是项目硬规范 / 派单要求
  精化 / 派单允许; 2 个是 forward-compat hook 与既有 KeyboardState::repeat_info
  同决策)
- 不引新 crate (sctk 0.19 / wayland-client 已封装, ADR 0007 不需) ✅
- 三源 PNG verify 全过 (writer + Lead + reviewer 三方独立 Read /tmp/csd_test.png +
  reviewer 独立 CLI flag PNG /tmp/csd_reviewer.png 双源 sanity)
- T-0503 decoration log 已更新 (派单 In #H)

**P0/P1/P2 全 0**, 仅 1 个 P3 建议 (mark_dirty 注释 vs 实装事实不一致, 维持现状,
Phase 6+ 接入时清理).

**Lead 合并路径** (推 Option C squash):
```bash
cd /home/user/quill-impl-csd
git reset --soft main  # 注: main 当前在 c2cfc28 派单 commit, 需先 fork-base 校验
git reset HEAD -- tasks/T-0504-csd-titlebar-pointer.md
git commit -m "<HEREDOC: T-0504: CSD 自画 titlebar + 3 按钮 + wl_pointer 接入>"
git add tasks/T-0504-csd-titlebar-pointer.md
git commit -m "T-0504: Status claimed -> in-review"

cd /home/user/quill
git merge --no-ff feat/T-0504 -m "Merge T-0504: CSD 自画 titlebar + 3 按钮 + wl_pointer 接入 (Phase 5 GNOME daily-drive)"

git add tasks/T-0504-csd-titlebar-pointer.md docs/audit/2026-04-25-T-0504-review.md
git commit -m "T-0504: in-review -> merged + audit 报告存档"

git worktree remove ../quill-impl-csd
git branch -d feat/T-0504
```

**手测 deliverable** (派单 acceptance 最后一条, 由 Lead 主导):
合并后 `cargo run --release` 在 GNOME 下启动 quill, 实测:
- 窗口顶部出现深灰 titlebar
- 右上角三按钮 (− □ ×) 可见
- 鼠标 hover 三按钮变深灰 (Close 变红)
- 拖 titlebar 可移动窗口
- 点 Min 按钮 → 窗口最小化
- 点 Max 按钮 → 窗口最大化 / 还原 toggle
- 点 Close 按钮 → quill 真退出
→ Phase 5 GNOME daily-drive 全套解锁

---

**审码人签**: reviewer-T0504
**审视范围**: feat/T-0504 全 3 commit 完整审计
**预算消耗**: token < 80k, wallclock < 1h (per-ticket fresh agent)
