# 审码报告: T-0505 fcitx5 text-input-v3 (中文输入法, Phase 5 收尾)

**审码人**: reviewer-T0505 (per-ticket fresh agent)
**日期**: 2026-04-25
**范围**: feat/T-0505 分支, 3 commits (fork 自 `47fbd10`)
- `e74c874` T-0505: claim by writer-T0505
- `af53dce` T-0505: fcitx5 text-input-v3 (中文输入法, Phase 5 收尾)
- `e1e6927` T-0505: Status claimed -> in-review

**diff stat** (independently verified `git diff 47fbd10..HEAD --stat`):

```
 Cargo.lock                                       |   1 +
 Cargo.toml                                       |   5 +
 docs/adr/0007-wayland-protocols-text-input-v3.md | 231 +++++++ (新)
 src/ime/mod.rs                                   | 731 +++++++++++++++++++++++ (新)
 src/lib.rs                                       |   1 +
 src/main.rs                                      |   1 +
 src/pty/mod.rs                                   |   6 +-
 src/wl/mod.rs                                    |   5 +
 src/wl/render.rs                                 | 358 ++++++++++-
 src/wl/window.rs                                 | 334 ++++++++++-
 tasks/T-0505-fcitx5-text-input-v3.md             |   4 +-
 tests/atlas_clear_on_full.rs                     |   3 +
 tests/cjk_fallback_e2e.rs                        |   1 +
 tests/headless_screenshot.rs                     |   1 +
 tests/ime_e2e.rs                                 | 187 ++++++ (新)
 tests/ime_preedit_render.rs                      | 275 +++++++++ (新)
 16 files changed, 2136 insertions(+), 8 deletions(-)
```

writer 自报 16 文件 +2136/-8 与实测**完全一致**。

**fork-point gap 说明**: feat/T-0505 fork 自 `47fbd10` (T-0505 派单 commit, 早于
T-0504 合并)。T-0504 CSD 已合 main (`32f0837`), Lead 合并时手解 src/wl/window.rs
+ render.rs 段冲突 — 与派单 "T-0504 / T-0505 不同段, 真冲突 Lead 手解" 一致, 本
audit 不验合并冲突, 仅审 feat/T-0505 vs fork-point 47fbd10 的 diff 完整性。

---

## 判定: ✅ **批准合并**

**全 P0/P1/P2 通过 + 5 偏离全 accept (理由充分, 派单 In scope 内的精化或硬规范要求)。**

T-0505 是 Phase 5 收尾 P1 — 接 `zwp_text_input_v3` (TIv3) 协议, 让 fcitx5-rime
在 quill 窗口里能输中文。链路 `compositor → ZwpTextInputV3 event →
handle_text_input_event → ImeAction (Composite/Commit/UpdatePreedit/...) →
apply_ime_action → pty.write / draw preedit / set_cursor_rectangle` 全打通。

13 个 lib 单测 + 6 个集成测试 (ime_e2e 3 + ime_preedit_render 3) 覆盖 atomic
done 状态机 + 多帧序列 + cursor_rectangle 防抖 + PTY commit 字节真到 cat echo
+ PNG 视觉验。4 门 release 全绿 (171 tests pass), INV-001..010 全守, 派单 Out
(src/text 零 diff) 全守, 新引 1 crate (wayland-protocols) 由 ADR 0007 sanction。

三源 PNG verify 全一致 (writer + Lead + reviewer Read /tmp/ime_test.png 视觉
描述对齐, 含 writer 主动告知 "下划线只覆盖 2 cell 而 CJK 占 4 cell" 的 KISS
取舍 — Phase 6+ east-asian width 表)。

---

## 1. 4 门 release 独立复跑

```bash
$ cd /home/user/quill-impl-ime
$ cargo build --release
   Finished `release` profile [optimized] target(s) in 35.14s   ✅

$ cargo clippy --release --all-targets -- -D warnings
   Finished `release` profile [optimized] target(s) in 0.75s    ✅ (零 warning)

$ cargo fmt --check
(无输出)                                                          ✅ rc=0

$ cargo test --release
test result: ok. 113 passed (lib, 含 13 ime::tests)               ✅
test result: ok. 5 passed (frame_stats lib)                       ✅
test result: ok. 3 passed (ime_e2e)                               ✅
test result: ok. 3 passed (ime_preedit_render)                    ✅
test result: ok. 4 passed (keyboard_event_to_pty)                 ✅
test result: ok. 11 passed (state_machine)                        ✅
test result: ok. 3 passed (xdg_decoration)                        ✅
... (全部 19 集成测试 binary 全绿)
```

**总计**: 113 lib + 5 frame_stats + 53 集成 = **171 tests passed, 0 failed**
(派单 acceptance 写 "≥159", 实测 171, 超额 +12)。

`src/ime/tests` 13 个全列:
- `preedit_then_commit_then_done_yields_commit_action` (拼音 ni → 你 典型路径)
- `commit_only_done_yields_single_commit_action` (单 variant 不包 Composite)
- `preedit_only_done_yields_single_update_preedit` (单 variant)
- `empty_done_yields_nothing` (空 done 边界)
- `composite_action_orders_delete_commit_preedit` (协议 apply 顺序)
- `preedit_string_with_none_text_treated_as_empty` (allow-null=true)
- `commit_string_with_none_text_yields_nothing`
- `commit_string_with_empty_text_yields_nothing`
- `leave_clears_preedit_and_returns_leave_focus` (留作正向验, leave 路径正文已
   通过 enter/leave 路径覆盖, 测试体内有自释说明 — 见下面 P3-1)
- `default_state_is_disabled_with_no_preedit`
- `cursor_rectangle_first_call_returns_some` / `..._unchanged_returns_none` /
  `..._changed_returns_some_new` (3 个 防抖 unit test)
- `multi_frame_sequence_two_commits` (ni→你→hao→好 状态机不漏字)
- `pending_cleared_after_done` (mem::take 清 pending)
- `commit_utf8_multibyte_bytes_preserved` (UTF-8 字节直传)

`tests/ime_e2e.rs` 3 个 (cat echo loopback):
- `fcitx5_commit_bytes_reach_pty_via_cat_echo` ("你" 三字节真到 cat 回显)
- `multi_commit_sequence_ni_hao` ("你好" 六字节真到 cat 回显)
- `preedit_only_frame_writes_no_bytes` (preedit-only 帧不写 PTY 回归保护)

`tests/ime_preedit_render.rs` 3 个 (PNG 视觉验):
- `preedit_overlay_renders_underline_to_png` (主路径 — 写 /tmp/ime_test.png +
   underline 区 ≥50 亮像素 + glyph 区 ≥30 亮像素)
- `no_preedit_means_no_underline` (None 路径回归保护)
- `empty_preedit_text_means_no_underline` (空 text 路径)

---

## 2. 三源 PNG verify (T-0408 SOP)

| 源 | 描述 |
|---|---|
| **writer** (test runtime) | 1600×1200 RGBA8 PNG `/tmp/ime_test.png`. underline 区 (cursor col=5, line=10, 底部 4 phys px) 有 ≥50 亮像素; glyph 区 (cursor cell 中部) 有 ≥30 亮像素. 测试用 `distance_from_clear > 100` 与 #0a1030 深蓝对比, 阈值通过。 |
| **Lead** (主 agent) | 已 Read PNG 第 2 源验过: 深蓝背景 + "你好" 浅灰 CJK + 下划线; writer 自报偏离 "下划线只覆盖 2 cell 而 CJK 占 4 cell, KISS 取舍 Phase 6+ east-asian width 表"。 |
| **reviewer** (本 audit) | 第 3 源 Read 复验: PNG 1600×1200, 深蓝 (#0a1030) 满铺, 左中位置 (约 phys x≈100-160, y≈510-545) 浅灰色 CJK 双字 "你好" 清晰可读, 下划线浅灰条仅出现在 "你" 一字下方 (大约 phys x=100..120, y=546..550, 即 logical 50..60×273..275, 正是 char_count=2 但每 char 只占 1 cell 宽度 = 20 phys px 的预期), CJK 字形实际跨 2 cell ≈ 40 phys px, 下划线只覆盖前一半。视觉与 writer + Lead 描述完全一致 ✅ |

**三源一致** ✅ (writer 主动告知偏离 #4 / KISS 取舍, Lead + reviewer 视觉确认与
writer 描述吻合, 不属 P0 三源不一致)。

---

## 3. 关键代码审视

### 3.1 src/ime/mod.rs (新, 731 行 含 13 单测)

**模块 doc-comment** (line 1-58):
- 8 段标准结构: 职责 / 模块边界 (INV-010) / TIv3 atomic 协议状态机 ascii 图 /
  与 wl_keyboard (T-0501) 的协调; 引 ADR 0007 + INV-010 + T-0501 ✅
- atomic 协议状态机 ascii 图清晰展示 enter → preedit/commit/delete → done →
  apply 流程 ✅

**`PendingFrame` struct** (line 74-88): 一帧暂存 `Option<PreeditPending>` /
`Option<Vec<u8>>` / `Option<(u32, u32)>`. `mem::take` 在 apply_pending 后清
pending. 字段全私有 ✅

**`PreeditPending` struct** (line 96-101): 暂存 preedit text + cursor offset (
i32, 协议规定 byte offset)。POD-like 字段, 派 `Default` + `Clone` + `PartialEq +
Eq`, 测试体便利 ✅

**`ImeState` struct** (line 118-140):
- 字段全私有 (INV-010 类型隔离, 下游构造不出来) ✅
- 字段顺序: pending → current_preedit → current_preedit_cursor →
  last_cursor_rect → enabled. 全 owned POD-like 类型 (无 wgpu/wayland 裸指针),
  drop 顺序无 UB 风险 ✅
- `Default` 派生 + `pub fn new() -> Self { Self::default() }` ✅
- 5 个 pub accessor (current_preedit / current_preedit_cursor / is_enabled /
  update_cursor_rectangle / new) — 全返 quill 自有类型 (str / (i32,i32) / bool /
  Option<CursorRectangle>) 无 wayland-protocols 漏出 ✅

**`CursorRectangle` struct** (line 149-154): 4 个 i32 (x/y/w/h logical px),
`#[derive(Debug, Clone, Copy, PartialEq, Eq)]`. 派单 In #E 必需 ✅

**`update_cursor_rectangle` 防抖** (line 189-195):
- 相同值返 None (减 protocol round-trip), 变化返 Some(new) — fcitx5 候选框定位
  减抖, 设计意图与 ADR 0007 "实测中 ime_state 缓存避免重复上报" 一致 ✅
- 3 个 unit test 锁住 first/unchanged/changed 三路径

**`ImeAction` enum** (line 209-242):
- 8 variant: Nothing / UpdatePreedit / Commit / DeleteSurroundingText /
  EnterFocus / LeaveFocus / Composite (派单 5 个 + 偏离 #3 加 Composite)
- `#[derive(Default)]` + `#[default] Nothing` 测试便利 ✅
- doc 注释引 conventions §3 抽状态机模式 + T-0107 / T-0205 / T-0501 同源

**`handle_text_input_event` 决策** (line 268-332):
- 入参 `event: zwp_text_input_v3::Event` (wayland-protocols 协议类型, INV-010
  doc 显式说明 "已在公共 API 边界, Dispatch 接同样类型不可避免") ✅
- match 6 个 Event variant + `_ => Nothing` 兜底 (T-0501 同套路, audit P3-1
  接受) ✅
- Enter/Leave 清 current_preedit + pending + 翻 enabled 标志 ✅
- PreeditString allow-null=true 处理 (text=None 等价 ""), CommitString
  text=None / text="" 等价无 commit (跳过暂存) — 协议正确实装 ✅
- Done 调 `apply_pending(state)` 算最终 ImeAction ✅

**`apply_pending` 协议顺序** (line 347-387):
- 协议规定 apply 顺序: delete → commit → preedit (ADR 0007 描述):
  - actions.push(Delete) 第一
  - actions.push(Commit) 第二
  - actions.push(UpdatePreedit) 第三
- 单一 / 双 / 三件套场景: 0 → Nothing, 1 → 单 variant, ≥2 → Composite (Vec)
- preedit None 时 fcitx5 实测每帧重发 preedit_string event (空 text 也发),
  防御性 doc 说明 "None = 维持 current 不动, foot/alacritty 同套路" ✅

### 3.2 src/wl/window.rs (改, +334 行)

**State 字段顺序** (line 1015-1037):
```
renderer  ←  INV-001 第 1
window
conn      ←  INV-001 第 3 (wgpu Surface 持 wayland 裸指针, 必须晚 drop)
core
seat_state            (T-0501 加, SCTK helper)
keyboard_state        (T-0501 加, KeyboardState 含 xkbcommon)
keyboard              (T-0501 加, Option<WlKeyboard>)
text_input_manager    (T-0505 加, Option<ZwpTextInputManagerV3>)
text_input            (T-0505 加, Option<ZwpTextInputV3>)
ime_state             (T-0505 加, ImeState)
pty       ←  Lead T-0202 拍板放最后
```
- INV-001 链 (renderer → window → conn) **完整保留** ✅
- 新加 3 字段 (text_input_manager / text_input / ime_state) 位于 keyboard 之后 /
  pty 之前, 不持 wgpu 裸指针, drop 顺序无 UB 风险 ✅
- 注释 (line 1015-1029) 明示 INV-001 链不破 + 性质归类 (与 keyboard 三件套
  POD-like) ✅

**registry bind manager** (line 705-713):
- `globals.bind(&qh, 1..=1, ())` 仅请求 v1 (协议唯一版本) ✅
- bind 失败 → `None`, IME 路径退化 (compositor 不导出全局或 fcitx5/ibus 未启) —
  快乐路径不阻塞 startup ✅
- 注释引 ADR 0007 ✅

**SeatHandler::new_capability(Keyboard)** (line 1346-1357):
- text_input 跟 keyboard 同生命周期 (协议: TIv3 focus 与 wl_keyboard 同步)
- 仅当 manager 已 bind 才 get text_input (compositor 无 IME 支持 → text_input
  None, 无副作用) ✅
- 防重复 bind: `if self.text_input.is_none()` ✅

**SeatHandler::remove_capability(Keyboard) + remove_seat** (line 1374-1395):
- text_input.take() 触发 wayland-protocols 自动 destroy (Drop impl 内调
  destructor request), 与 wl_keyboard.release() 同套路 ✅

**`cursor_rectangle_for_cell` 纯 fn** (line 1399-1418):
- 抽 fn (conventions §3 模式) — `pub(crate)` 让 unit test / window.rs 同包内
  使用, 不外露公共 API ✅
- 用 logical px (协议要求, ADR 0007 明示, 非 physical / × HIDPI) ✅
- doc 注释引派单 In #E + ADR 0007 + Phase 4 字体 metrics 接入注脚 ✅

**`Dispatch<ZwpTextInputManagerV3>` impl** (line 1424-1435):
- manager 协议 0 event, Dispatch::event 不应被调用; 防御性空 impl ✅
- doc 注释明示 (协议 0 event) ✅

**`Dispatch<ZwpTextInputV3>` impl** (line 1466-1480) — T-0505 主路径:
- `event` arm: handle_text_input_event → apply_ime_action → 副作用分派 ✅
- doc 注释 (line 1437-1465) 4 段: 类型隔离 (INV-010) / PTY write 路径 (INV-009)
  / enable+commit 协议要求 / preedit dirty (复用 idle 不加新 dirty 标志,
  KISS) ✅

**`apply_ime_action` 副作用分派** (line 1486-1602):
- ImeAction::Commit → pty.write (4 路径全覆盖: Ok(n)=len, Ok(partial),
  WouldBlock, 其它 Err) — 与 T-0501 keyboard 路径同套路 ✅
  - INV-005 不重试 WouldBlock 直接丢 (派单 In #D 允许) ✅
  - INV-009 master fd O_NONBLOCK 既有保证 ✅
- ImeAction::UpdatePreedit → 仅 trace, 渲染层下次 idle 读 ime_state.current_preedit() ✅
  - doc 注释 (line 1538-1547) 显式说明 "why 不主动 trigger redraw" — KISS, idle
    频率与 wayland event 同节奏, 协议 done 后下一次 idle 必走
- ImeAction::DeleteSurroundingText → 仅 trace (派单 Out, Phase 6 决定 BS×N seq) ✅
- ImeAction::EnterFocus → text_input.enable() + set_content_type(None,
  Terminal) + set_cursor_rectangle (初值 0,0) + commit() — 协议要求一次 atomic
  推过去 ✅
- ImeAction::LeaveFocus → text_input.disable() + commit() (释放 fcitx5 grab) ✅
- ImeAction::Composite → 递归 apply_ime_action (协议规定应用顺序) ✅
  - 防无限递归: handle_text_input_event 内 apply_pending 仅生成单一 variants
    列表, 不会嵌套 Composite

**idle callback preedit 渲染整合** (line 921-948):
- 读 `state.ime_state.current_preedit()` 决定 PreeditOverlay (None 时不画) ✅
- 走 `r.draw_frame(.., Some(&overlay))` 通路 ✅

**idle callback cursor_rectangle 上报** (line 961-983):
- 仅当 `ime_state.is_enabled()` 才上报 (减 wasted protocol traffic) ✅
- 用 ime_state.update_cursor_rectangle 防抖 (相同值跳过) ✅
- 调 `text_input.set_cursor_rectangle(x, y, w, h) + commit()` 协议要求 atomic ✅

### 3.3 src/wl/render.rs (改, +358 行)

**`PREEDIT_UNDERLINE_PX = 2` 常数** (line 288):
- logical px, render 内部 × HIDPI_SCALE → 4 phys px ✅
- 派单 In #D "1-2 px 下划线 (主流 IME 风格)" ✅

**`PreeditOverlay` struct** (line 302-312):
- pub struct, 字段 text: String + cursor_col: usize + cursor_line: usize ✅
- INV-010 全 std 类型, 不漏 wayland-protocols ✅
- doc 注释解释 "why pub struct 而非多参数" — 派单 In #D 描述 ✅

**`draw_frame` 签名** (line 969-976):
- 加 `preedit: Option<&PreeditOverlay>` 末参 ✅
- `#[allow(clippy::too_many_arguments)]` (`render_headless` 同) ✅
- API breaking change (派单 In #D 要求, callsite 同步加入参 None / Some) — 偏
  离项 #2, 见下文

**preedit overlay 整合** (line 1094-1132 in draw_frame, line 2103+ in
render_headless):
- 通过 `append_preedit_glyphs` (Renderer method, 复用 atlas + glyph slot) 处理
  preedit 字 ✅
- 通过 `append_preedit_underline_to_cell_bytes` (free fn) 追加下划线矩形到
  cell pass ✅
- preedit 字色用 cell.fg 浅灰 (#d3d3d3, 与正文同), 下划线用 #ffffff 白色
  (亮于 fg, 让用户区分组词态) ✅
- char_count 用 `chars().count()` 算 underline 宽度 (KISS, 偏离 #4 — east-asian
  width 表 Phase 6+ 接) ✅

**`append_preedit_glyphs` Renderer method** (line 1572-1641):
- shape preedit 行 → allocate_glyph_slot (atlas 复用) → 6 个顶点 (位置 + UV +
  fg color) append 到 glyph_vertex_bytes ✅
- 起绘点 base_x_px = cursor_col × cell_w_px (与 cells 同源 cell px 网格) ✅
- `#[allow(clippy::too_many_arguments)]` (12 args, 拆 struct ROI 小) ✅

**`append_preedit_underline_to_cell_bytes` free fn** (line 1660-1701):
- 不依赖 self, 纯 vertex generation 数学 ✅
- `cell_pass REPLACE` 路径 (颜色 #ffffff 满覆盖, 不 alpha-blend) ✅
- y 区域: cell 底部往上 PREEDIT_UNDERLINE_PX × HIDPI_SCALE ✅
- char_count=0 早返 ✅

**`render_headless` 签名 + preedit 内联** (line 1772-2103):
- 8 args (+1 preedit), `#[allow(clippy::too_many_arguments)]` 标 ✅
- 内联 glyph 处理 (T-0408 设计选择: 不复用 Renderer method, 防 GPU 资源耦合) ✅
- 与 draw_frame 完全等价语义 ✅

### 3.4 ADR 0007 (新, 231 行)

5 段标准结构 (Status / Context / Decision / Alternatives / Consequences) 完整 ✅

- **Status**: Accepted, 2026-04-25
- **Context**: TIv3 协议特点 (atomic done / commit / preedit / cursor_rect /
  enter/leave) + sctk 0.19.2 不封装 — 详尽 ✅
- **Decision**: `wayland-protocols = "0.32"` features `["client", "unstable"]`,
  zwp_text_input_v3 + zwp_text_input_manager_v3, 类型隔离 INV-010 显式说明 ✅
- **Alternatives 4 路全 reject 理由清楚**:
  - Alt 1 (等 sctk 上游) — 时间未知 + daily-drive 不能等 + foot/alacritty 同款
    直接走 wayland-protocols
  - Alt 2 (手写 wayland-scanner) — wayland-protocols 内部就是这条路径, 重做无
    新增价值 + libwayland-bin CI 配置成本
  - Alt 3 (fcitx5 D-Bus 旁路) — Wayland 安全模型不允许 + 不可移植 (ibus 失效)
    + D-Bus 在 calloop 整合复杂违反 INV-005
  - Alt 4 (X11 XIM 旁路) — 反向路径 + XIM on-the-spot 在 mutter 上失效
- **Consequences 正负残留三段齐**:
  - 正面: fcitx5 / ibus / wlroots 全栈 + 代码量小 (~500 行) + Phase 6 扩展位 +
    INV-010 守得住
  - 负面: Cargo.lock +1 transitive (wayland-protocols 自身) + unstable feature
    flag + atomic done 协议复杂
  - 已知残留: 真键盘 + IME 协调 / fractional scale / 多 surface IME (Phase 6+)
- ADR 0002 主干清单不进 (与 ADR 0005 image / ADR 0006 xkbcommon 同套路) ✅
- 实装验证 / 相关文档段齐 (派单 + ADR 0006 + INV-005 / 010) ✅

---

## 4. 5 偏离 accept/reject

| # | 偏离 | accept/reject | 理由 |
|---|------|---------------|------|
| 1 | src/pty/mod.rs 1 处 fmt 重格式 (libc::write 单行 → 多行) | ✅ accept | writer 自报 "派单 Out 列, baseline cargo fmt --check 已 fail 自 T-0410 hotfix v2 留下, 不修门红; 仅 rustfmt 多行重排零语义" — **writer 描述不准确**, 实测 baseline 47fbd10 cargo fmt --check 已 rc=0 (无门红遗留), HEAD 也 rc=0。两种格式 (单行 / 三参 break) rustfmt 均接受 (width-driven), writer 改成多行只是无害自描述更清晰。**零语义变化, 不影响门绿, 接受**。但建议 writer 后续 ticket 更准确诊断 fmt 状态前再描述偏离。 |
| 2 | Renderer::draw_frame / render_headless 签名加 `preedit: Option<&PreeditOverlay>` 末参 + `#[allow(clippy::too_many_arguments)]` (公共 API breaking change) | ✅ accept | 派单 In #D **明示** "preedit 渲染在 cursor 当前 cell 起点之后" 是 acceptance 必须项, 加入参是唯一让 PreeditOverlay 数据流到 render 的方法。callsite 同步更新: src/main.rs 加 None / src/wl/window.rs idle callback 加 Some(&overlay) / 3 个集成测试 (atlas_clear_on_full / cjk_fallback_e2e / headless_screenshot) 加 None — 全 4 处 callsite 已正确同步。`#[allow(clippy::too_many_arguments)]` 抽 builder struct ROI 小, 与 T-0407/T-0408 设计一致 (派单 In #D 接受). 偏离描述准确, 派单 In 内的精化, **非真偏离**。 |
| 3 | ImeAction::Composite variant 加在派单 5 个 variant 之外 (派单原列 Nothing / UpdatePreedit / Commit / DeleteSurroundingText / EnterFocus / LeaveFocus) | ✅ accept | TIv3 atomic 协议规定一帧 done 内可同时含 delete + commit + preedit 三类副作用, 必须按 "delete → commit → preedit" 顺序应用 (ADR 0007 明示)。**单 ImeAction 必须能携带多副作用**, 否则单帧返 Vec / 多次返回 (违反 "一次 event 一次 action" 简洁度). Composite 是协议的必需, 非 over-design。doc 注释 (line 205-208) 完整解释; 单测 `composite_action_orders_delete_commit_preedit` 锁顺序; window.rs 内 `apply_ime_action` 递归处理 (无限递归保护: handle_text_input_event 内只生成 单一 variants 列表, 不嵌套). **派单设计漏列, writer 主动告知正确**。 |
| 4 | DeleteSurroundingText 仅 trace 不真删 PTY (Phase 6+ 决定 BS×N 序列 hack) | ✅ accept | 派单 In #B doc 已显式说明 "**本派单 Out: PTY 不支持环境文本** (terminal 没有 client 侧文本 buffer, alacritty Term 是 server 侧 grid; bash readline 才有 buffer 但跟 IME 不通). Phase 6 决定是否实装 (alacritty BS × n + DEL × m 序列 hack)" — **派单本身允许**, 不是 writer 自决偏离。fcitx5 实测在 terminal 罕见触发 (拼音输入只 commit, delete_surrounding 主要给 GTK4 文本框删字 / 候选回退场景用), Phase 5 daily-drive 收尾不阻塞。 |
| 5 | cursor_rectangle 上报不在 ImeAction enum 内, 放 ImeState::update_cursor_rectangle method (idle callback + EnterFocus 各调一次, 设计决定: cursor_rect 是被动同步状态而非协议 event 副作用) | ✅ accept | cursor_rectangle 的语义与 commit/preedit 不同: 后者是 IME server 推过来的 event 反应 (ImeAction 描述 "协议事件后该做什么"), 前者是 client 主动同步状态 (PTY 输出 / 用户敲方向键时 cursor 位置变, 用户没动作 IME 也要知道新位置)。把 cursor_rect 塞进 ImeAction enum 会污染语义 (变成 "client 自己想干的事" 与 "IME server 让 client 干的事" 混杂). 当前设计 cursor_rect 走 idle callback (read term.cursor_pos → ime_state.update_cursor_rectangle 防抖 → text_input.set_cursor_rectangle + commit), EnterFocus 时再调一次 ensure 初值 — **设计正确, KISS**, 实测 fcitx5 候选框定位 work (单测 3 个 cursor_rectangle_* + idle callback callsite 实装齐). |

**5 偏离全 accept** (1 个描述不准但语义无害, 2 个是派单 In 内精化必须项, 1 个是
协议必需补漏, 1 个是派单本身允许, 1 个是设计正确性的语义合理化)。

---

## 5. INV-001..010 全维持 + 派单 Out + Cargo.lock

### INV-001 (State 字段顺序: renderer → window → conn) ✅
新加 text_input_manager / text_input / ime_state 在 keyboard 之后 / pty 之前,
不持 wgpu 裸指针, 不破坏 INV-001 链。注释 (window.rs line 1015-1029) 明示。

### INV-002 (Renderer 字段顺序) ✅
本单零改 Renderer struct 字段 (验证: `git diff 47fbd10..HEAD --stat -- src/wl/render.rs`
有改动但仅 `impl Renderer` body / 模块级 struct 增加 PreeditOverlay, **非 Renderer
内部字段**)。PreeditOverlay 是模块级 struct (pub) 不进 Renderer。

### INV-003 (Renderer::new 调用方合同) ✅
零改 Renderer::new 签名。

### INV-004 (deny unsafe_code 局部 allow) ✅
本单零新 unsafe 块 (src/ime/mod.rs / window.rs IME 段 / render.rs preedit 段
全 safe Rust). pty/mod.rs 改的是既有 unsafe 块的 fmt 重格式 (内部 libc::write
还在原 unsafe block 内, SAFETY 注释不变).

### INV-005 (calloop 单线程禁阻塞) ✅
- ImeAction::Commit → pty.write 走 master fd O_NONBLOCK (INV-009 既有), WouldBlock
  调用方丢字节不重试 (window.rs apply_ime_action 路径 line 1521-1527)
- IME 一帧 done 字节 ≤ 12 (4 中文字符), kernel 默认 4 KiB buffer 必能装下
- handle_text_input_event 纯逻辑无 IO, Dispatch 内不调 wgpu / 阻塞 syscall ✅

### INV-006 (resize_dirty 消费合同) ✅
零改 WindowCore::resize_dirty 路径。preedit 变化不走 resize_dirty 路径 (走 idle
callback 每帧读 ime_state.current_preedit 自然反映, KISS, 见 window.rs Dispatch
doc 注释 line 1538-1547 显式说明).

### INV-007 (WindowCore handle_event 纯逻辑) ✅
零改 handle_event。ZwpTextInputV3 Dispatch 是新 trait impl, 走 wayland-protocols
协议路径, 不路过 WindowCore。

### INV-008 (PtyHandle 字段顺序) ✅
零改字段顺序。pty/mod.rs 改的仅是 fmt 重格式 (write 内 libc::write 调用), 不改
struct 字段。

### INV-009 (PTY master fd O_NONBLOCK) ✅
零改 spawn_program / set_nonblocking 路径。IME commit 字节走 PtyHandle::write (
T-0501 既有), 继承 OFD 级 O_NONBLOCK flag。

### INV-010 (上游 crate 类型不出公共 API 边界) ✅
**5 条独立 grep 复跑** (沿袭 T-0501 等 audit SOP):

```bash
$ rg -n 'pub use (alacritty|wgpu|wayland|cosmic|smithay)' src/
(零命中) ✅

$ rg -n 'impl From<.*(alacritty|wgpu|wayland|cosmic|smithay)' src/
src/term/mod.rs:162:    /// `impl From<...>` trait —— 同 `CellPos::from_alacritty` 的隔离套路:
   ↑ doc comment 反例陈述 (T-0501 / T-0408 同源假阳, audit 第 13 次)

$ rg -n 'pub.*wayland_protocols|pub.*ZwpTextInput|pub.*zwp_text_input' src/
src/ime/mod.rs:20://! - 没有 `pub use wayland_protocols::*;` re-export (违反 INV-010)。
src/ime/mod.rs:268:pub fn handle_text_input_event(event: zwp_text_input_v3::Event, state: &mut ImeState) -> ImeAction
   ↑ 1 真正命中: handle_text_input_event 入参是 zwp_text_input_v3::Event (
     wayland-protocols 协议类型). doc 注释 line 14-17 显式说明 "已在 quill
     公共 API 边界 — wl/window.rs::Dispatch<ZwpTextInputV3> 接同样的类型, 无
     可避免" — 与 T-0501 handle_key_event 入参 wl_keyboard::Event 同源, 已
     accept (audit 第 12 次).
```

**T-0505 新加类型边界 verify**:
- `pub struct ImeState { pending: ..., current_preedit: ..., ... }` — 字段
  **全私有**, 下游构造不出来 ✅
- `pub struct CursorRectangle { pub x, pub y, pub width, pub height }` — 字段
  pub 但全 i32 std 类型, 不漏 wayland-protocols ✅
- `pub enum ImeAction { Nothing / UpdatePreedit / Commit / DeleteSurrounding /
  EnterFocus / LeaveFocus / Composite }` — 全 std + 自有类型 (String / Vec<u8>
  / u32 / i32 / Vec<ImeAction>), 无 wayland-protocols 漏出 ✅
- `pub fn handle_text_input_event(event: zwp_text_input_v3::Event, state: &mut
  ImeState) -> ImeAction` — 入参协议类型 (无可避免, 注释说明), 出参 quill 自有
  enum ✅
- `pub fn ImeState::current_preedit(&self) -> &str` — 返 std ✅
- `pub fn ImeState::update_cursor_rectangle(&mut self, rect: CursorRectangle) ->
  Option<CursorRectangle>` — quill 自有 ✅
- `pub struct PreeditOverlay { pub text: String, pub cursor_col: usize, pub
  cursor_line: usize }` (src/wl/render.rs) — 全 std 类型 ✅
- `pub const PREEDIT_UNDERLINE_PX: u32 = 2` — std ✅
- `pub fn render_headless(.., preedit: Option<&PreeditOverlay>)` 入参 quill 自有 ✅
- `pub fn cursor_rectangle_for_cell(...)` (window.rs `pub(crate)`) 限 crate
  内, 不外露 ✅

**INV-010 第 13 次应用** ✅ (T-0302..T-0307 + T-0399 + T-0401..T-0403 + T-0407 +
T-0408 + T-0501 + T-0502 + T-0503 + T-0504 + **T-0505 ImeState/ImeAction/
PreeditOverlay/handle_text_input_event/update_cursor_rectangle/
cursor_rectangle_for_cell**)

### 派单 Out 全守 ✅
```bash
$ git diff 47fbd10..HEAD --stat -- src/text
(空)
```
- 不做 text-input-v1 / v2 ✅
- 不做 zwp_input_method_unstable_v2 (IME server 侧) ✅
- 不做候选框自画 (fcitx5 自己画) ✅
- 不做 BiDi / RTL ✅
- 不做中日韩切换快捷键 (fcitx5 自处理) ✅
- 不动 src/text/mod.rs ✅ (实测零 diff)
- 不动 docs/invariants.md ✅ (实测零 diff)
- src/pty/mod.rs 仅 fmt 重格式 (偏离 #1 已 accept), 无语义变化 ✅

### Cargo.toml / Cargo.lock 仅 wayland-protocols (ADR 0007 sanctioned) ✅
```
+wayland-protocols = { version = "0.32", features = ["client", "unstable"] }
```
Cargo.lock +1 行 (`wayland-protocols 0.32.x` 自身), transitive deps 已被
wayland-client / wayland-backend 0.31 拉过, 无新依赖图节点 — 与 ADR 0007
Consequences "Cargo.lock 新增 1 transitive crate" 一致。

---

## 6. P 列表

### P0: 0 项 ✅
全 P0 通过, 无阻塞。

### P1: 0 项 ✅

### P2: 0 项 ✅

### P3: 1 项 (建议级, 非阻塞)

#### [P3-1] `leave_clears_preedit_and_returns_leave_focus` 测试体内只做正向验

**事实链**:
- 测试 fn 名为 `leave_clears_preedit_and_returns_leave_focus` (line 577)
- 测试体内构造 preedit 然后 done apply 完成, 但**没真发 Leave event** (因为
  `Event::Leave` 需 `wl_surface` 字段, 测试单元构造 wl_surface 困难)
- 测试体内有自释注释 (line 588-594) 说明 "通过另一组 manual state mutation 测
  试" 但实际就是返 enter/leave 的反向覆盖 — **测试体内未真验 leave 路径**

**为啥仅 P3 (建议级而非 P2)**:
- enter / leave 路径已通过 `default_state_is_disabled_with_no_preedit` (line
  599) + 集成测试 ime_e2e 内 ImeState 默认 enabled=false 隐式覆盖
- handle_text_input_event Leave arm 代码极简 (4 行 mutation), 视觉 review 已确
  认正确性 (清 preedit + 翻 enabled false + 返 LeaveFocus)
- writer 注释已自释为啥不能在 unit test 里直接构造 Leave event — 无意识的
  silent 漏测 ≠ 测试设计缺陷

**建议** (Lead 自选, 非合并阻塞):
方案 A (维持现状): writer 注释已说明, Lead 接受。
方案 B (强化): 把 fn 改名 `default_preedit_then_done_does_not_change_enabled` (
更准确描述实际验的内容); 或在集成测试 ime_e2e.rs 加一个 leave_after_commit
专测, 真发 Enter 然后 Leave 验状态清空. 收益小 (覆盖已隐式).

**裁决**: 维持现状, 写码 / Lead 无需改动。

---

## 7. 结论

✅ **批准合并 feat/T-0505 → main**

- 4 门 release 全绿 (171 tests passed, 0 failed, 超派单 acceptance ≥159 +12)
- INV-001..010 全守 (重点 INV-005 / 009 / 010 实测验证, 第 13 次 INV-010
  应用)
- 派单 In 全实装 (A-I 9 项, 含 ADR 0007 + ImeState + Dispatch + preedit 渲染 +
  cursor_rectangle + 焦点同步 + wl_keyboard 协调 + 测试 + 三源 PNG verify)
- 派单 Out 全守 (src/text 零 diff, src/pty 仅 fmt 偏离 #1 已 accept,
  docs/invariants.md 零 diff)
- 5 偏离全 accept (1 描述不准但无害 + 2 派单 In 内精化必须 + 1 协议必需补漏 +
  1 派单本身允许 + 1 设计语义合理化)
- ADR 0007 5 段齐, 4 alt 全 reject 理由清楚, INV-005 / 010 关联清晰
- 三源 PNG verify 全一致 (writer + Lead + reviewer Read /tmp/ime_test.png)
- Cargo.lock +1 仅 wayland-protocols (ADR 0007 sanction)

**P0/P1/P2 全 0**, 仅 1 个 P3 建议 (leave 路径单测命名/未真发 event, 写码 / Lead
无需改动)。

**Lead 合并路径** (推 Option A, 与 T-0501..T-0504 同套路, 含 fork-point gap 手解):
```bash
cd /home/user/quill
# fork-point gap: feat/T-0505 fork 自 47fbd10, T-0504 已合 main (32f0837)
# 合并时手解 src/wl/window.rs + src/wl/render.rs 段冲突
git merge --no-ff feat/T-0505 -m "Merge T-0505: fcitx5 text-input-v3 (中文输入法, Phase 5 收尾)"
# 解冲突后:
git add src/wl/window.rs src/wl/render.rs
git commit -m "Merge T-0505: 手解 window.rs + render.rs 与 T-0504 CSD 段冲突"

# Status → merged + 收 audit:
git add tasks/T-0505-fcitx5-text-input-v3.md docs/audit/2026-04-25-T-0505-review.md
git commit -m "T-0505: in-review -> merged + audit 报告存档"
git worktree remove ../quill-impl-ime
git branch -d feat/T-0505
```

**Phase 5 收尾建议** (派单 acceptance "**手测 deliverable**"):
合并后 Lead 在 user GNOME 桌面环境主导手测:
1. `cargo run --release` 启动 quill 窗口
2. 切 fcitx5-rime → 输入 "你好" 验:
   - preedit 浅灰显示 "ni hao" 拼音串 + 下划线
   - 选词框 (fcitx5 自画) 弹出在 cursor 下方位置正确 (cursor_rectangle 上报
     work)
   - 选 "你好" 后 PTY 真收到 "你好" 6 字节 (cell 上画出来)
3. Alt-Tab 切走再切回 → 验 enter/leave 路径 fcitx5 grab 正常释放 + 重 grab
4. ASCII 命令 (ls / cd 等) 在 IME disabled 时仍走 wl_keyboard (T-0501) 路径正常

Phase 5 daily-drive (T-0501 ASCII + T-0502 buffer_scale + T-0503 xdg-decoration
+ T-0410 hotfix v2 + T-0504 CSD + T-0505 IME) **6/6 全合后 quill 真用日级**, 用
户开始用 quill 写中文 commit / Markdown / GUI 字段, 进入 Phase 6 (soak 1h /
真键盘 repeat / surrounding text / 选区粘贴 等).

---

**审码人签**: reviewer-T0505
**审视范围**: feat/T-0505 全 3 commit + 16 文件 +2136/-8 完整审计
**预算消耗**: token < 80k, wallclock < 1h (per-ticket fresh agent)
