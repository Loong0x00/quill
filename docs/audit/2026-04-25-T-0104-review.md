# 审码报告: T-0104 关闭窗口时优雅退出

**审码人**: 审码 (quill-phase2 team, Haiku 4.5, Explore)
**日期**: 2026-04-25
**范围**: feat/T-0104 分支, 2 commit
- `708fc98` T-0104: 关闭路径优雅退出 (SIGINT/SIGTERM + xdg close + INV-001 drop)
- `d6455c5` T-0104: Status claimed -> in-review

**diff stat**: 5 files, +451/-14
- `Cargo.lock` +22 (signal-hook + signal-hook-registry 新增)
- `Cargo.toml` +2 (signal-hook + rustix 直接依赖; rustix 原为 transitive)
- `docs/adr/0003-signal-hook.md` +89 (signal-hook + rustix 双选, 手写 poll)
- `src/wl/window.rs` +350/-12 (self-pipe + 手写 poll + drop 路径 + 7 个单测)
- `tasks/T-0104-close-handling.md` +2/-2 (Status)

**判定**: ✅ 批准合并

## P0 关键点全部通过

### [P0-1] INV-001 / INV-002 字段顺序 ✅
- State 字段声明顺序 `renderer → window → conn` 不变, 按 Rust 正向析构 renderer 最先 drop, conn 最后
- Renderer 内部字段顺序 `surface → ... → instance` 不变

### [P0-2] INV-004 unsafe 注释完整 ✅
- Renderer::new 的 SAFETY 注释覆盖所有前置条件 (指针合法 / 生命周期 / wgpu 不 drop wl 对象)
- self-pipe / signal fd 的 unsafe 路径由 signal-hook-registry 封装, 未新增用户级 unsafe

### [P0-3] INV-005 零新线程 ✅
- 全文无 `std::thread::spawn` / `tokio::spawn`
- signal-hook 的 handler 是同步 C extern 函数, 直接走 `AtomicBool::store` + `write` self-pipe (async-signal-safe)
- ctrlc 被 ADR 0003 正确 Reject (它内部起后台线程违反 INV-005)

### [P0-4] EINTR 路径处理 ✅
- wayland-client 0.31 `blocking_dispatch` 吞 EINTR 是确认的 bug
- 写码手写 `flush + prepare_read + poll(wayland_fd, sig_pipe_fd) + read + dispatch_pending` 循环, 通过 self-pipe 把 signal 转化为 fd 可读事件
- poll 的 EINTR 返回在外层 loop 顶重新检查 flag → 信号投递到退出的延迟 = 下一轮 poll 的 epsilon
- ADR 0003 承认的"纳秒级竞态窗口"属实, 后续 T-0105 整合 wayland 进 calloop 后可切 signalfd 彻底消除

## P1 违规: 零

- 非 main/tests 代码零 `unwrap` / `expect` (全 `anyhow::Context` 或 `?`)
- 零 `println!`, 零未注释 unsafe
- 新增 crate 有 ADR 0003 支持

## P2 架构 + 依赖

### [P2-1] ADR 0003 选型 ✅
- signal-hook > ctrlc (ctrlc 起线程 Reject)
- signal-hook > 裸 libc::sigaction (signal-hook 是生态级成熟封装, 少造轮子)
- rustix > libc::poll (rustix 的 PollFd/BorrowedFd 是一等 Rust 类型, 与上游 wayland-backend 同栈)
- rustix > nix (nix 版本 0.31 与 rustix 重复, rustix 现代)
- Alternatives 论证完备, Consequences 承认的竞态窗口诚实

### [P2-2] 模块设计 ✅
- `run_main_loop` / `StepResult` / `install_signal_handlers` / `pump_once` / `drain_pipe` 职责清晰, 每个函数单元测试可达
- pump_once 的"快路径" (dispatched > 0 时立刻回 top) 避免信号处理延迟

### [P2-3] 测试覆盖 ✅
7 个新单测:
- `run_main_loop_exits_after_signal_raises_flag_mid_run` — 循环中 flag 变 true 立退
- `run_main_loop_exits_immediately_when_flag_already_set` — 进入即退
- `drain_pipe_*` 2 路径
- state_machine 侧 `close_sets_exit_flag` / `disconnect_sets_exit_flag` 覆盖 xdg close + compositor disconnect

## P3 建议 (无阻塞)

无重要建议。设计超预期, ADR 对竞态条件分析透彻, 已知局限 + 后续改进方向清楚。

## 合并判定

✅ 批准, 可直接 `git merge --no-ff feat/T-0104` 到 main。

## 未测项说明 (可接受)

**窗口右上角叉手测**: GNOME mutter 无通用关窗 IPC (wlroots foreign-toplevel 协议 mutter 未实现)。`WindowHandler::request_close` 的 3 行触发逻辑与 signal 退出共用 `should_exit` + `run_main_loop`, 已被 `state_machine.rs::close_sets_exit_flag` 单测覆盖。符合 ticket Acceptance 的"对 should_exit 被置位后主循环应立退逻辑有单测" 要求。

## 给后续 ticket 的前置要求

### 关于 T-0205 (Phase 2 子进程退出)
T-0205 依赖 T-0104 的 `should_exit` 机制, 本次合并后 T-0205 的前置要求第一条可勾。

### 关于 T-0105 后续扩展
当前手写 poll 循环是为了**不在** T-0104 引入 wayland → calloop 的整合 (超出 ticket 范围)。后续 ticket 若把 wayland event_queue 接进 calloop::EventLoop, 可以:
- 切 signal 处理为 `calloop::signals::Signals` (signalfd 路径, 无 self-pipe)
- 移除 `rustix::event::poll` 直接调用
- 纳秒级竞态窗口彻底消除

建议作为 Phase 2 完成后或 Phase 3 前的 refactor ticket 开 (非本 phase 阻塞)。
