# T-0604 cell.bg default skip + cursor inset - audit 报告

日期: 2026-04-26
reviewer: reviewer-T0604 (Opus 1M, ASCII name, per-ticket fresh agent)
worktree: /home/user/quill-impl-cellbg (feat/T-0604)
fork-point: f52fb46 (T-0602 Lead 跟进合并后 Phase 6 三并行全合 HEAD)
HEAD: 4247546

commits 审 (3):
- 2fa0521 T-0604: claim by writer-T0604
- 4cd672a T-0604: cell.bg default skip + cursor inset (视觉对齐主流终端)
- 4247546 T-0604: Status claimed -> in-review

**判定**: ✅ **pass** (P0 零阻塞, P2 观察 2 + P3 跟进 1, 均不阻塞)

---

## 4 门状态 (独立复跑)

```
$ cd /home/user/quill-impl-cellbg

$ cargo build --release
Finished `release` profile [optimized] target(s) in 0.08s ✅

$ cargo clippy --all-targets --release -- -D warnings
Finished `release` profile [optimized] target(s) in 0.05s ✅ (零 warning)

$ cargo fmt --check
(零输出) ✅

$ cargo test --release
TOTAL: 265 tests pass ✅
  含本单新增 8 lib unit tests:
    - cursor_inset_is_one_logical_px
    - cell_bg_default_matches_alacritty_default
    - build_vertex_bytes_skips_default_bg_under_bg_source
    - build_vertex_bytes_keeps_explicit_bg_under_bg_source
    - build_vertex_bytes_keeps_fg_path_for_default_bg_cell
    - build_vertex_bytes_still_skips_space_cell
    - cursor_block_x_range_is_inset_from_cell_edges
    - cursor_hollow_block_x_range_is_inset
  含本单改 2 老 cursor 单测 inset 同步:
    - cursor_block_at_origin_has_correct_ndc_corners
    - cursor_beam_left_edge_thickness_correct
  含本单改 2 cursor_render_e2e 集成测试 inset 同步:
    - cursor_beam_renders_left_strip
    - cursor_hollow_block_renders_4_borders
```

派单 Acceptance "总测试 257 + ≥3 ≈ 260+ pass" — 实测 265 (+8). 命中 ✅

**4 门全绿** ✅

---

## 视觉 verify (T-0408 三源 PNG SOP)

派单 In #E + Acceptance 要求三源 PNG verify (T-0405 SOP: 任一不一致即 P0)。

### 写码-T0604 自报 (commit 4cd672a body)
- /tmp/t0604_test.png 1600×1200, 整片深蓝 #0a1030 + prompt `[user@userPC ~]$`
  浅灰 #d3d3d3 直接画在深蓝上 (无每字黑底), titlebar + 三按钮在右上角

### Lead 第 2 源 Read /tmp/t0604_test.png
派单上下文 Lead 已视觉验过 "整片 #0a1030 深蓝清屏 + `[user@userPC ~]$` 浅灰直接
画在深蓝上, 不再每字黑底矩形" ✅

### reviewer 第 3 源 Read 2 PNG (本审码独立复跑)

**Read /tmp/t0604_test.png** (writer 产出, 1600×1200 PNG):
- 顶部 titlebar 深灰 + 右上 minimize / maximize / close 3 按钮符号 (T-0501 CSD)
- 主区整片深蓝 #0a1030 (clear color), 无任何黑色矩形
- prompt `[user@userPC ~]$` 浅灰 #d3d3d3 直接画在深蓝上
- 字符之间无黑色空隙 (Bug 1 + Bug 2 视觉修复) ✅

**reviewer 独立 PNG**:
```
$ cd /home/user/quill-impl-cellbg
$ cargo run --release -- --headless-screenshot=/tmp/t0604_reviewer.png
[INFO] TextSystem::new: locked PREFERRED monospace face primary_face=DejaVu Sans Mono
[INFO] TextSystem::new: emoji face blacklist populated emoji_face_count=1
[INFO] headless PTY drain 完成 read_bytes=258
[INFO] wgpu adapter (headless) 选中 backend=Vulkan name=NVIDIA GeForce RTX 5090
[DEBUG] render_headless stats cols=80 rows=24 logical_w=800 logical_h=600
        physical_w=1600 physical_h=1200 cell_vertex_count=198 glyph_vertex_count=90 atlas_count=12
[INFO] headless screenshot 写出 PNG 完成 path=/tmp/t0604_reviewer.png bytes=7680000
```

**Read /tmp/t0604_reviewer.png** (1600×1200 PNG, reviewer 独立产出):
- 与 writer PNG 视觉一致, 整片深蓝 + 浅灰 prompt 直接画在深蓝上 ✅
- `cell_vertex_count=198` (33 quads): T-0604 跳过路径生效 (无 default bg cell
  画矩形, 198 来自其他几何元素 — titlebar / 按钮 + scrollbar 等); T-0601 时
  全 cell 画 bg 路径 cell_vertex_count 远高于此 ✅
- `glyph_vertex_count=90 atlas_count=12` 与 T-0407 / T-0408 / T-0601 trace 数学
  一致 (15 字符 prompt → 15 visible glyph × 6 verts = 90, 12 unique glyph in
  atlas), 几何不变 ✅

**三源一致**: writer ✅ Lead ✅ reviewer ✅, T-0405 三源 PNG SOP "任一不一致
即 P0" — **三源全一致** ✅

### cursor 视觉验证 (相对 T-0601 reference)

本单不重做 cursor 4 形状 PNG (T-0601 已三源验过), inset 视觉验证由本单
单测 `cursor_block_x_range_is_inset_from_cell_edges` +
`cursor_hollow_block_x_range_is_inset` 严格 NDC 数学锁 (TL.x > cell_left_ndc,
BR.x < cell_right_ndc), 集成测试 `cursor_beam_renders_left_strip` 实测像素
左侧竖线起点 +inset_phys 通过, `cursor_hollow_block_renders_4_borders` 4 边
+ inset 像素阈值 ≥80% 通过. 几何 + 像素双层验证, 不需独立 PNG.

---

## 代码改动审视

### CELL_BG_DEFAULT 私有 const (src/wl/render.rs:362-376)

```rust
const CELL_BG_DEFAULT: crate::term::Color = crate::term::Color {
    r: 0x00,
    g: 0x00,
    b: 0x00,
};
```

- 模块私有 (无 pub), 与 TITLEBAR_BG / BUTTON_BG_HOVER / render_headless inline
  fg_default 同套路 (T-0501 / T-0407 沿袭)
- doc 注释 12 行 引派单硬约束"不动 src/term" + 与 `crate::term::Color::DEFAULT_BG`
  同源说明 + reviewer 走 grep 校对
- 测试 `cell_bg_default_matches_alacritty_default` 锁住 (0x00, 0x00, 0x00) 三字段
  契约, 防 src/term 改 DEFAULT_BG 时本模块漂移 ✅

### CURSOR_INSET_PX pub const (src/wl/render.rs:362)

```rust
pub const CURSOR_INSET_PX: u32 = 1;
```

- pub (re-export 给集成测试), 与 CURSOR_THICKNESS_PX 同模式
- doc 注释 9 行 引"why 不直接校准 CELL_W_PX" (跨 render/window/term 大改, Phase 6+
  单独 ticket 如需) + 派单字面 + 实战 cell 几何真因
- 测试 `cursor_inset_is_one_logical_px` 锁 = 1 防顺手改 0/2/4 ✅

### Renderer::build_vertex_bytes 跳过 default bg (src/wl/render.rs:1258-1270)

```rust
if matches!(color_source, CellColorSource::Bg) && cell.bg == CELL_BG_DEFAULT {
    continue;
}
```

- 紧跟 `cell.c == ' '` 短路之后, 在 vertex 计算之前 → 跳过路径短路, 不计算 NDC
- `matches!(color_source, CellColorSource::Bg)` 限定: Fg 路径 (Phase 3 fallback
  text_system None 时) 不受影响, T-0305 视觉契约维持 (P0-3 偏离主动告知)
- inline 注释 10 行 引 alacritty/xterm/foot 标准 + WIDE_CHAR_SPACER 同源 (Bug 2
  自动修) + Fg/Bg 路径区别原因 ✅

### render_headless 内联跳过 (src/wl/render.rs:2490-2500)

```rust
if cell.bg == CELL_BG_DEFAULT {
    continue;
}
```

- render_headless 是独立 free fn (T-0408 起手), 复用 build_vertex_bytes 同语义但
  inline 实现 (CellColorSource enum 在 Renderer impl, free fn 不持 self)
- 此处仅 Bg 路径 (T-0407 D fix render_headless 走 bg 染色, 见上方 line 注释)
  → 不需 CellColorSource 区分, 直接跳过 default bg ✅

### append_cursor_quads_to_cell_bytes inset (src/wl/render.rs:2193-2204)

```rust
let inset_px = CURSOR_INSET_PX as f32 * HIDPI_SCALE as f32;
let cell_x0 = cursor.col as f32 * cell_w_px + inset_px;
let cell_y0 = cursor.line as f32 * cell_h_px + y_offset_px;
let cell_x1 = cursor.col as f32 * cell_w_px + cell_w_px - inset_px;
let cell_y1 = cell_y0 + cell_h_px;
```

- inset_px 仅 x 方向 (派单字面 + writer 注释 line 2197 "字形 advance 是 x 方向,
  y 上下溢出非典型 ASCII / CJK 路径")
- 4 形状 (Block / Underline / Beam / HollowBlock) 共用 cell_x0 / cell_x1 一处
  计算 → 一处改 4 形状全 inset (偏离 4 主动告知, 接受)
- inline 注释 6 行 引派单 Bug 3 真因 + 字形 advance 跨 cell 溢出原理 ✅

### 8 新 lib 单测 + 2 老测试 inset 同步

新单测覆盖维度:
- 常数锁 2 (cursor_inset_is_one_logical_px / cell_bg_default_matches_alacritty_default)
- build_vertex_bytes Bg 路径跳过 1 (skips_default_bg_under_bg_source)
- build_vertex_bytes Bg 路径不误抹 explicit bg 1 (keeps_explicit_bg_under_bg_source)
- build_vertex_bytes Fg 路径不误抹 default bg cell 1 (keeps_fg_path_for_default_bg_cell)
- build_vertex_bytes 空格优先短路 1 (still_skips_space_cell)
- cursor inset NDC 几何 2 (cursor_block_x_range_is_inset_from_cell_edges /
  cursor_hollow_block_x_range_is_inset)

老测试改:
- cursor_block_at_origin_has_correct_ndc_corners: 加 inset_px 项, NDC 期望 TL.x
  从 -1.0 改 inset_px/sw*2-1, BR.x 从 cw/sw*2-1 改 (cw-inset_px)/sw*2-1
- cursor_beam_left_edge_thickness_correct: BR.x 加 inset_px 偏移 + thickness_px

### MaybeRenderer sentinel (src/wl/render.rs:3692-3777)

writer 引入 #[cfg(test)] 工具结构体 `MaybeRenderer`, 复制 Renderer::build_vertex_bytes
的 vertex 数学 body, 仅替换 `self.color_for_vertex(color)` 路径为
`color_for_vertex_with_srgb(color, self.surface_is_srgb)` (避免 wgpu 资源依赖).

**判**: ✅ 接受 — `Renderer::new` 需要 GPU init (wgpu adapter / surface / pipeline),
无法在纯 lib unit test 跑; `MaybeRenderer` 沿袭 T-0408 render_headless "走
surface_is_srgb 显式参数" 策略 (T-0408 audit P0-12 同源). body 复制有
**P3 跟进风险** (build_vertex_bytes body 改后 sentinel 可能漂移), 但当前 body
极简 (15 行), 测试集中校 default bg 跳过 + Fg 路径维持, 漂移风险低. 见 P3-1.

### tests/cursor_render_e2e.rs 改 (+52/-11)

- import `CURSOR_INSET_PX` 加 (line 25, 与 CURSOR_THICKNESS_PX 并列)
- `cursor_beam_renders_left_strip` Beam left strip 起点 `(x0, x0+thickness)` →
  `(x0+inset_phys, x0+inset_phys+thickness)`, inset_phys = 2 phys
- `cursor_hollow_block_renders_4_borders` 4 边 bbox 全 inset 同步:
  - inner_x0 = x0 + inset_phys, inner_x1 = x1 - inset_phys
  - top/bottom strip 取 inner_x0..inner_x1 (内缩后)
  - left strip (inner_x0, inner_x0+thickness), right (inner_x1-thickness, inner_x1)
  - 中心 hollow inset 跟 (inner_x0+thickness, inner_x1-thickness)
  - 6 集成测试全过 ✅

---

## 5 偏离 accept/reject (writer 主动告知)

### [偏离 1] CELL_BG_DEFAULT 私有 const 替代 crate::term::Color::DEFAULT_BG ✅ accept

**派单原文** (In #A): `if cell.bg == crate::term::Color::DEFAULT_BG { continue; }`

**writer 实装**: render 模块本地 `const CELL_BG_DEFAULT: crate::term::Color = ...`
(literal 复制 #000000)

**writer 论据**: src/term/mod.rs::Color::DEFAULT_BG 是模块私有 const (无 pub),
派单字面引用编译不通过. 派单硬约束 "不动 src/term/mod.rs (DEFAULT_BG 常数语义
保持)" — 改可见性虽不破语义但属字面动 src/term, 折中走 render 本地 const +
`cell_bg_default_matches_alacritty_default` 测试锁同步契约.

**独立判**: ✅ **接受偏离** — 我 grep verify `src/term/mod.rs::Color`:

```
$ grep -nE "DEFAULT_BG" /home/user/quill-impl-cellbg/src/term/mod.rs | head -5
```

DEFAULT_BG 确实是模块私有 const (T-0407 audit P0-10 已 verify line 218-222),
无 pub. 派单硬约束"不动 src/term"是真实 binding constraint, 改 visibility 虽
最小但属字面动 src/term — writer 的 render 本地 const + 测试锁同步契约是合理
折中:
- 与 TITLEBAR_BG / BUTTON_BG_HOVER (T-0501) 同模式: 渲染相关常数 inline 在
  render 模块, 不依赖 term 模块暴露
- 测试 `cell_bg_default_matches_alacritty_default` 锁 (0x00, 0x00, 0x00) 三字段
  契约, src/term 改 DEFAULT_BG 时该测试失败 → 提示同步, 漂移风险低
- 副作用: 双源真相 (term + render 各自 #000000), 但因测试锁 + 注释引"reviewer
  走 grep 校对" → 长期可维护

未来如需统一来源, Phase 6+ 可 refactor: 把 DEFAULT_BG / DEFAULT_FG 提到 src/term
公共 pub const (无 break 风险, 仅可见性升级), render 模块改用 `crate::term::Color::DEFAULT_BG`.
当前 ticket 范围内零改 src/term 是正解 ✅

### [偏离 2] 走 DEFAULT_BG 而非 clear_color_rgb ✅ accept (派单字面允许)

**派单原文** (In #A): "也可走更准的判断 `if cell.bg == clear_color_rgb` (配
clear color #0a1030) — 但 alacritty 标准是 'default bg 不画', 跟具体 clear color
无关; 实装走 DEFAULT_BG"

**writer 实装**: 走 DEFAULT_BG (派单字面建议)

**独立判**: ✅ **接受** — 派单字面已 explicit 给方向, 不构成偏离. alacritty
NamedColor::Background 是 vt100 协议层概念 (与 clear color 解耦), 与 clear
color 比较是错语义. ✅

### [偏离 3] 跳过路径仅 Bg source 生效 ✅ accept

**派单原文** (In #A 隐含): 派单 #A 字面只论 Bg 路径 (cell.bg 跳过 vertex), Fg
路径 (Phase 3 fallback) 未提及.

**writer 实装**: `if matches!(color_source, CellColorSource::Bg) && cell.bg ==
CELL_BG_DEFAULT { continue; }` — source 限定 Bg.

**writer 论据**: Fg 路径 (text_system None 时, draw_cells 调用) 走 fg 色块作
"看见 prompt 字符位置以色块画出" 锚点 (T-0305 acceptance). 若不区分 source 一律
跳过 default bg cell, Fg 路径下绝大多数 cell.bg = DEFAULT_BG → 全跳过 → fg 色块
全消失, T-0305 fallback 视觉契约破.

**独立判**: ✅ **接受偏离** — 关键: T-0305 acceptance 是 fallback 视觉契约
(text_system 加载失败 graceful degradation), 不能为修 default-bg 路径副作用而
偷偷破坏. writer 加 `build_vertex_bytes_keeps_fg_path_for_default_bg_cell` 测试
锁住 Fg 路径不受影响, 防未来"顺手改一律跳过"的回归. 与 T-0407 D fix CellColorSource
enum 设计完全契合 (P0-7/P0-8 拓展). ✅

### [偏离 4] cursor inset 4 形状全应用 (派单字面 underline/beam → 实装包 HollowBlock) ✅ accept

**派单原文** (In #C): "block / hollow / underline / beam 都 inset, 字形溢出像素
不被覆盖"

**writer 实装**: 4 形状 (Block / Underline / Beam / HollowBlock) 全 inset,
共用 cell_x0 / cell_x1 一处计算.

**独立判**: ✅ **接受 (派单原文实际已 explicit 列 4 形状)** — 重读派单 line 60
"block / hollow / underline / beam 都 inset", 4 形状已字面要求, 非偏离. writer
"主动告知" 此项实为风险预警 (HollowBlock 边框 4 边内缩 inset 后视觉边界与 Block
一致, 无怪异), 实装与派单完全一致. HollowBlock 内缩验证由 `cursor_hollow_block_x_range_is_inset`
单测 + `cursor_hollow_block_renders_4_borders` 集成测试双层覆盖 ✅

### [偏离 5] PNG 单文件 /tmp/t0604_test.png ✅ accept

**派单原文** (In #E): "writer 跑 /tmp/t0604_test.png 自验"

**writer 实装**: 单 PNG (与派单字面一致)

**独立判**: ✅ **接受** — 派单字面要求单 PNG. T-0601 4 PNG 是因为 4 cursor 形状
独立验证, 本 ticket 验 cell.bg 跳过整页视觉 (Bug 1 + Bug 2 同源, 一张 PNG 即覆盖)
+ cursor inset 由单测/集成测试双层覆盖, 不需独立 cursor PNG. 单 PNG 充足. ✅

**5 偏离全 accept, 主动告知 + 论据完整, conventions §5 命中** ✅

---

## INV-001..010 全维持

### INV-001 (State 字段顺序)
```
$ git diff f52fb46..HEAD -- src/wl/window.rs
(零输出) ✅
```
src/wl/window.rs 零改动, struct State 字段顺序保 ✅

### INV-002 (Renderer 14 字段顺序 + GlyphAtlas 6 字段顺序)
```
$ git diff f52fb46..HEAD -- src/wl/render.rs | grep -E "^[+-]\s*(surface|cell_|glyph_|device|queue|config|clear|surface_is_srgb|instance):"
(零输出) ✅
```
Renderer / GlyphAtlas struct 字段定义未动 (本 ticket 仅函数 body / 私有 const /
测试改动), 字段顺序 invariant 维持 ✅

### INV-003 (unsafe Renderer::new 合同)
```
$ git diff f52fb46..HEAD -- src/wl/render.rs | grep -E "^\+.*unsafe"
(零输出) ✅
```
本 ticket 零新 unsafe ✅

### INV-004 (deny unsafe_code)
src/lib.rs / src/main.rs 顶端 deny 未动 ✅

### INV-005 (calloop 单 IO 调度器)
本 ticket 仅改 vertex 数学 + 私有 const + 测试, 无 IO / fd / thread::spawn ✅

### INV-006 / INV-007 (resize_dirty 消费契约 / WindowCore 纯逻辑)
src/wl/window.rs 零改动 ✅

### INV-008 / INV-009 (PtyHandle 字段顺序 + O_NONBLOCK)
```
$ git diff f52fb46..HEAD -- src/pty/
(零输出) ✅
```

### INV-010 (上游 crate 类型不出公共 API 边界) **第 13 次应用零真违规**
```
$ grep -rnE 'pub use (alacritty|wgpu|wayland|cosmic)' src/
src/ime/mod.rs:20://! - 没有 `pub use wayland_protocols::*;` re-export (违反 INV-010)。
   ↑ 仅 doc comment 内反例陈述 (T-0407 / T-0601 假阳同源) ✅

$ grep -rnE 'impl From<(alacritty|wgpu|wayland|cosmic)' src/
(零命中) ✅
```

**T-0604 INV-010 真路径 verify**:
- `CELL_BG_DEFAULT: crate::term::Color` — quill 自有 Color struct, 不暴露
  alacritty NamedColor / vte::ansi::Color
- `CURSOR_INSET_PX: u32` — Rust 基础类型, 无上游耦合
- `MaybeRenderer` 测试结构体 — 仅 #[cfg(test)], 不在 pub API 边界
- 改动函数 `build_vertex_bytes` / `append_cursor_quads_to_cell_bytes` /
  `render_headless` 签名维持 (本 ticket 零新 pub API)

**INV-010 第 13 次应用 (T-0302 / 0303 / 0304 / 0305 / 0306 / 0399 / 0401 / 0402 /
0403 / 0407 / 0405 / 0601 / **0604**) 零真违规** ✅

---

## 派单 Out 守

```
$ git diff f52fb46..HEAD -- src/term src/pty src/text docs/invariants.md Cargo.toml Cargo.lock src/main.rs src/wl/window.rs
(零输出) ✅
```

派单 Out 全清单: src/term / src/pty / src/text / docs/invariants.md / Cargo.toml /
Cargo.lock / src/main.rs / src/wl/window.rs — **全部未动** ✅

---

## 不引新 crate

```
$ git diff f52fb46..HEAD -- Cargo.toml Cargo.lock
(零输出) ✅
```

---

## 生产零 unwrap / expect / println / unsafe / thread::spawn

```
$ git diff f52fb46..HEAD -- src/wl/render.rs | grep -nE '^\+.*(\.unwrap\(\)|\.expect\(|println!|thread::spawn|unsafe)'
325:+        let tl_x = f32::from_ne_bytes(bytes[0..4].try_into().unwrap());
327:+        let br_x = f32::from_ne_bytes(bytes[br_off..br_off + 4].try_into().unwrap());
375:+            let x = f32::from_ne_bytes(bytes[off_v..off_v + 4].try_into().unwrap());
```

3 处 `.unwrap()` 全在 src/wl/render.rs `#[cfg(test)] mod tests` 内 (line 3640+
范围, 即 cursor_block_x_range_is_inset_from_cell_edges + cursor_hollow_block_x_range_is_inset
单测), 走 `[u8] -> [u8; 4]` cast — 长度静态保证不会失败. conventions §3
"test 路径允许" 命中 ✅

---

## Commit message 风格 (conventions §1)

- `2fa0521` claim commit: 标题 ≤ 70 char + ASCII writer name ✅
- `4cd672a` 主体 commit: HEREDOC 单引号 + What/Why/副作用/测试 4 段齐 + 派单 vs
  实装偏离主动告知 (5 项详述) + diff stat + INV grep 状态 + 视觉 PNG 描述,
  完整度优秀 ✅
- `4247546` Status commit: 单文件 ±1 行 (Status + Assigned), 符合 conventions §4
  Step 4 ✅

---

## P 列表

### P0 (阻塞)
- 无 ✅

### P1 (主合后跟进)
- 无 ✅

### P2 (低优观察, 不阻塞)

**[P2-1] CELL_BG_DEFAULT 双源真相 (render 本地 const vs term::Color::DEFAULT_BG)**

writer 因派单"不动 src/term"硬约束折中走 render 本地 const + 测试锁同步. 测试
`cell_bg_default_matches_alacritty_default` 锁 (0x00, 0x00, 0x00), src/term 改
DEFAULT_BG 时该测试失败 → 提示同步. 漂移风险低但非零.

**不阻塞** (writer 注释 line 369 已写 "改动同步 ... reviewer 走 grep 校对",
测试锁基本契约). 建议 Phase 6+ 一起把 src/term::Color::{DEFAULT_BG, DEFAULT_FG}
改 pub (无 break 风险, 仅 visibility 升级), render 改用
`crate::term::Color::DEFAULT_BG` 单源真相. 留 follow-up.

**[P2-2] 5090 NVIDIA Wayland 已实测 — display-on cursor inset 视觉感受未真测**

本 ticket headless PNG 验证清晰, 但 cursor 4 形状 + inset 在真窗口 (cargo run
--release, NVIDIA 5090 6K display-on) 的视觉感受 (字形溢出真被 inset 让出 / 用户
是否觉得 cursor "飘") 未由 reviewer / Lead 物理验. writer 在 commit body 标
"派单 Acceptance 手测 deliverable: cargo run --release 视觉" 但实际只交付 headless
PNG.

**不阻塞** (T-0408 SOP "headless PNG = 三源 verify 替代真窗口手测" 已被前 11
ticket 沿袭接受, 物理 GUI 限制是 agent 共性). user 实测后若 cursor inset = 1
logical px = 2 physical 视觉不"飘" → 接受; 若觉得"飘" → 简单调常数 0/1/2 重测,
代价低. inset 量校准是 user 主观判定, agent 数学层 + 像素阈值层验证已充分.

### P3 (nice-to-have)

**[P3-1] MaybeRenderer body 复制漂移风险**

`MaybeRenderer::build_vertex_bytes` (test 工具 line 3724-3776) 复制
`Renderer::build_vertex_bytes` (line 1242-1305) 的 vertex 数学 body, 仅替换
sRGB 路径. 当前 body 极简 (15 行), 漂移风险低; 但未来若 build_vertex_bytes 加
逻辑 (如 Selection 高亮 / IME composition cell 染色), 测试 sentinel 不跟则测试
覆盖与生产路径分叉.

**不阻塞** — writer 注释 line 3719-3722 已说明 sentinel 套路 (与 T-0408
render_headless 走 surface_is_srgb 显式参数策略一致). 升级路径明确: 提取
`build_vertex_bytes_inner(...) -> Vec<u8>` 静态 fn, Renderer + MaybeRenderer 都
调用, 一处改两处自动同步. Phase 6+ 加 Selection / IME composition 等 cell
染色路径时一起 refactor.

---

## 写码偏离 vs 派单 总览 (conventions §5 主动告知 verify)

| # | writer 偏离/扩展 | 派单字面 | reviewer 独立判 |
|---|---|---|---|
| 1 | CELL_BG_DEFAULT 私有 const | `crate::term::Color::DEFAULT_BG` | 接受 (偏离 1) — DEFAULT_BG 模块私有, 派单"不动 src/term"硬约束, 折中合理 + 测试锁同步 |
| 2 | 走 DEFAULT_BG (非 clear_color_rgb) | "实装走 DEFAULT_BG" 字面建议 | 接受 (偏离 2) — 派单字面允许, 非真偏离 |
| 3 | 跳过仅 Bg source 生效 | 派单字面只论 Bg 路径 | 接受 (偏离 3) — Fg 路径保 T-0305 fallback 视觉契约, source 限定是正解 |
| 4 | cursor inset 4 形状全应用 | "block / hollow / underline / beam 都 inset" 字面 | 接受 (偏离 4) — 派单原文已 explicit 列 4 形状, 非真偏离 |
| 5 | PNG 单文件 t0604_test.png | "/tmp/t0604_test.png 自验" 字面 | 接受 (偏离 5) — 派单字面一致, 非偏离 |

**5 项主动告知, 实际 3 项是派单字面已允许的非偏离 (writer 谨慎披露), 2 项是
真折中 (CELL_BG_DEFAULT 私有 const + Bg source 限定), 全 accept. conventions §5
主动告知 SOP 命中** ✅

---

## 给后续 ticket 的前置信息

### 给 Phase 6 后续 (theme / 闪烁 / focus-aware)

- T-0604 修复三视觉 bug 同源 (default bg 不画 + cursor inset), 解 user 实测
  daily-drive P0
- 渲染清屏色 #0a1030 现真透出 (而非每字黑底覆盖), Phase 6+ theme ADR 改 clear
  color 时无需特殊处理 cell.bg = DEFAULT 路径 (DEFAULT_BG 跳过逻辑与 clear 色解耦)
- explicit cell.bg (vim status / less 反色 / ls --color SGR 48) 路径不受影响,
  vim 高亮等场景 cell 仍画 explicit bg 矩形
- cursor inset = 1 logical 实测视觉 ok 的话 Phase 6+ blink ticket 不需调; 若
  user 反馈 inset "飘", 简单改常数 0 关闭 inset 即可
- HollowBlock cursor 在 inset 后视觉边界仍清晰 (4 边框 + 中心 hollow), focus-aware
  Block ↔ HollowBlock 切换 (Phase 6+ wl_keyboard enter/leave) 视觉一致

### 给 mainline-audit Phase 6 末

- INV-010 第 13 次应用零真违规 (沿袭统计)
- INV-001..009 全维持
- 总测试 257 → 265 (T-0604 +8)
- T-0604 视觉验收: 三源 PNG 一致 (writer + Lead + reviewer), Bug 1 (每字黑底)
  + Bug 2 (CJK 黑空隙) 同源修复, Bug 3 (cursor 盖字) 几何 inset 修复
- 双源真相 P2-1 留 follow-up: src/term Color::{DEFAULT_BG, DEFAULT_FG} 改 pub
  统一 (Phase 6+ 安全 refactor)

---

## 整体评价

T-0604 是 Phase 6 daily-drive polish 的 P0 (user 实测三视觉 bug 同源 — 每字
黑底 / CJK 黑空隙 / cursor 盖字, 都跟主流终端不一致). **跨 4 文件 / 3 commits
/ +431/-28 行 diff** (主体 commit 4cd672a 单文件主导 src/wl/render.rs +388,
聚焦 cell.bg + cursor 几何单一关注点, 派单 budget 80k 适当).

**派单 In #A (cell.bg default skip) + In #C (cursor cell inset) + In #D (测试
覆盖) + In #E (PNG verify) 全落地**, **In #B 字面 "cursor cell 不跳过 cell.bg"
通过 source 限定 Bg + cursor 在 cell 几何上独立画 quad 自动满足 (cursor cell
默认 bg 跳过后, cursor 在 clear color 上直接画 quad 视觉对)**. **Out 全维持
(src/term / src/pty / src/text / docs/invariants.md / Cargo.toml / src/main.rs /
src/wl/window.rs 零 diff)**, **4 门绿 265 tests, INV-010 第 13 次应用零真违规,
INV-001..009 全维持**.

**关键架构判断 — 三 bug 同源修复**: Bug 1 (每字黑底) 与 Bug 2 (CJK 字间黑空隙)
本质同源 (cell.bg = DEFAULT_BG 默认黑覆盖 clear color), 一个 `if cell.bg ==
CELL_BG_DEFAULT { continue; }` 跳过分支同时修. Bug 3 (cursor 盖字) 是 cell
几何独立问题 (字形 advance > CELL_W_PX 时上一字形像素溢出本 cell 左 1 px,
cursor 在本 cell 左缘开画就盖到溢出像素), inset 1 logical px (= 2 physical)
让出溢出区. user 在派单已 trace 验证 cursor.col 数据正确 (输 'a' 后 col=18 =
prompt 17 + 1), 排除 vt100 协议 bug → 真因是 cell 几何, inset 是行业标准缓解
(alacritty / xterm / foot 同套路). **修法不动 CELL_W_PX (跨 render/window/term
大改, Phase 6+ 单独 ticket 如需) 是正解** — 当前 ticket scope 严守, 不 gold-plate.

**视觉验收三源齐**: writer commit body 自报 + Lead Read /tmp/t0604_test.png 第 2
源 + reviewer 第 3 源 Read /tmp/t0604_test.png + 独立产 /tmp/t0604_reviewer.png
Read, 全一致 (整片 #0a1030 深蓝 + 浅灰 prompt 直接画在深蓝上, 无每字黑底, 无 CJK
黑空隙). T-0405 三源 PNG SOP "任一不一致即 P0" — **三源全一致** ✅. cursor inset
视觉验证由 8 lib 单测 (NDC 数学锁) + 6 集成测试 (像素阈值锁) 双层覆盖, 不需独立
cursor PNG.

**5 偏离全主动告知 + 论据合理 + accept**, conventions §5 "派单 vs 实装对照, 不
静默偏离" 命中. 实际只 2 项是真折中 (CELL_BG_DEFAULT 私有 const + Bg source
限定), 其余 3 项是派单字面已允许的非偏离 (writer 谨慎披露). 主动告知 hygiene
优秀.

**P2 跟进 2 项 + P3 跟进 1 项**: P2-1 双源真相 CELL_BG_DEFAULT (Phase 6+ 安全
refactor src/term pub) / P2-2 cursor inset 真窗口视觉感受 (user 实测后调常数即可) /
P3-1 MaybeRenderer body 复制漂移 (Phase 6+ 提取静态 fn). 全不阻塞.

**Lead 决策**: ✅ **批准合并** (3 commits 保留 / Option C squash 单 commit, Lead 选).
