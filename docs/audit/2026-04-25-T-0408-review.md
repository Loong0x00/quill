# 审码报告: T-0408 Headless Screenshot (offscreen render → PNG, agent 自验视觉)

**审码人**: reviewer-T0408 (per-ticket fresh agent)
**日期**: 2026-04-25
**范围**: feat/T-0408 分支, 2 commits
- `86d9228` 主体: headless screenshot (offscreen render → PNG, agent 自验视觉)
- `a952d75` Status claimed → in-review

**diff stat** (independently verified `git -C /home/user/quill-impl-headless diff $(git merge-base main HEAD)..HEAD --stat`, **fork-point d98795f** = T-0407 合并后 / T-0404 合并前):

- Cargo.lock +99 (image / png / flate2 / miniz_oxide / 等 7 transitive crate, ADR 0005 sanctioned)
- Cargo.toml +4 (`image = { version = "0.25", default-features = false, features = ["png"] }`)
- docs/adr/0005-image-crate-png-encoding.md +138 (新文件, Status / Context / Decision / Alternatives / Consequences 五段齐)
- src/main.rs +203/-? (parse_headless_screenshot_arg + run_headless_screenshot + 5 unit tests)
- src/wl/mod.rs +6 (re-export `render_headless`)
- src/wl/render.rs +654 (render_headless free fn + create_headless_cell_pipeline + create_headless_glyph_pipeline + color_for_vertex_with_srgb)
- tasks/T-0408-headless-screenshot.md +1/-1 (Status)
- tests/headless_screenshot.rs +214 (新文件, 3 集成测试 + render_headless_with_shell_prompt helper)

合计 8 文件 +1318/-2, writer 自报 "7 文件 +965" 实际 8 文件 (+Cargo.lock 漏数, 派单 Acceptance 列 ADR 0005 必有 Cargo.lock 增长) +1318 (writer 漏算 +Cargo.lock 99 + 注释行 ≈ 350 行差异), 实测 diff 比自报多 — Cargo.lock 是 Cargo 自动维护文件, +99 在 ADR 0005 已 sanction (Consequences "Cargo.lock 新增 ~7 transitive crate"), **不是隐瞒**, 仅 self-report 数字粗。

**判定**: ⚠️ **条件批准合并 — P0-NEW rebase 阻塞**

T-0408 实装本身**全部 P0 通过** (功能 + 视觉 + 四门 + INV / 硬约束 / 测试覆盖), **但分支 fork-point 已落后于 main**: T-0408 fork 自 d98795f (T-0407 合并后), 复审期间 T-0404 (HiDPI 2x) 已合 main (5ee427b 当前 HEAD)。`git merge-tree main HEAD` 实测 `src/wl/mod.rs` 真冲突 + `src/wl/render.rs` 自动合并但**语义破** (T-0404 加 `HIDPI_SCALE: u32 = 2` 让 cell 渲染走 physical px = logical × HIDPI_SCALE, render_headless 仍用 logical CELL_W_PX / CELL_H_PX 未跟进, 视觉将变 cell 偏移 / glyph 字号双倍但 cell 尺寸不变 → 字形溢出 cell)。

**Lead 决策路径** (二选一):
1. **rebase 路径** (推): Lead 让 writer-T0408 (或 fresh writer) rebase feat/T-0408 onto main (= 当前 5ee427b), 解 src/wl/mod.rs 2 行 `pub use` merge + 在 render_headless 内引 `HIDPI_SCALE` 让 cell px / surface size / BASELINE_Y_PX 跟 T-0404 同步, 重跑 4 门 + 视觉 PNG verify, 然后 re-审 (我可继续接 round 2)。
2. **Lead 手 squash-merge 路径**: Lead 自己做 conflict resolution + HiDPI 适配 (Option C squash), 再合; 风险: 改非-trivial 代码后未走 reviewer 审计链。

**T-0408 本身 (fork-point d98795f baseline) 已是合格交付, 视觉 PNG verify 通过 (Lead 之前 Read /tmp/quill_t0408.png 真显 prompt + 我 Read /tmp/reviewer_t0408.png 同视觉), 仅因 T-0404 后到合 main 触发被动过期。审码不强求 T-0408 单方面承担 T-0404 之后的合并代价 — Lead 决定。**

---

## P0: 11 项 + 1 NEW

### [P0-NEW] T-0404 后到合 main, T-0408 fork-point 落后 ⚠️ Lead 决策

**事实链**:
- feat/T-0408 实际 fork-point = `d98795f` (T-0407 Lead 跟进 commit, T-0407 合并后 / T-0404 合并前)
- 复审期间 main 演进:
  - `808b8f5` Merge T-0404: HiDPI 2x scale hardcode
  - `5ee427b` T-0404 Lead 跟进: audit 落盘 (current main HEAD)
- `git merge-base feat/T-0408 main` = `d98795f` (旧 fork-point, T-0408 未 rebase)
- `git merge-base --is-ancestor 289b409 HEAD` = ✅ T-0407 已在 T-0408 ancestry (writer 自报对 — fork 自 T-0407 合并后, 不是 "前")

**冲突实测** (`git merge-tree main HEAD`):

```
自动合并 src/wl/mod.rs
冲突（内容）：合并冲突于 src/wl/mod.rs
自动合并 src/wl/render.rs
```

**冲突详情**:

#### A. src/wl/mod.rs (真 conflict)
- T-0404 加 `pub use render::HIDPI_SCALE;` 在文件尾
- T-0408 加 `pub use render::render_headless;` 在文件尾
- 解法: 两条 `pub use` 都保留, 注释保留 (各自含 why), 顺序 HIDPI_SCALE 在前 (T-0404 早合)

#### B. src/wl/render.rs (auto-merge 但**语义破**)
- T-0404 加 `HIDPI_SCALE: u32 = 2` + cell 渲染路径 (Renderer::draw_frame / build_vertex_bytes) 用 cell_px × HIDPI_SCALE 算 NDC
- T-0408 `render_headless` 直接用 `CELL_W_PX = 10.0` / `CELL_H_PX = 25.0` / `BASELINE_Y_PX = 18.0` (logical px), **未跟进 × HIDPI_SCALE**
- 后果: rebase 后 render_headless 的 cell 顶点 NDC 仍是 logical 像素 (10 × 25), 但 shape_line 已被 T-0404 改为 `Metrics(17.0 × HIDPI_SCALE = 34.0, ...)` (`src/text/mod.rs:419`) → glyph raster 物理 ~14×17 px → cell (10×25) 容不下 glyph (字形溢出 / 错位)
- shape_line 已强制 HiDPI raster, render_headless cell 几何与 surface 800×600 logical 也未对齐 (T-0404 后 surface logical → physical × 2)
- 解法: render_headless 内引 `HIDPI_SCALE` 让 width/height (logical → physical × 2) / cell_w_px / cell_h_px / BASELINE_Y_PX 全 × HIDPI_SCALE; 或 render_headless 入参改 logical 尺寸 + 内部 × HIDPI_SCALE; 或新加 `--headless-size=WxH` flag 让调用方明示 logical/physical (T-0404 派单已写入 future work)

**重跑视觉 verify 必需**: rebase + HIDPI_SCALE 适配后, 再 `cargo run --release -- --headless-screenshot=/tmp/x.png` + Read PNG 看 prompt 字符仍对齐 cell + 不溢出。

**为啥 P0 而非 P1**: rebase 后视觉断裂 → T-0408 的 deliverable (agent 自验视觉) 在 main 上**不工作**, 直接破坏 ticket goal。

### [P0-1] INV-010 类型隔离 strict reading 第 11 次应用 ✅✅

**5 条独立 grep 复跑** (沿袭 T-0399 + T-0401..T-0407 验证 SOP):

```
$ cd /home/user/quill-impl-headless

$ grep -rnE 'pub use (alacritty|wgpu|wayland|cosmic)' src/
(零命中) ✅

$ grep -rnE 'impl From<(alacritty|wgpu|wayland|cosmic)' src/
(零命中) ✅

$ grep -rnE 'pub.*cosmic_text::' src/
src/text/mod.rs:531:    /// **不 pub** (INV-010 strict reading 第 9 次应用): cosmic-text [`cosmic_text::CacheKey`]
   ↑ 1 命中, doc comment 内 "**不 pub**" 反例陈述 (T-0403 + T-0407 audit 假阳同源)

$ grep -rnE 'pub\(crate\).*cosmic_text::' src/
(零命中) ✅

$ grep -rnE 'cosmic_text' src/ | grep -v 'src/text/mod.rs'
(零命中) ✅
```

**T-0408 新加类型边界 verify**:

`pub fn render_headless(text_system: &mut TextSystem, cells: &[CellRef], cols: usize, rows: usize, row_texts: &[String], width: u32, height: u32) -> Result<Vec<u8>>`
- `&mut TextSystem` ✅ quill 自定义
- `&[CellRef]` ✅ quill 自定义 (T-0305)
- `cols: usize, rows: usize` ✅ std
- `&[String]` ✅ std
- `width / height: u32` ✅ std
- 返 `Result<Vec<u8>>` (anyhow::Result, RGBA8 字节流) ✅ std + anyhow

**模块私有 fn (free fn 在 render.rs)**:
- `fn create_headless_cell_pipeline(device: &wgpu::Device, format: wgpu::TextureFormat) -> wgpu::RenderPipeline` — 模块私有 (无 `pub`), wgpu 类型仅本模块内 ✅
- `fn create_headless_glyph_pipeline(device: &wgpu::Device, bgl: &wgpu::BindGroupLayout, format: wgpu::TextureFormat) -> wgpu::RenderPipeline` — 模块私有 ✅
- `fn color_for_vertex_with_srgb(c: crate::term::Color, is_srgb: bool) -> [f32; 3]` — 模块私有 ✅

**src/main.rs 调用边界**:
- 只引 `quill::pty::PtyHandle` / `quill::term::TermState` / `quill::text::TextSystem` / `quill::wl::render_headless` (全 quill 公共 API, 无 wgpu / cosmic-text 漏出)
- `image::codecs::png::PngEncoder` / `image::ExtendedColorType` / `image::ImageEncoder` 仅在 main.rs 用, 不出 lib API (派单 In #C ADR 0005 sanctioned)

**tests/headless_screenshot.rs 边界**:
- 只引 `quill::pty::PtyHandle` / `quill::term::TermState` / `quill::text::TextSystem` / `quill::wl::render_headless` + image crate (test-only, 派单 In #D sanctioned)

**INV-010 第 11 次应用** ✅ (T-0302..T-0307 + T-0399 + T-0401..T-0403 + T-0407 + **T-0408 render_headless free fn + headless pipeline 私有 fn**)

### [P0-2] PNG 视觉 verify (本 ticket 自反 deliverable) ✅✅

**独立跑** `cd /home/user/quill-impl-headless && cargo run --release -- --headless-screenshot=/tmp/reviewer_t0408.png`:

```
INFO quill 进入 headless screenshot 模式 (T-0408) path=/tmp/reviewer_t0408.png
INFO quill::text TextSystem::new: locked PREFERRED monospace face primary_face=DejaVu Sans Mono
INFO quill::text TextSystem::new: emoji face blacklist populated emoji_face_count=1
INFO quill headless PTY drain 完成 read_bytes=258
INFO quill::wl::render wgpu adapter (headless) 选中 backend=Vulkan name=NVIDIA GeForce RTX 5090
DEBUG quill::wl::render render_headless stats cols=80 rows=24 width=800 height=600
       cell_vertex_count=90 glyph_vertex_count=90 atlas_count=12
INFO quill headless screenshot 写出 PNG 完成 path=/tmp/reviewer_t0408.png width=800 height=600 bytes=1920000
```

**PNG header verify** (`xxd /tmp/reviewer_t0408.png | head -1`):
```
00000000: 8950 4e47 0d0a 1a0a 0000 000d 4948 4452  .PNG........IHDR
00000010: 0000 0320 0000 0258 0806 0000 009a 7682  ... ...X......v.
```
- `89 50 4e 47 0d 0a 1a 0a` = PNG magic ✅
- `00 00 03 20` width = 0x320 = 800 ✅
- `00 00 02 58` height = 0x258 = 600 ✅
- `08 06` bit depth 8 + color type 6 (RGBA) ✅

**Read /tmp/reviewer_t0408.png** (Claude multimodal 看 PNG):
- 浅灰 `[user@userPC ~]$` 在左上 (logic px ~0..170 × ~0..25 区域) ✅
- 反色块 cursor 在 `~]` 与 `$` 之间 (空格位置, 文本输入入光标) ✅
- 大块深蓝 #0a1030 背景 (其余 cell 区域) ✅
- 字体: 等宽 (DejaVu Sans Mono primary_face_id 锁定 T-0407)
- 视觉与 Lead 此前 Read /tmp/quill_t0408.png 一致 ✅

**写码 trace 数学**: cell_vertex_count=90 (15 字符 × 6 顶点) + glyph_vertex_count=90 (15 glyph × 6) + atlas_count=12 (15 字符去重 — `[user@PC]$` 共 12 unique char + space 不 raster) — 与 T-0407 P0-13 trace 同源 ✅。

**T-0408 自反 deliverable 物理验证 PASS** ✅。

### [P0-3] render_headless 完全独立于 wgpu Surface ✅✅

**Lead 重点 verify 项**: render_headless 真不依赖 wgpu Surface (即不需要 Wayland)?

**独立 grep** `grep -n "Surface\b" src/wl/render.rs`:
```
4 //! Wayland 客户端封装 ... (doc comment)
8 - Instance / Surface / Device / Queue 四件套全部打包进 [Renderer], (doc comment)
13 - SCTK 0.19 不实现 raw_window_handle::HasDisplayHandle ... 裸指针构造 handle, via SurfaceTargetUnsafe::RawHandle (doc comment)
78 surface: wgpu::Surface<'static>, ← Renderer struct 字段 (T-0102 起手)
537 错误处理: Surface acquire 的 Outdated/Lost/Timeout/Occluded ... (Renderer::draw_cells doc)
858 之后调 draw_glyphs", 但 wgpu Surface 的 get_current_texture ... (Renderer::draw_frame doc)
1428 Renderer 持 [wgpu::Surface] (Wayland-bound), 离屏路径不需 (render_headless doc, 反例陈述)
1429 Surface, 也不能挂 Renderer 字段 ... (render_headless doc, 反例陈述)
1440 1. wgpu Instance/Adapter/Device/Queue (无 Surface, headless 路径) (render_headless step doc)
```

**关键 verify**: render_headless 函数体内 (line 1464-1909) **零** `Surface` 字面 — `instance.request_adapter(&wgpu::RequestAdapterOptions { ..., compatible_surface: None, ... })` 显式 `None` (line 1485), 离屏 Texture (line 1507) 是 `wgpu::Texture` 而非 `Surface`. ✅

**Wayland 物理独立性**: writer 自报 "headless 路径**不接 Wayland**" — render_headless 不持任何 wl_display / wl_surface 裸指针, INV-001 / INV-003 (State / Renderer::new 字段顺序 + unsafe 合同) 不适用本 fn. ✅

**Acceptance "render_headless API 实装 + 不依赖 wgpu Surface" 通过** ✅

### [P0-4] bytes_per_row 256 对齐 ✅✅

**Lead 重点 verify 项**: 800×4=3200 已对齐, 1024×4=4096 已对齐, div_ceil 算法对吗?

**实装** `src/wl/render.rs:1844-1848`:
```rust
let unpadded_bytes_per_row = width
    .checked_mul(4)
    .ok_or_else(|| anyhow!("width × 4 溢出 u32 (width = {})", width))?;
let align = wgpu::COPY_BYTES_PER_ROW_ALIGNMENT;
let padded_bytes_per_row = unpadded_bytes_per_row.div_ceil(align) * align;
```

**算法 verify**:
- `wgpu::COPY_BYTES_PER_ROW_ALIGNMENT = 256` (wgpu 0.29 const)
- `u32::div_ceil(self, rhs)` = `(self + rhs - 1) / rhs` (Rust std, 1.73+ 稳定 — Rust 1.85 user 满足)
- 边界 case 验证 (writer 自报 + 我独立):
  - width=800: 3200 div_ceil 256 = 13, 13 × 256 = 3328 ❌ wait — 3200 / 256 = 12.5, div_ceil = 13, 13 × 256 = 3328?? 但 3200 已 256 对齐 (3200 / 256 = 12.5)
  - **重新算**: 3200 / 256 = 12.5 (不整除), div_ceil = 13, 13 × 256 = 3328 — **800×4=3200 不是 256 对齐!**
  - writer 自报 "800×4=3200 已对齐" **数学错误** — 3200 mod 256 = 3200 - 12×256 = 3200 - 3072 = 128, 不为 0
  - 实装 `div_ceil` 仍**正确处理**: padded = 3328, 实际 readback 行 stride = 3328, output Vec 取 unpadded 3200 byte/row × 600 行 = 1920000 byte (与 INFO trace `bytes=1920000` 吻合) ✅
  - 1024: 1024×4=4096; 4096/256=16, div_ceil=16, padded=4096 ✅ (1024 真 256 对齐)
  - 1366: 1366×4=5464; 5464/256=21.34, div_ceil=22, padded=5632 ✅ (writer 自报对)

**实装数学正确** ✅, 但 writer "800×4 已对齐" 自报陈述错 (实际 padded=3328, 写 unpadded 3200) — **不影响功能** (div_ceil 容错), 注释 line 1842 `padded = 5632` 例子对, 800 例子省略避 confusion. **接受**.

**INFO trace `bytes=1920000`** verify: 800 × 600 × 4 = 1920000 = output Vec<u8> 长度, 正是 unpadded RGBA8 ✅.

### [P0-5] map_async + Device::poll(Wait) 阻塞等 (INV-005 影响 verify) ✅✅

**Lead 重点 verify 项**: 不在 calloop callback 内调? 不影响 INV-005?

**实装** `src/wl/render.rs:1884-1894`:
```rust
let slice = readback_buf.slice(..);
let (sender, receiver) = std::sync::mpsc::channel();
slice.map_async(wgpu::MapMode::Read, move |result| {
    let _ = sender.send(result);
});
device
    .poll(wgpu::PollType::wait_indefinitely())
    .map_err(|e| anyhow!("device poll wait 失败: {e:?}"))?;
receiver
    .recv()
    .context("readback receiver recv 失败 (sender 已 drop?)")?
    .map_err(|e| anyhow!("buffer map_async 失败: {e:?}"))?;
```

**调用路径 verify**:
- `render_headless` 由 `src/main.rs::run_headless_screenshot` 调
- `run_headless_screenshot` 由 `main()` 在 `headless_path.is_some()` 分支直接调 (line 25-28), **不入 calloop 路径** (`quill::wl::run_window()` 走 calloop, 但 main.rs return 之前未触)
- `tests/headless_screenshot.rs::render_headless_with_shell_prompt` 由 `#[test]` fn 调, **不入 calloop 路径**

**INV-005 影响**: 零 — INV-005 约束 "calloop 单线程禁阻塞", render_headless 不挂 calloop EventLoop, `device.poll(wait_indefinitely)` + `receiver.recv()` 阻塞合规. writer 注释 line 1879-1881 显式说明 ✅.

**派单 Acceptance "headless 路径允许阻塞 (跟 INV-005 不冲突, 不接 calloop)" 通过** ✅

### [P0-6] CLI flag 解析 5 unit test 覆盖 ✅✅

**实装** `src/main.rs::parse_headless_screenshot_arg` (line 42-63):

```rust
const PREFIX: &str = "--headless-screenshot=";
for arg in args.iter().skip(1) {
    if let Some(rest) = arg.strip_prefix(PREFIX) {
        if rest.is_empty() {
            return Err(anyhow!("--headless-screenshot= 后路径不能为空 ..."));
        }
        return Ok(Some(PathBuf::from(rest)));
    }
    if arg == "--headless-screenshot" {
        return Err(anyhow!("--headless-screenshot 必须 = 形式 ..."));
    }
}
Ok(None)
```

**5 unit test verify** (line 174-218):
1. `parse_no_arg_returns_none` — `["quill"]` → Ok(None) ✅
2. `parse_screenshot_path_found` — `["quill", "--headless-screenshot=/tmp/foo.png"]` → Ok(Some(PathBuf)) ✅
3. `parse_screenshot_empty_path_errs` — `["quill", "--headless-screenshot="]` → Err ✅
4. `parse_screenshot_space_separator_errs` — `["quill", "--headless-screenshot", "/tmp/foo.png"]` → Err ✅
5. `parse_unrelated_flags_ignored` — `["quill", "--unrelated", "--other=value"]` → Ok(None) ✅

**5 测试覆盖 5 形态** (no_arg / valid / empty / space-sep / unrelated): exhaustive 正常路径 + 错误路径. ✅

**P2 观察**: 形式锁死 `--foo=bar` 而非 `--foo bar` — P2-3 详述, 不阻塞.

### [P0-7] ADR 0005 格式合规 (沿袭 ADR 0001-0004) ✅✅

**实装** `docs/adr/0005-image-crate-png-encoding.md` (138 行):

**段落 verify**:
- ✅ Title: `# ADR 0005: 引 image crate 作 PNG 编码 (T-0408 headless screenshot)`
- ✅ Status: Accepted, 2026-04-25
- ✅ Context (8-26 行) 真因 trigger + 两条路对照 + CLAUDE.md 触发条件
- ✅ Decision (28-44 行) image = "0.25" + 调用接口固定示例 + format 锁定
- ✅ Alternatives (46-86 行) 4 个备选 (Alt 1 自写 / Alt 2 png crate / Alt 3 PPM / Alt 4 imagemagick), 每个有 reject 主因
- ✅ Consequences (88-119 行) 正面 + 负面 / 代价 + 已知残留三段
- ✅ 实装验证 (122-129 行) 走 4 门 + cargo run + 3 集成测试 acceptance
- ✅ 相关文档 (131-138 行) 派单 / 主体实装路径 / 集成测试 / 相关 ADR (0002 技术栈锁说明本 ADR 不进锁清单)

**ADR 0001-0004 对比** (`ls docs/adr/`):
- `0001-language-rust.md` Status / Context / Decision / Consequences 四段
- `0002-stack-lock.md` 同四段 + 锁清单
- `0003-signal-hook.md` (被 0004 supersede)
- `0004-calloop-unified-loop.md` 同四段
- ADR 0005 沿袭格式 + 加 `Alternatives` 段 (4 备选评估) + `实装验证` 段, **优于** 0001-0004 的"决策 + 后果"二段范式 ✅

**ADR 0005 是 quill 第一个引新 dep 的 ADR, 沿袭性 + 创新性兼具** ✅

### [P0-8] INV / 硬约束 ✅

**独立 grep verify**:
```
$ git -C /home/user/quill-impl-headless diff main..HEAD -- docs/invariants.md src/pty src/term src/wl/window.rs
(零输出) ✅
```

**INV-001..010 兼容**:
- INV-001 State 字段顺序未动 ✅ (T-0408 不改 src/wl/window.rs)
- INV-002 Renderer 14 字段顺序未动 ✅ (`grep -nE "^pub struct Renderer" src/wl/render.rs:62` + 14 字段链 + `# instance` 末位 verify; render_headless 是 free fn 不挂 Renderer 字段)
- INV-002 GlyphAtlas 内部字段顺序未动 ✅ (T-0408 不改 GlyphAtlas struct, render_headless 用 local 变量 atlas_cursor_x / atlas_cursor_y / atlas_row_height + HashMap 而非复用 GlyphAtlas struct — 派单 In #A "render_headless 完全独立于 Renderer struct" 落地)
- INV-003 unsafe fn Renderer::new 合同未动 ✅ (T-0408 零新 unsafe)
- INV-004 forbid → deny unsafe_code 未动 ✅
- INV-005 calloop 单调度器 ✅ (P0-5 verify, render_headless 不挂 calloop)
- INV-006 resize_dirty 消费契约 ✅ (T-0408 不改 propagate_resize_if_dirty)
- INV-007 WindowCore state machine 纯逻辑 ✅ (T-0408 不改 WindowCore)
- INV-008 PtyHandle drop 顺序 ✅ (T-0408 走 PtyHandle::spawn_shell + read, 不改字段)
- INV-009 PTY master fd O_NONBLOCK ✅ (T-0408 走 read 路径, WouldBlock 当退出条件 main.rs:120)
- INV-010 类型隔离第 11 次应用 ✅ (P0-1 严格 verify)

**零新 ADR 之外的依赖** ✅ (Cargo.toml 仅 +image crate, ADR 0005 sanctioned)
**零合并标记** (`grep -nE '<{7}|={7}|>{7}' src/` 零命中, fmt --check 通过) ✅

### [P0-9] 四门 ✅

```
$ cd /home/user/quill-impl-headless

$ cargo build
Finished `dev` profile [optimized + debuginfo] target(s) in 0.04s ✅

$ cargo build --release
Finished `release` profile [optimized] target(s) in 0.06s ✅

$ cargo clippy --all-targets -- -D warnings
Finished `dev` profile [optimized + debuginfo] target(s) in 0.05s ✅ (零 warning)

$ cargo fmt --check
fmt OK (零输出) ✅

$ cargo test
running 79 tests (lib unit) → ok
running 5 tests (main.rs unit, T-0408 新 — parse_no_arg / valid / empty / space / unrelated) → ok
running 2 tests (frame_stats_integration) → ok
running 3 tests (frame_stats_smoke) → ok
running 2 tests (glyph_atlas) → ok
running 3 tests (headless_screenshot, T-0408 新) → ok
   - headless_renders_prompt_to_png ✅
   - headless_png_pixels_have_glyph_on_dark_bg ✅
   - headless_png_no_emoji_color_artifact ✅
running 2 tests (ls_la_e2e) → ok
running 2 tests (pty_calloop_smoke) → ok
running 2 tests (pty_echo) → ok
running 3 tests (pty_resize) → ok
running 2 tests (pty_to_term) → ok
running 2 tests (resize_chain) → ok
running 11 tests (state_machine) → ok
running 0 tests (lib doc) → ok
TOTAL: 79 + 5 + 2 + 3 + 2 + 3 + 2 + 2 + 2 + 3 + 2 + 2 + 11 = 118 tests pass ✅
```

**writer 自报 113 tests 实测 118** (writer 漏算 main.rs 5 unit tests, 派单 Acceptance "总测试 110 + 3 ≈ 113" 仅算集成测试, 不算 main.rs unit) — **数字粗 但实装多 5 unit tests 覆盖更稳**, 接受 ✅.

**四门全绿** ✅.

### [P0-10] 视觉 acceptance (派单 Acceptance "PNG visual 看着跟 user 截图 2026-04-25 18-47-00 一致") ✅

**独立 PNG verify** (P0-2 详述): /tmp/reviewer_t0408.png Read 视觉 = 浅灰 `[user@userPC ~]$` + 反色块 cursor + 深蓝背景 + 黑 cell.bg(几乎不可见).

**与 user 2026-04-25 18-47-00 截图对比** (Lead 此前 Read /tmp/quill_t0408.png 已 verify 真显此): 我看 /tmp/reviewer_t0408.png 与之**视觉一致**. ✅

**T-0408 自反 deliverable 物理 PASS**: agent 跑 cargo run + Read PNG 真**自验视觉**, 不依赖 user 手测截图. ✅

### [P0-11] 测试 self-cover 真 IO + GPU 路径 ✅

**Lead 隐性 verify 项**: 3 集成测试是真跑 wgpu offscreen + PTY spawn 链路, 不是 mock 假信号?

**独立 verify** `tests/headless_screenshot.rs::render_headless_with_shell_prompt` (line 36-70):
- `TextSystem::new()` — 真 fontdb scan + face lock (T-0407 路径)
- `PtyHandle::spawn_shell(80, 24)` — 真 fork bash 子进程 (INV-008 + INV-009 路径)
- `std::thread::sleep(500ms)` — 等 prompt (派单 In #B 同源)
- `pty.read()` 循环 + `term.advance()` — 真 PTY drain + alacritty_terminal advance
- `render_headless(&mut text_system, &cells, ...)` — 真 wgpu offscreen + glyph shape + raster + atlas

**真 GPU 验证** (P0-2 trace): cargo run 起到 `wgpu adapter (headless) 选中 backend=Vulkan name=NVIDIA GeForce RTX 5090` — 走真 RTX 5090 Vulkan, 不是 software fallback. tests/headless_screenshot.rs 也走同栈 (3 测试 finished in 0.99s 暗示真 GPU 跑出 3 帧 PNG render). ✅

**零 mock 链路, 真 IO + GPU end-to-end 测试** ✅.

---

## P1: 无违规

### 生产零 unwrap / expect / println / unsafe / thread::spawn ✅

**独立 grep**:
```
$ grep -nE 'unwrap\(\)|\.expect\(|println!|thread::spawn|unsafe' src/wl/render.rs src/main.rs src/wl/mod.rs
src/main.rs:1: ... wgpu FFI / wayland-scanner ... unsafe ... (注释)
src/main.rs:3: ... 加 #[allow(unsafe_code)] + // SAFETY: 才通过 (注释)
src/main.rs:4: #![deny(unsafe_code)]
src/main.rs:177: .expect("no arg should be Ok(None)") (#[cfg(test)] mod tests)
src/main.rs:188: .expect("with valid flag should parse") (#[cfg(test)] mod tests)
src/main.rs:189: .expect("should be Some") (#[cfg(test)] mod tests)
src/main.rs:216: .expect("unrelated flags should not error") (#[cfg(test)] mod tests)
src/wl/render.rs:324: #[allow(unsafe_code)] (T-0102 起手 Renderer::new, 不动)
src/wl/render.rs:325: pub unsafe fn new ... (T-0102 起手, 不动)
src/wl/render.rs:343: #[allow(unsafe_code)] (T-0102 起手 unsafe block, 不动)
src/wl/render.rs:344: let surface = unsafe { ... (T-0102 起手, 不动)
```

**T-0408 改动零新 unsafe** ✅, expect 仅 #[cfg(test)] mod tests 内 (CLAUDE.md "禁用 unwrap/expect" 仅约束 src/, conventions §3 "test 路径允许 panic / expect" sanction).

**Result 处理用 `?` / `.context()` / `.map_err(|e| anyhow!())`**:
- main.rs `headless_path = parse_headless_screenshot_arg(&args)?;` (line 22)
- main.rs `run_headless_screenshot(&path)?;` (line 26)
- main.rs `quill::wl::run_window()?;` (line 32)
- run_headless_screenshot 内 `TextSystem::new().context(...)?` (line 99-100), `PtyHandle::spawn_shell(...).context(...)?` (line 103-104), `render_headless(...).context(...)?` (line 134-143), `File::create(path).with_context(...)?` (line 151-152), `encoder.write_image(...).with_context(...)?` (line 154-156), `file.flush().with_context(...)?` (line 157-158)
- render_headless `instance.request_adapter(...).map_err(|e| anyhow!(...))?` (line 1487), `adapter.request_device(...).context(...)?` (line 1500), `width.checked_mul(4).ok_or_else(|| anyhow!(...))?` (line 1846), `device.poll(wait_indefinitely()).map_err(|e| anyhow!(...))?` (line 1888-1890), `receiver.recv().context(...)?.map_err(|e| anyhow!(...))?` (line 1891-1894)

**全部 ? / context / anyhow** ✅, 零 production unwrap.

**println 零** (`grep -n "println!" src/`) ✅, 调用 `tracing::info!` / `tracing::debug!` / `tracing::warn!` (CLAUDE.md 约束).

**std::thread::sleep** 在 main.rs:107 + tests/headless_screenshot.rs:43 — **派单 In #B 显式 sanction "headless 路径允许阻塞"**, 不挂 calloop, INV-005 不冲突 ✅.

**0 thread::spawn** ✅ (`grep -n "thread::spawn"` 零命中).

### 注释 why 非 what ✅

- `parse_headless_screenshot_arg` doc (main.rs:37-41) 引派单硬约束 + 单形式简化逻辑 + 防 shell 误传 (why)
- `run_headless_screenshot` doc (main.rs:65-82) 引派单 In #B 800×600 + 80×24 grid + 500ms prompt + INV-005 阻塞合规 (why, 4 段)
- `render_headless` doc (render.rs:1417-1463) 真因 trigger (T-0403 字形 bug 一周 3 次诊断错位) + 完全独立于 Renderer struct (why) + 流程 9 步 + PNG encoding 在调用方 + 错误处理 + 集成测试位置 — **42 行 doc, 引 T-0407 D fix + INV-005 + ADR 0005**
- `color_for_vertex_with_srgb` doc (render.rs:1912-1916) 引 Renderer::color_for_vertex 等价但 free fn (why), `is_srgb` 路径 (sRGB pre-compensation)
- `create_headless_cell_pipeline` doc (render.rs:1932-1933) 引 Renderer::ensure_cell_pipeline 同骨架 + format 入参解释 (why)
- `create_headless_glyph_pipeline` doc (render.rs:1998-2000) 引 Renderer::ensure_glyph_pipeline 同骨架 + bgl 入参 (why)
- 内联注释引 T-0407 D fix 真因 (line 1619-1621) / cosmic-text Mask 路径 / atlas shelf packing 算法 / bytes_per_row 256 对齐 / map_async + Device::poll(Wait) INV-005 路径

**注释 traceability 完整**, conventions §2 "B. 引 audit / ticket / ADR / INV 编号" 全程命中 ✅.

### Commit message 引 ADR + 派单 + INV ✅

`86d9228` (主体) commit body 4 段齐 (我跑 `git -C /home/user/quill-impl-headless show --stat 86d9228` 验):
- 标题 ≤ 70 字符: `T-0408: headless screenshot (offscreen render → PNG, agent 自验视觉)` ✅
- HEREDOC 格式无特殊符号撞 ✅

`a952d75` Status commit 单文件 ±1, 符合 conventions §4 Step 4 "Status 变更**独立 commit**" 模板 ✅.

---

## P2: API 设计 / 架构判断

### [P2-1] render_headless 独立 free fn vs Renderer::render_headless method ✅ writer 选 free fn

**writer 选**: `pub fn render_headless(text_system, cells, ..., width, height) -> Result<Vec<u8>>` 模块顶级 free fn, 完全独立于 Renderer struct.

**论据** (writer doc render.rs:1427-1437):
1. `Renderer` 持 `wgpu::Surface` (Wayland-bound), 离屏路径不需 Surface
2. 不能挂 `Renderer` 字段 (否则 INV-002 14 字段链需扩展, 字段顺序破)
3. 函数返回时全部 (Instance/Adapter/Device/Queue + offscreen Texture + 自有 atlas/pipeline) drop, 不污染 Renderer

**替代评估**:

| 路径 | 优 | 劣 |
|---|---|---|
| **A. Free fn (writer 选)** | INV-002 字段顺序零破; Renderer 责任纯 (Wayland surface render); future swap test impl 容易 (free fn 可替换) | 与 Renderer::draw_frame 代码重复 (cell pass + glyph pass + WGSL + atlas shelf packing); 维护两套 |
| B. `Renderer::render_headless(&mut self)` method | 复用 Renderer 内部 ensure_* 资源 | INV-002 字段需扩展 (新 offscreen Texture / pipeline / atlas slot); Renderer 责任不纯 |
| C. `impl Renderer { fn render_to_target(&mut self, target: TextureView) }` 抽象 | 既复用又不破 INV-002 | 需要 trait 抽象 surface vs offscreen 双源, Phase 4 复杂度过 |

**独立判**: ✅ **接受 free fn** — INV-002 字段顺序是硬不变式 (字段加错位 → drop 序错 → wgpu UB), 接受代码重复换字段顺序稳定. writer doc 解释清晰. **未来 Renderer 大重构** (Phase 5+ 多 surface / 多 target) 可重构 free fn → impl, 当前不超前.

### [P2-2] 不复用 Renderer::ensure_cell_pipeline / ensure_glyph_atlas (派单 "抽 draw_frame 公共逻辑") ✅ writer 选 inline 接受

**派单 In #A 写**: "跟 draw_frame 同 pipeline (cell + glyph), 输出到 offscreen texture 而不是 surface"

**writer 选**: `create_headless_cell_pipeline` / `create_headless_glyph_pipeline` 是 module-private free fn, **WGSL shader (CELL_WGSL / GLYPH_WGSL) + 常量 (ATLAS_W / ATLAS_H / CELL_W_PX / VERTEX_BYTES) + helper fn (srgb_to_linear / clear_color_for) 复用**, **pipeline 创建 / atlas 创建 / 顶点生成 inline 不复用** Renderer struct ensure_* 方法.

**论据** (writer doc render.rs:1432-1437):
1. "派单'抽 draw_frame 公共逻辑'我读为指核心算法 (cell pass + glyph pass + WGSL shader + atlas shelf packing)"
2. "实装走'复用常量与 free fn ([CELL_WGSL] / [GLYPH_WGSL] / [ATLAS_W] / [CELL_W_PX] / [srgb_to_linear] / [clear_color_for]), pipeline / atlas / 顶点生成 inline'"
3. "防与 Renderer struct 耦合 + 防 T-0407 并行分支合并冲突 (T-0407 改 GlyphAtlas HashMap key 类型, 离屏路径走自有 local atlas state)"

**独立评估**:

- 复用 WGSL + 常量 + helper 是合理 DRY 边界 ✅ (改 WGSL 时两路径同步)
- pipeline 创建 inline 是合理 trade-off — Renderer::ensure_cell_pipeline 走 lazy 初始化挂 Renderer 字段, render_headless 不挂 Renderer (P2-1 判) 自然 inline; 重构成 pure factory fn `fn build_cell_pipeline(device: &wgpu::Device, format: wgpu::TextureFormat) -> wgpu::RenderPipeline` 是 future cleanup, 派单 budget 100k 不强求
- atlas state inline (`HashMap<_, AtlasSlot>` + `atlas_cursor_x/y/row_height`) 而非 GlyphAtlas struct: 防 T-0407 合并冲突 — 实测 T-0407 已合 main, 此考量过期, 但 future-proof 防 T-0405 / T-0406 LRU 改 GlyphAtlas struct 内部 fields 时 render_headless 不跟改, 仍合理 ✅
- 顶点生成 inline (`for cell in cells { ... cell_vertex_bytes.extend_from_slice ... }`) — Renderer::build_vertex_bytes 走 method 形式, render_headless 内 inline 复制 ~30 行: 接受 (与 draw_frame 同源 + T-0407 D fix 已 sync `cell.bg` 路径), **future cleanup**: 抽 free fn `fn cells_to_vertex_bytes(cells, cell_w/h_px, surface_w/h, color_source: CellColorSource, is_srgb: bool) -> Vec<u8>` 让两路径都调

**独立判**: ✅ **接受 inline** — 派单 budget 100k 已被 ADR 0005 + render_headless 654 行 + 5 unit + 3 集成测试吃掉, future 可拆 helper fn (P3-1). 当前 DRY 边界 (WGSL + 常量 + sRGB helper) 合理.

### [P2-3] CLI flag 形式锁 `--foo=bar` 不接受空格分隔 ✅ writer 选锁死接受

**writer 选**: `--headless-screenshot=<PATH>` 单形式, `--headless-screenshot <PATH>` 显式 Err.

**论据** (writer doc main.rs:37-41 + main.rs:54-60):
1. "派单硬约束: 手写 std::env::args 解析", 单形式简化逻辑
2. "防 shell 误传" — 用户 `--headless-screenshot` 漏 `=` 时显式 Err 而非吞过

**替代评估**:

| 路径 | 优 | 劣 |
|---|---|---|
| **A. 锁 = 形式 (writer 选)** | 解析极简 (strip_prefix); 错误时显式 Err 引导用户 (`必须 = 形式`); 5 unit test 覆盖完整 | 与 GNU getopt / clap 通用风格不一致 (`--foo bar` 也常见) |
| B. 同时接受 `=` 和 space-separated | 兼容 GNU 风格 | 解析 +5 行 (peek 下一个 arg, EOF 错误处理) + 测试 +3 case |

**独立判**: ✅ **接受锁 = 形式** — 派单显式约束 "不引 clap" + "手写 std::env::args", writer 选最简路径; quill 是 single-user daily driver (Lead = user + Claude Code), 不需 GNU 兼容, 显式 Err 是 best UX (静默吞反而 bug 难找). Phase 5+ 若加多 flag 再重构 (届时考虑引 clap, 另写 ADR).

### [P2-4] /tmp/quill_t0408.png 命名 vs 配置 ✅ 派单已 sanction

**实装**: 调用方传 `--headless-screenshot=<PATH>`, fn 内不 hardcode 路径 (派单 In #B sanction).

**Lead spawn message 提**: "/tmp/quill_t0408.png 命名固定还是允许配置?" — 我读为 P3 future 问题, 当前实装 already 允许配置 (`--headless-screenshot=<PATH>`).

**判**: ✅ **当前已 sanction 任意路径**, 测试 helper 用 `std::env::temp_dir().push(format!("quill_t0408_test_{}.png", std::process::id()))` 防并发测试同名冲突, 工程上正确 ✅.

### [P2-5] image crate features 最小化 (default-features = false, features = ["png"]) ✅✅

**实装** Cargo.toml:13:
```toml
image = { version = "0.25", default-features = false, features = ["png"] }
```

**评估**:
- `default-features = false` 关 jpg / webp / tiff / gif / etc. (image 0.25 默认启 5+ codec)
- 只留 `png` feature (PngEncoder + decode 路径)
- 实测 Cargo.lock +99 行 (~7 transitive crate: image / png / flate2 / miniz_oxide / num-traits / crc32fast / 等), 与 ADR 0005 Consequences "新增 ~7 transitive crate" 吻合
- 若启全 default features, 估 +200 行 lock + 15+ transitive crate (jpeg-decoder / image-webp / tiff / 等)

**最小化合规**, 沿袭 conventions "依赖最小集" 原则 ✅.

---

## P3: 建议 (不阻塞, 跟进)

### [P3-1] render_headless 内部 inline 顶点生成可抽 free fn helper

**当前**: render_headless 内 `for cell in cells { ... cell_vertex_bytes.extend_from_slice ... }` (line 1605-1638) ~33 行 inline; `for (row_idx, row_text) in row_texts.iter().take(effective_rows).enumerate() { ... text_system.shape_line + raster + atlas allocate + glyph vertex }` (line 1652-1771) ~120 行 inline.

**建议** (P3, 不阻塞): future cleanup 抽 free fn:
- `fn build_cell_vertex_bytes_for_target(cells, cell_w/h_px, surface_w/h, color_source: CellColorSource, is_srgb: bool) -> Vec<u8>` — Renderer::build_vertex_bytes 也调
- `fn build_glyph_vertex_bytes_inline(text_system, row_texts, rows, cell_h_px, surface_w/h, atlas_state: &mut LocalAtlasState, glyph_color: [f32;3]) -> Result<Vec<u8>>` — render_headless 调

**当前不做** because 派单 budget 100k 已耗 + DRY 边界 (WGSL + 常量 + sRGB helper) 已抽 + render_headless 主体 inline 反而 step-by-step 易读. T-0405 / T-0406 重构若需要再抽.

### [P3-2] no_emoji_color_artifact 测试阈值 5% (建议 4% 或更紧?) ⚠️ 接受 5%

**实装** tests/headless_screenshot.rs:206-213:
```rust
let other_ratio = (other_pixels as f64) / (total_pixels as f64);
// 5% 是"明显彩色 artifact"阈值。T-0407 face lock 后视觉稳定, 实测此值
// 应 < 1%; 留 5% 余量给字形 anti-alias 边界过渡像素 ...
assert!(other_ratio < 0.05, ...);
```

**Lead spawn message 问**: 5% 是否过宽? 4% 可疑像素也应触发警报?

**独立判**: ✅ **接受 5%** —
- 实测 T-0407 face lock 后 emoji 排除 + 视觉稳定, other_ratio < 1% (writer 自报)
- 留 5% 余量是 CI flaky 防御 (sRGB → linear 转换数值漂移可能让某些边界像素超出 ±12 / ±24 容差) — 合理工程裕度
- 4% 过紧风险: GPU driver 升级 / wgpu version bump 时数值微变 (sRGB encoding 算法浮点漂移) 可能触发 false positive
- 5% 仍能 catch "彩色 emoji 大块出现" (单 emoji raster 通常 ≥ 10 cell × 24×25 px ≈ 6000 pixel, 占 800×600 = 480000 的 1.25%, 多 emoji 累积超 5% 会触发)

**P3 不阻塞** — 5% 实测稳定, 若未来发现 false negative (彩色 emoji 漏报) 再收紧.

### [P3-3] writer self-report 数字粗 (113 vs 实测 118 / 7 文件 vs 实测 8 / "800×4 已对齐" 数学错)

**writer 自报 vs 实测对比**:
| 自报 | 实测 | 误差 | 评 |
|---|---|---|---|
| 7 文件 +965 | 8 文件 +1318 (-2) | +1 文件 (Cargo.lock 漏数) +353 行 | 接受 — Cargo.lock 是 Cargo 自动维护, ADR 0005 sanction; 行数差 = 注释行 + Cargo.lock 增长; **不是隐瞒** |
| 113 tests | 118 tests | +5 tests | 接受 — main.rs 5 unit tests writer 漏算 (派单 Acceptance 仅算集成测试 +3) |
| "800×4=3200 已对齐" | 3200 mod 256 = 128 ≠ 0, padded = 3328 | 数学错 | 接受 — 实装 div_ceil 正确处理, 自报陈述粗; output trace `bytes=1920000` (= 3200 × 600) verify 实装对; 注释 line 1842 仅 1366 例子准 |

**P3 跟进** (不阻塞): writer 提审 self-report 数字应**实跑确认**, 不靠记忆估算. T-0408 自报粗但实装对, 不影响 merge 决策.

### [P3-4] f32 to_ne_bytes 跨平台稳定性 (大端机 vs 小端机) ✅ 接受 little-endian only

**实装** render_headless line 1632-1637:
```rust
cell_vertex_bytes.extend_from_slice(&v[0].to_ne_bytes());
cell_vertex_bytes.extend_from_slice(&v[1].to_ne_bytes());
cell_vertex_bytes.extend_from_slice(&color[0].to_ne_bytes());
...
```

**`to_ne_bytes`** = native endian, 与 host CPU 字节序一致.

**wgpu 期望**: vertex buffer 二进制布局需与 GPU 端 shader struct 一致. wgpu 假设 little-endian (主流 x86/ARM 都 little-endian).

**风险**: 大端机 (PowerPC, SPARC, MIPS BE) 跑 quill 会 vertex 数据错位 → glyph 不可见.

**独立判**: ✅ **接受** — quill 派单明示 "Wayland-only" + 5090 NVIDIA Vulkan, 全是 little-endian 硬件; 大端机不在目标平台. Renderer::draw_cells / draw_frame 也用 to_ne_bytes (T-0305 / T-0403 既有), 一致 idiom.

**P3 跟进** (future): 若未来跨平台 (armv7l BE 等罕见路径), 改 `to_le_bytes` (显式 little-endian) 防意外.

### [P3-5] T-0404 / T-0405 / T-0406 reviewer 必走 --headless-screenshot 自验

**给后续 ticket 的前置**:
- **T-0405 CJK fallback verify**: reviewer 跑 `cargo run --release -- --headless-screenshot=/tmp/cjk_test.png` (term grid 含 "你好abc"), Read PNG 看 CJK 字符真画 ≠ tofu (atlas_count 跨 face); 不再依赖 user 截图
- **T-0406 LRU evict**: reviewer 跑长串 unique chars 触 atlas overflow, Read PNG 看 LRU 后视觉对 + atlas_count 不 explode
- **Phase 5 IME / Phase 6 多 face / Phase 7 配色**: 每个视觉 ticket reviewer 加 PNG verify step (2026-04-25 后 SOP)
- **Future**: scripts/visual_regression.sh 自动化 baseline diff (派单 In #E future, 当前不做)

---

## 给后续 ticket 的前置信息

### 给 Lead (本 ticket P0-NEW rebase 决策)

**当前状态**:
- T-0404 已合 main (`5ee427b` HEAD), T-0408 fork-point `d98795f` (T-0407 合后, T-0404 合前)
- `git merge-tree main feat/T-0408` 实测 src/wl/mod.rs 真冲突 + src/wl/render.rs auto-merge 但语义破 (HiDPI_SCALE 未跟进)

**Lead 决策路径**:
1. **rebase 路径** (推 ✅): 派 writer-T0408 (或 fresh) rebase + HiDPI 适配:
   - src/wl/mod.rs: 两 `pub use` 都保留, HIDPI_SCALE 在前 (T-0404 早合)
   - src/wl/render.rs::render_headless: 引 `HIDPI_SCALE` 让 `width × HIDPI_SCALE` 算 physical surface size + `cell_w_px × HIDPI_SCALE` / `cell_h_px × HIDPI_SCALE` / `BASELINE_Y_PX × HIDPI_SCALE` 算 NDC; 或入参改 logical 在内部 × HIDPI_SCALE
   - 重跑 4 门 + cargo run + Read PNG verify HiDPI 视觉对 (字号双倍 + cell 双倍 + 物理像素 1600×1200 显示)
   - 我可继续接 round 2 审 (token 预算还充足)
2. **Lead 手 squash-merge 路径**: Lead 自己解 conflict + HiDPI 适配 + 强行合; 不走二审
3. **不合, 关闭重派** ❌ (不推, T-0408 实装本身合格, 仅时序问题)

**reviewer 推荐**: 路径 1 (rebase 由 writer 做 + 我二审), 保审计链完整.

### 给 T-0405 (CJK fallback)

- **T-0408 PNG verify 入口可用** (rebase 后): reviewer 跑 `cargo run --release -- --headless-screenshot=/tmp/cjk.png`, term grid 含 "你好abc", Read PNG 看 CJK 真画 + atlas_count 跨 face
- **render_headless cell.bg 路径** (T-0407 D fix 同源) 已工作, CJK 字符在 black cell.bg 上 alpha-blend, 视觉对比稳定
- **不依赖 user 手测** — Phase 4+ 视觉 ticket reviewer 永久收益

### 给 T-0406 (LRU atlas evict)

- **T-0408 atlas overflow 路径** (render_headless line 1683-1693): 实装 `panic` → 改 `Err(anyhow!())` (writer 主动改派单字面 panic → Err, 接受 — Err 让 main.rs 显式 exit code != 0 而非 panic backtrace, 工程上更健壮)
- **T-0406 LRU 落地后**: render_headless 也需同步 LRU 逻辑 (本地 atlas state 加 LRU 队列), 与 Renderer::draw_frame 同源
- **建议 P3-1 抽 helper fn `fn allocate_glyph_in_local_atlas(...)` 让两路径共享 LRU 算法**

### 给 mainline-audit 下次跑 (Phase 4 末)

- INV-010 类型隔离 SOP **第 11 次应用** 零真违规 (T-0302..T-0307 + T-0399 + T-0401..T-0403 + T-0407 + **T-0408 render_headless free fn + headless pipeline 私有 fn**)
- INV-002 字段顺序 14 字段维持 ✅ (T-0408 不挂 Renderer 字段, render_headless 是 free fn + local atlas state)
- TD 状态: T-0408 引 image crate (ADR 0005) 是新 dep, mainline-audit 应登记为 stack 演进 (不进 ADR 0002 锁清单, 仅 ADR 0005 单点)
- Phase 4 进度: T-0401 / T-0402 / T-0403 / T-0407 / T-0404 (已合) + **T-0408 待 rebase 合**, 6/8 (派单数视 ROADMAP 同步)
- **视觉验收 SOP 升级**: 2026-04-25 起所有视觉 ticket reviewer 必跑 `cargo run --release -- --headless-screenshot=<PATH>` + Read PNG 自验, 不再依赖 user 手测截图

---

## 整体评价

T-0408 是 quill **agent 自动视觉验证**的 P1 基建, **跨 8 模块 / 2 commits / 1318-2 行 diff + ADR 0005**, 把 wgpu offscreen render → image::PngEncoder → PNG 文件落盘的链路打通, 让 reviewer 跑 `cargo run --release -- --headless-screenshot=/tmp/x.png` + Read PNG 直接看视觉, 物理替代 user 手测截图. **11 项 P0 派单 scope (A render_headless / B CLI flag / C ADR 0005 / D 3 集成测试 + 5 unit / E 自动化脚本 future) 全落地, 4 门绿 118 tests, INV-010 第 11 次应用零真违规, INV-001..009 全维持**.

**关键架构判断 — render_headless 独立 free fn 路径 (P2-1)**: writer 选不挂 Renderer 字段, INV-002 14 字段顺序零破; 复用 WGSL + 常量 + sRGB helper, pipeline / atlas / 顶点生成 inline (P2-2 接受 inline trade-off). 派单 In #A "render_headless 完全独立于 Renderer struct" 字面落地, INV-002 字段顺序契约维持. Future cleanup 抽 helper fn (P3-1) 留待 T-0405 / T-0406 重构.

**T-0408 自反 deliverable 物理验证 (P0-2 + P0-10)**: 我独立跑 `cargo run --release -- --headless-screenshot=/tmp/reviewer_t0408.png` + Read PNG, 视觉显 浅灰 `[user@userPC ~]$` + 反色块 cursor + 深蓝背景, 与 Lead 此前 Read /tmp/quill_t0408.png 视觉一致. **agent 跑 cargo run + Read PNG 真自验视觉, 物理替代 user 手测截图 — T-0408 ticket goal 物理 PASS**.

**审码 SOP 沿袭**: T-0399 + T-0401..T-0407 5 grep 复跑 / writer 自报偏离逐条 verify / Goal-driven correction 模式继承; 本审码 fresh agent (per-ticket 第 N 棒), 1.5h budget 完成 (Step 0 baseline 1h + Step 1 diff + Step 2 复判 0.3h + Step 3 四门 + cargo run + Read PNG 0.1h + Step 4 报告写作 0.1h + Step 5 Lead 决策 = 1.5h budget 内).

**P0-NEW (rebase 阻塞) 是被动时序问题, 非实装质量问题**: T-0408 fork 期间 T-0404 后到合 main, src/wl/mod.rs 真 conflict + src/wl/render.rs HiDPI_SCALE 语义破 — Lead 决策 rebase 由 writer 做 + 我二审 (推) / Lead 手解 (备选).

**P3 跟进**: 5 项不阻塞 (inline 顶点抽 helper fn / 5% emoji 阈值 / writer self-report 数字粗 / to_ne_bytes 大端机 / T-0405..T-0406 reviewer SOP). 全部 future 性质, 不影响本 merge 决策 (rebase 后).

**Lead 决策**: ⚠️ **条件批准合并** — T-0408 实装本身 ✅, 但 P0-NEW rebase 阻塞需 Lead 决定路径 (rebase 派 writer + 二审 推 / Lead 手解 备选). rebase 后视觉 PNG verify 必跑 + 4 门复绿.

---

## Round 2 (rebase + HIDPI 适配, 2026-04-25)

**触发**: R1 P0-NEW 已被 Lead 选路径 1 (rebase 由 writer 做 + 我接 round 2). writer-T0408 完工 v2 → Lead ping → reviewer-T0408 接审.

**新 commits** (`git -C /home/user/quill-impl-headless log --oneline main..HEAD`):
- `e382540` 主体: headless screenshot (rebase 后重生, +HIDPI 适配)
- `bb8269b` Status claimed → in-review

**diff stat** (`git diff main..HEAD --stat`, 现 main HEAD = `8112d26` T-0404 Status in-review -> merged):
- Cargo.lock +99 (image transitive 不变)
- Cargo.toml +4 (image dep 不变)
- docs/adr/0005-image-crate-png-encoding.md +138 (内容不变)
- src/main.rs +208 (+5 行 vs R1 — caller pattern `let (rgba, physical_w, physical_h) = render_headless(...)?;` + log 加 logical/physical 字段)
- src/wl/mod.rs +6 (`pub use render::render_headless;`, T-0404 已加的 `pub use render::HIDPI_SCALE;` 与 T-0408 行不冲突)
- src/wl/render.rs +689 (+35 行 vs R1 — render_headless 返 tuple + 内部 physical_w/h 推导 + cell/baseline × HIDPI_SCALE + readback 用 physical)
- tasks/T-0408-headless-screenshot.md +1/-1 (Status)
- tests/headless_screenshot.rs +231 (+17 行 vs R1 — 用 PHYSICAL_W/H 常数 + helper 返 tuple)

**合计 8 文件 +1375/-2** (vs R1 +1318, 净 +57 行 = HIDPI 适配 + tuple API). writer self-report 数据精准 ✅ (R1 P3-3 跟进 Lead 已传, writer v2 提审 应自报准).

### Round 2 SOP 7 步逐项 verify

#### Step 1: rebase verify ✅

`git merge-base main HEAD = 8112d26` (= current main HEAD, T-0404 Status commit). feat/T-0408 fork-point 移到 main HEAD, **rebase 完整** ✅.

R1 fork-point `d98795f` (T-0407 合后, T-0404 合前) → R2 fork-point `8112d26` (T-0404 完整合后). 跨 4 commits (`b513bf4` rebase / `808b8f5` Merge T-0404 / `5ee427b` Lead 跟进 / `8112d26` Status), 全部 T-0404 内容已包含.

#### Step 2: diff stat ✅

8 文件不变 (R1 同样 8), +1375 (vs R1 +1318, 净 +57 行 HIDPI 适配). 文件 list 完全一致 (Cargo.lock / Cargo.toml / ADR 0005 / main.rs / wl/mod.rs / wl/render.rs / tasks / tests).

#### Step 3: merge-tree zero conflict ✅

```
$ git -C /home/user/quill-impl-headless merge-tree main HEAD
ae7bd8e6eee61cec4e54ffc18abc345ecbc08901
(无 "冲突" 字样, 仅打印 merged tree hash)
```

R1 时 src/wl/mod.rs 真 conflict + src/wl/render.rs 语义破 → R2 全部消解 ✅.

#### Step 4: 6 HIDPI 适配点逐一 verify (核心 P0)

**实装** `src/wl/render.rs::render_headless` (line 1542-2008) + `src/main.rs::run_headless_screenshot` (line 83-180) + `tests/headless_screenshot.rs` (line 21-214):

| 适配点 | SOP 期望 | 实装行号 | 实装内容 | 验 |
|---|---|---|---|---|
| 1 | surface 物理像素 = `width.saturating_mul(HIDPI_SCALE) / height.saturating_mul(HIDPI_SCALE)` | render.rs:1563-1564 | `let physical_w = width.saturating_mul(HIDPI_SCALE);` / `let physical_h = height.saturating_mul(HIDPI_SCALE);` | ✅ |
| 2 | 离屏 Texture extent = physical_w × physical_h | render.rs:1597-1599 | `size: wgpu::Extent3d { width: physical_w, height: physical_h, depth_or_array_layers: 1, }` | ✅ |
| 3 | cell_w_px = `CELL_W_PX * HIDPI_SCALE as f32` (与 draw_frame 同源) | render.rs:1693-1694 | `let cell_w_px = CELL_W_PX * HIDPI_SCALE as f32;` / `let cell_h_px = CELL_H_PX * HIDPI_SCALE as f32;` | ✅ |
| 4 | BASELINE_Y_PX = `BASELINE_Y_PX * HIDPI_SCALE as f32` | render.rs:1695 + 1837 | `let baseline_y_px = BASELINE_Y_PX * HIDPI_SCALE as f32;` 用于 `(row_idx as f32) * cell_h_px + baseline_y_px - slot.bearing_y as f32` | ✅ |
| 5 | bytes_per_row 256 对齐基于 physical_w | render.rs:1942-1947 | `physical_w.checked_mul(4)?...; physical_w.div_ceil(align) * align ... buffer_size = padded_bytes_per_row × physical_h` | ✅ |
| 6 | readback Vec<u8> 长度 = physical_w × physical_h × 4 | render.rs:1996-1999 | `Vec::with_capacity(unpadded_bytes_per_row × physical_h)`, for row in `0..physical_h` | ✅ |

**全部 6 项命中** ✅ — 与 SOP 一一对应, 与 Renderer::resize / Renderer::draw_frame 同源 (`render.rs:416-417` `physical_w = width.saturating_mul(HIDPI_SCALE)` + `:613-614` `cell_w_px = CELL_W_PX * HIDPI_SCALE as f32`).

**关键 invariant 1 — glyph raster 已是 physical px**: cosmic-text shape_line 用 `Metrics(17.0 × HIDPI_SCALE = 34.0, ...)` (T-0404 src/text/mod.rs:419), rasterize bitmap 也 physical. render_headless 内 `glyph.x_offset / bearing_x / bearing_y / slot.width / slot.height` 都是 physical (注释 line 1834-1836 显式说明) — **不需再乘 HIDPI_SCALE**, 与 cell_h_px / baseline_y_px (logical const × HIDPI_SCALE) 单位对齐. ✅

**关键 invariant 2 — NDC 换算分母用 physical**: `surface_w = physical_w as f32` / `surface_h = physical_h as f32` (line 1691-1692), 与 target_texture extent (physical) 同, NDC `x / surface_w * 2.0 - 1.0` 正确归一化. ✅

#### Step 5: PNG 重测 (核心 deliverable) ✅

**独立跑** `cd /home/user/quill-impl-headless && cargo run --release -- --headless-screenshot=/tmp/reviewer_t0408_v2.png`:

```
INFO quill 进入 headless screenshot 模式 (T-0408)
INFO quill::text TextSystem::new: locked PREFERRED monospace face primary_face=DejaVu Sans Mono
INFO quill::text TextSystem::new: emoji face blacklist populated emoji_face_count=1
INFO quill headless PTY drain 完成 read_bytes=258
INFO quill::wl::render wgpu adapter (headless) 选中 backend=Vulkan name=NVIDIA GeForce RTX 5090
DEBUG quill::wl::render render_headless stats cols=80 rows=24 logical_w=800 logical_h=600
       physical_w=1600 physical_h=1200 cell_vertex_count=90 glyph_vertex_count=90 atlas_count=12
INFO quill headless screenshot 写出 PNG 完成 path=/tmp/reviewer_t0408_v2.png logical_w=800 logical_h=600
       physical_w=1600 physical_h=1200 bytes=7680000
```

**PNG header verify** (`xxd /tmp/reviewer_t0408_v2.png | head -2`):
```
00000000: 8950 4e47 0d0a 1a0a 0000 000d 4948 4452  .PNG........IHDR
00000010: 0000 0640 0000 04b0 0806 0000 00a3 0186  ...@............
```
- `89 50 4e 47 0d 0a 1a 0a` = PNG magic ✅
- `00 00 06 40` = width 0x640 = **1600** ✅ (R1: 0x320 = 800)
- `00 00 04 b0` = height 0x4b0 = **1200** ✅ (R1: 0x258 = 600)
- `08 06` bit depth 8 + RGBA ✅
- bytes = 7680000 = 1600 × 1200 × 4 ✅ (R1: 1920000 = 800 × 600 × 4)

**Read /tmp/reviewer_t0408_v2.png** (Claude multimodal 看 PNG):
- 浅灰 `[user@userPC ~]$` 在左上 (logic px 区域 ~340 × 50, R1 时 ~170 × 25 — 字号物理像素 **2x 放大** 真发生) ✅
- 反色块 cursor 在 `~]` 与 `$` 之间, 块尺寸也 2x ✅
- 大块深蓝 #0a1030 背景 ✅
- 字体仍 DejaVu Sans Mono primary_face (T-0407 锁定不变) ✅
- 视觉与 v1 形状一致 + 字号 2x — **HIDPI 适配真 work** ✅

**trace 数学维持** (T-0407 / R1 同源): cell_vertex_count=90 (15 字符 × 6) + glyph_vertex_count=90 + atlas_count=12 (15 → 12 unique) — 几何不变, 仅像素尺寸 ×2. ✅

**Lead 此前 Read /tmp/quill_t0408_v2.png 实证** (派单 message): "视觉对齐 v1 + 字号物理像素 ×2 真放大" — 我独立 Read /tmp/reviewer_t0408_v2.png 同视觉, 双源 verify ✅.

**T-0408 自反 deliverable 物理验证 PASS — round 2 HIDPI 路径稳定** ✅

#### Step 6: 4 门复跑 ✅

```
$ cargo build (dev): Finished `dev` profile [optimized + debuginfo] target(s) in 0.04s ✅
$ cargo build --release: Finished `release` profile [optimized] target(s) in 34.13s ✅ (rebase 后首次 release 编)
$ cargo clippy --all-targets -- -D warnings: Finished ... in 0.05s ✅ (零 warning)
$ cargo fmt --check: (零输出) ✅
$ cargo test:
   running 81 tests (lib unit) → ok      [+2 vs R1 79: T-0404 加 HIDPI_SCALE u32 + Renderer resize 测试]
   running 5 tests (main.rs unit) → ok
   running 2 tests (frame_stats_integration) → ok
   running 3 tests (frame_stats_smoke) → ok
   running 2 tests (glyph_atlas) → ok
   running 3 tests (headless_screenshot) → ok
   running 2 tests (ls_la_e2e) → ok
   running 2 tests (pty_calloop_smoke) → ok
   running 2 tests (pty_echo) → ok
   running 3 tests (pty_resize) → ok
   running 2 tests (pty_to_term) → ok
   running 2 tests (resize_chain) → ok
   running 11 tests (state_machine) → ok
   running 0 tests (lib doc) → ok
   TOTAL: 81 + 5 + 2 + 3 + 2 + 3 + 2 + 2 + 2 + 3 + 2 + 2 + 11 = 120 tests pass ✅
```

**与 Lead 报告 120 tests (= 118 R1 baseline + 2 T-0404) 完全一致** ✅. 4 门全绿.

#### Step 7: P3 数据精准化 verify ✅

| R1 P3-3 跟进项 | R2 实测 | 精准化 ✓/✗ |
|---|---|---|
| writer 自报 113 tests | R2 应报 120 (实测 120, Lead message 也报 120) | ✅ 准 |
| writer 自报 7 文件 +965 | R2 应报 8 文件 +1375 (实测 8 文件 +1375) | ✅ 准 |
| writer 自报 "800×4 已对齐" 数学错 | R2 注释 line 1939 改 "physical 1600 × 4 = 6400, 已 256 对齐" — 6400 / 256 = 25 真整除 ✅ | ✅ 准 |

**writer 主动告知数据精准化 done** (Lead message), 我 verify 全部对 ✅.

### Round 2 P0 / P1 / P2 / P3

#### [R2-P0-1] API 签名变化 `Result<Vec<u8>>` → `Result<(Vec<u8>, u32, u32)>` ✅✅

**变化** (render.rs:1542-1550):
```rust
pub fn render_headless(
    text_system: &mut TextSystem,
    cells: &[CellRef],
    cols: usize,
    rows: usize,
    row_texts: &[String],
    width: u32,        ← logical
    height: u32,       ← logical
) -> Result<(Vec<u8>, u32, u32)>    ← (rgba, physical_w, physical_h)
```

**writer 论据** (render.rs:1526-1531 doc):
> **why 返 tuple 不只 Vec<u8>** (T-0404 适配): physical 尺寸是 HIDPI_SCALE × logical, 调用方写 PNG 需要这两值。返 tuple 让调用方不依赖 `crate::wl::HIDPI_SCALE` 常量推导, decoupling 清晰。`(width, height)` 入参单位变化 (logical) 不破坏 caller — 之前没用 `(width, height)` 写 PNG (旧 API 直接用 width 写) 的 caller 现在编译失败 (返回类型变), 显式提示迁移

**独立评估**:

| 路径 | 优 | 劣 |
|---|---|---|
| **A. tuple `(Vec<u8>, u32, u32)` (writer 选)** | caller 不 import HIDPI_SCALE 也拿 physical 尺寸; API 自描述 "physical 是输出"; 编译期 catch 旧 caller | tuple 字段无名 (rgba/w/h 顺序靠位置, magic 易错) |
| B. struct `RenderHeadlessResult { rgba: Vec<u8>, physical_w: u32, physical_h: u32 }` | 字段有名, IDE auto-complete 友好 | 引一新 pub type, 命名 / 文档 / 测试增 ~20 行; Phase 4 有点 over-engineer |
| C. caller 自算 `physical_w = logical_w * HIDPI_SCALE` | API 不变 (Vec<u8>) | caller 必依赖 HIDPI_SCALE 常量, 紧耦合; HIDPI_SCALE 变化时所有 caller 跟改 |

**独立判**: ✅ **接受 tuple 路径** — Phase 4 单 caller (main.rs) + 单 test helper, tuple 顺序 magic 风险低; future Phase 5+ 多 caller 时 promote struct (P3 跟进, 不阻塞). caller 解构 `let (rgba, physical_w, physical_h) = ...?;` 三字段名清晰, 等价于 struct 字段名. **写码 doc line 1526-1531 解释清晰**, 路径合理.

**main.rs caller verify** (main.rs:137-168):
```rust
let (rgba, physical_w, physical_h) = render_headless(...)?;
...
encoder.write_image(&rgba, physical_w, physical_h, ExtendedColorType::Rgba8)?;
tracing::info!(... logical_w = WIDTH, logical_h = HEIGHT, physical_w, physical_h, bytes = rgba.len(), ...);
```
✅ caller pattern 干净, PNG header 用 physical 尺寸 (与 wgpu Texture 实尺寸一致).

**tests/headless_screenshot.rs verify** (line 21-91):
```rust
use quill::wl::{render_headless, HIDPI_SCALE};
const LOGICAL_W: u32 = 800;
const LOGICAL_H: u32 = 600;
const PHYSICAL_W: u32 = LOGICAL_W * HIDPI_SCALE;
const PHYSICAL_H: u32 = LOGICAL_H * HIDPI_SCALE;
...
let (rgba, physical_w, physical_h) = render_headless_with_shell_prompt();
assert_eq!(physical_w, PHYSICAL_W);
assert_eq!(physical_h, PHYSICAL_H);
assert_eq!(rgba.len(), (PHYSICAL_W as usize) * (PHYSICAL_H as usize) * 4, ...);
```
✅ 测试显式锁 `physical = logical × HIDPI_SCALE` 契约, T-0405 / T-0406 改 HIDPI_SCALE 时测试自跟随.

#### [R2-P0-2] cell_h_px 同改 (writer 也 ×= HIDPI_SCALE, 与 Renderer::draw_frame 同源) ✅

R1 我 P0-NEW 列 "cell_w_px / cell_h_px / BASELINE_Y_PX 全 × HIDPI_SCALE" — R2 实测全部命中 (Step 4 详).

**关键 cross-check**: Renderer::draw_frame (line 952-954):
```rust
let cell_w_px = CELL_W_PX * HIDPI_SCALE as f32;
let cell_h_px = CELL_H_PX * HIDPI_SCALE as f32;
let baseline_y_px = BASELINE_Y_PX * HIDPI_SCALE as f32;
```

vs render_headless (line 1693-1695): **三行字面 100% 同源** ✅.

#### [R2-P0-3] INV-010 类型隔离第 11 次应用维持 ✅

R2 grep 复跑:
```
$ grep -rnE 'pub use (alacritty|wgpu|wayland|cosmic)' src/    → 零命中 ✅
$ grep -rnE 'impl From<(alacritty|wgpu|wayland|cosmic)' src/  → 零命中 ✅
$ grep -rnE 'cosmic_text' src/ | grep -v src/text/mod.rs      → 零命中 ✅
```

**新公共 API** (R2 加):
- `pub fn render_headless(...) -> Result<(Vec<u8>, u32, u32)>` — 全 quill / std 类型, 零 wgpu / cosmic-text ✅
- 入参 `text_system: &mut TextSystem` (quill), `cells: &[CellRef]` (quill), 其余 std (usize / &[String] / u32) ✅
- 返 tuple `(Vec<u8>, u32, u32)` 全 std ✅

**已 pub 的 HIDPI_SCALE** (T-0404 加, 不是 T-0408): `pub const HIDPI_SCALE: u32 = 2;` — std u32 类型, INV-010 不适用 (上游 wgpu / cosmic-text 不暴露). T-0408 tests 用 `quill::wl::HIDPI_SCALE` 推导 PHYSICAL_W/H, 是合规消费.

**INV-010 第 11 次应用维持** ✅ (R1 + R2 同).

#### [R2-P0-4] INV-002 字段顺序维持 ✅

R2 不动 Renderer struct (`grep -nE "^pub struct Renderer" src/wl/render.rs:62` + 14 字段链不变), 不动 GlyphAtlas struct (内部字段顺序). render_headless 仍 free fn, 不挂 Renderer 字段. ✅

#### [R2-P1] 生产零 unwrap / expect / println / unsafe / thread::spawn ✅

```
$ grep -nE 'unwrap\(\)|\.expect\(|println!|thread::spawn' src/wl/render.rs src/main.rs src/wl/mod.rs
src/main.rs:182: .expect("no arg should be Ok(None)") (#[cfg(test)] mod tests)
src/main.rs:193: .expect("with valid flag should parse") (#[cfg(test)] mod tests)
src/main.rs:194: .expect("should be Some") (#[cfg(test)] mod tests)
src/main.rs:221: .expect("unrelated flags should not error") (#[cfg(test)] mod tests)
```

R2 改动**零新 unwrap / expect 在生产路径**, 仅 #[cfg(test)] mod tests 内 (5 unit tests, conventions §3 sanction). ✅

R2 改动**零新 unsafe** ✅, 既存 unsafe (Renderer::new T-0102) 不动.

R2 改动**零新 println** ✅, tracing::debug! / info! 注释行加 logical_w / logical_h / physical_w / physical_h 字段 (line 1898-1899) 是合规 trace.

R2 改动**零新 std::thread::spawn** ✅. main.rs:107 + tests/headless_screenshot.rs:43 既有 std::thread::sleep (派单 In #B sanction) 不变.

#### [R2-P2-1] 测试用 LOGICAL_W / PHYSICAL_W 双常数 vs 单 const ✅ writer 选双常数接受

**实装** tests/headless_screenshot.rs:25-28:
```rust
const LOGICAL_W: u32 = 800;
const LOGICAL_H: u32 = 600;
const PHYSICAL_W: u32 = LOGICAL_W * HIDPI_SCALE;
const PHYSICAL_H: u32 = LOGICAL_H * HIDPI_SCALE;
```

**评估**: 双常数显式 `physical = logical × HIDPI_SCALE` 契约, 测试断言两套用 (`assert_eq!(physical_w, PHYSICAL_W)` + `rgba.len() == PHYSICAL_W × PHYSICAL_H × 4`). HIDPI_SCALE 改 (Phase 5+ DPI awareness) 时测试自跟随. ✅

替代: 只存 PHYSICAL 常数 + 注释 "= logical × 2" — 失去编译期 link, 改 HIDPI_SCALE 时手忘改. 双常数路径**更稳** ✅.

#### [R2-P2-2] R1 P3-1 (inline 顶点抽 helper fn) 仍未做 — 接受 future 跟进 ✅

writer R2 未抽 free fn helper (`build_cell_vertex_bytes_for_target` / `build_glyph_vertex_bytes_inline`), inline 仍 ~150 行. 派单 budget 100k 已被 HIDPI 适配 + tuple API + ADR 0005 + 5 unit + 3 集成测试吃掉, 不强求 R2 顺手抽. T-0405 / T-0406 重构再考虑. ✅

#### [R2-P3-1] tracing log 加 logical_w / logical_h / physical_w / physical_h 字段 (info / debug) ✅

`tracing::info!(... logical_w=800, logical_h=600, physical_w=1600, physical_h=1200, bytes=7680000, "headless screenshot 写出 PNG 完成")` (main.rs:160-167) + `tracing::debug!(... logical_w=width, logical_h=height, physical_w, physical_h, ...)` (render.rs:1895-1903).

**好**: 排障时直接看 logical/physical 对照, 防 "physical_w 是 800 还是 1600" 之类困惑. R1 时 trace 仅 width/height, R2 显式区分. **建议沿袭** Phase 5+ 加更多 DPI 路径时.

#### [R2-P3-2] log INFO "headless screenshot 写出 PNG 完成" 重复 width / logical_w 字段 ⚠️ 微跟进

main.rs:160-167:
```rust
tracing::info!(
    path = %path.display(),
    logical_w = WIDTH,        ← const WIDTH = 800
    logical_h = HEIGHT,
    physical_w,
    physical_h,
    bytes = rgba.len(),
    "headless screenshot 写出 PNG 完成"
);
```

**字段名**: `logical_w` / `logical_h` 显式; const `WIDTH` / `HEIGHT` 在 main.rs:93-94 仍叫 `WIDTH = 800` / `HEIGHT = 600` (未改 LOGICAL_WIDTH). **微不一致** — log 字段叫 logical_w 但 const 叫 WIDTH.

**建议** (P3, 不阻塞 future cleanup): main.rs:93-94 const 改 `LOGICAL_WIDTH` / `LOGICAL_HEIGHT` 与 log 字段名一致, 避新读者疑惑.

**不阻塞** — log 字段 logical_w 已显式, const 注释行 line 69-72 已说 "800 × 600 hardcode" 在 logical 维度 (与 INITIAL_WIDTH × INITIAL_HEIGHT 同 logical), 上下文足够清晰.

### Round 2 整体评价

T-0408 round 2 (rebase + HIDPI 适配) **全部 7 SOP 步骤通过**, **6 HIDPI 适配点 1:1 命中 SOP 期望**, **API 签名 tuple 化 decoupling 清晰**, **PNG 物理 verify 1600×1200 字号 ×2 视觉真放大**, **120 tests pass / 4 门绿 / INV-010 + INV-002 维持**.

**关键架构判断 — `Result<(Vec<u8>, u32, u32)>` tuple API (R2-P0-1)**: writer 选 tuple 让 caller 不依赖 HIDPI_SCALE 常量推导 — 设计正确. main.rs caller 解构 `let (rgba, physical_w, physical_h) = render_headless(...)?;` + tests 用 `assert_eq!(physical_w, PHYSICAL_W)` 显式锁契约. Phase 5+ 多 caller 时 promote struct (P3 跟进).

**视觉物理 verify (Step 5)**: 我独立 Read /tmp/reviewer_t0408_v2.png 1600×1200 PNG, 视觉显 浅灰 `[user@userPC ~]$` (字号 ×2 vs v1) + 反色块 cursor (块尺寸 ×2) + 深蓝背景 — **HIDPI 适配真 work**, T-0404 + T-0407 + T-0408 三方组合 visual 稳定. Lead 此前 Read /tmp/quill_t0408_v2.png 同视觉, 双源 verify.

**writer 数据精准化 (Step 7)**: R1 P3-3 跟进 3 项 (113→120 / 7→8 / "800×4 已对齐"数学错改 "1600×4=6400 真对齐") R2 全部精准 ✅. writer self-report 质量提升.

**审码 SOP 沿袭**: per-ticket fresh agent round 2 接审, 1.5h budget round 2 段实际 ~30 分钟 (rebase verify + HIDPI 6 项 1:1 cross-check + PNG 重测 + 4 门 + 报告写作), 远低于 budget. 审码迭代效率高.

**Round 2 决策**: ✅ **批准合并** (R1 条件 P0-NEW 已解, 6 HIDPI 适配点全过, 视觉 PNG verify pass, 4 门绿 120 tests).

### Round 2 给后续 ticket 的前置信息

#### 给 T-0405 (CJK fallback verify)

- **render_headless API 已稳** (R2 后): caller pattern `let (rgba, physical_w, physical_h) = render_headless(text_system, cells, cols, rows, row_texts, 800, 600)?;`, 自动 ×2 出 1600×1200 PNG
- **HIDPI 路径已工作**: T-0405 加 CJK 字符 (`shape "你好abc"`) 不需重适配, render_headless / shape_line / rasterize 全在 physical px 链路自动跟随
- **建议 T-0405 reviewer SOP**: 用 `cargo run --release -- --headless-screenshot=/tmp/cjk_test.png` (term grid 含 CJK), Read PNG 看中文字符真画 + 字号也 ×2 (HIDPI 一致)
- **不需要做** (T-0408 R2 已覆盖): physical 适配 / tuple API / 测试 helper

#### 给 T-0406 (LRU atlas evict)

- **render_headless atlas 路径**: `panic` → `Err(anyhow!())` (R1 写码主动改, R2 维持) — main.rs 显式 exit code != 0 而非 panic backtrace, 工程上更健壮
- **T-0406 LRU 落地**: render_headless 也需同步 LRU (本地 atlas state 加 LRU 队列), 与 Renderer::draw_frame 同源
- **R1 P3-1 跟进 (inline 顶点抽 helper)** 仍未做, T-0406 是抽 free fn `fn build_cell_vertex_bytes_for_target` / `fn allocate_glyph_in_local_atlas_with_lru` 的自然契机

#### 给 mainline-audit 下次跑 (Phase 4 末)

- INV-010 类型隔离 SOP **第 11 次应用** R1 + R2 维持零真违规
- INV-002 字段顺序 14 字段维持 (T-0408 R2 不挂 Renderer 字段)
- ADR 0005 引 image crate (R1 + R2 同, 不进 ADR 0002 锁清单)
- Phase 4 进度: T-0401 / T-0402 / T-0403 / T-0407 / T-0404 (已合) + **T-0408 (R2 后批准合)**, **6/8** (派单数视 ROADMAP 同步, T-0405 + T-0406 待)
- **视觉验收 SOP 升级**: 2026-04-25 起所有视觉 ticket reviewer 必跑 `cargo run --release -- --headless-screenshot=<PATH>` + Read PNG 自验, 不再依赖 user 手测截图 (T-0408 R2 后正式生效)
- **writer self-report 数据精准化要求**: R1 P3-3 / R2 Step 7 教训 — writer 提审 self-report 数字应实跑确认, 不靠记忆估算 (113 vs 118 / 800×4 数学错). 将此条加入 conventions §1 "Body 测试 + diff stat" 的硬约束 (Lead 决定是否补 conventions.md).
