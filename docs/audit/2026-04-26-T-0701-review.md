# T-0701 审码报告 (窗口 4 边 + 4 角 resize, Phase 7 P1 CSD 完整化)

**reviewer**: reviewer-T0701
**date**: 2026-04-26
**branch**: feat/T-0701
**fork-point**: 62cb8b8 (T-0701/0702/0703 派单 commit)
**HEAD**: 3bdf7f6
**diff stat (vs fork-point)**: 2 src + 1 task = +350 / -18

## 评级

**全绿放行 (P0/P1/P2 全过, 仅 1 条 P3 备注)**.

## commits

- `cea7448` T-0701: claim
- `c7025ac` T-0701: 实装 (348 行 src 改动)
- `3bdf7f6` T-0701: Status claimed -> in-review

## 4 门 release

| 门 | 状态 | 备注 |
|---|---|---|
| `cargo build --release` | OK | finished, no warnings |
| `cargo fmt --check` | OK | exit=0 |
| `cargo clippy --all-targets --release -- -D warnings` | OK | finished, 0 warnings |
| `cargo test --release --lib` | OK | **197 passed**, 0 failed (+6 vs main 191; 实际 pointer.rs +14 测试, fork-point 比 main 少 8 个 T-0604 render tests, 不归 T-0701) |
| `cargo test --release --all-targets` integration | OK | 11 + 3 + 2 + 2 + 2 = 20 passed |

## 审查项逐条

### 1. ResizeEdge enum (8 variant 含 corner)

`src/wl/pointer.rs:96-105` 8 variant 全:
`Top / Bottom / Left / Right / TopLeft / TopRight / BottomLeft / BottomRight`. 不含 None
(注释明确说明 wayland 协议 None=0 仅作 default value, 客户端不发). **PASS**.

### 2. quill_edge_to_wayland INV-010 单一翻译边界

`src/wl/pointer.rs:116-130`:
- 函数签名返回 `wayland_protocols::xdg::shell::client::xdg_toplevel::ResizeEdge`
- 内部 `use wayland::ResizeEdge as WlEdge` **局部 use, 不出此 fn**
- exhaustive match 8 variant **无 `_ =>` arm**, 上游加 variant 编译期 catch
- `pub(crate)` visibility, 不出 crate

**全 codebase grep 验证 INV-010 单一翻译边界**:

```
$ grep -rn "wayland_protocols.*ResizeEdge\|xdg_toplevel::ResizeEdge\|wayland.*ResizeEdge" src/
src/wl/pointer.rs:90  (注释)
src/wl/pointer.rs:94  (注释)
src/wl/pointer.rs:107 (注释)
src/wl/pointer.rs:118 (fn 签名)
src/wl/pointer.rs:119 (use)
src/wl/pointer.rs:1071+ (单测内 use, 同模块允许)
```

`src/wl/window.rs` **零** wayland::ResizeEdge import — Dispatch<WlPointer> 通过
`quill_edge_to_wayland(edge)` 返回值类型隐式拿 wayland enum, 与 `from_alacritty`
模块私有 inherent fn 同套路. **PASS**.

### 3. hit_test 优先级 (边界外 > 4 角 > 4 边 > 三按钮 > titlebar > textarea)

`src/wl/pointer.rs:502-584` `hit_test` fn 优先级:
1. `x<0 || y<0` → None (line 504)
2. `x>=w || y>=h` → None (line 509)
3. **4 角** ([0,8)×[0,8) / [w-8,w)×[0,8) / [0,8)×[h-8,h) / [w-8,w)×[h-8,h)) → ResizeEdge corner (line 521-532)
4. **4 边** (y<4 / y>=h-4 / x<4 / x>=w-4) → ResizeEdge edge (line 536-547)
5. textarea 段 (y >= titlebar_h) → TextArea (line 554)
6. titlebar 段 (y < button_h):
   - Close [w-24, w) → Button(Close)
   - Maximize [w-48, w-24) → Button(Maximize)
   - Minimize [w-72, w-48) → Button(Minimize) (含 `min_x_min >= 0.0` 防越界)
7. 否则 → TitleBar (line 583, fallback)

**优先级硬约束**: 角覆盖优先 edge ✓; edge 顶 4 px 优先 titlebar drag (派单 In #A 顶边走 Top resize 而非 titlebar) ✓; corner 与右上 Close 重叠时 corner 赢 (单测 `hit_test_corner_overlaps_close_button_corner_wins` 验证) ✓. **PASS**.

### 4. RESIZE_EDGE_PX = 4 / RESIZE_CORNER_PX = 8 logical

`src/wl/pointer.rs:247` `RESIZE_EDGE_PX: f64 = 4.0` (派单要求 4) ✓
`src/wl/pointer.rs:256` `RESIZE_CORNER_PX: f64 = 8.0` (派单要求 8) ✓
均 `pub(crate)`, 注释引派单 + GNOME/KDE 实测对齐. **PASS**.

### 5. Dispatch<WlPointer> StartResize 路径

`src/wl/window.rs:2190-2213`:
- 解构 `serial` + `edge` 两字段
- `pointer_seat.as_ref()` 防御 None (与 StartMove 同套路, line 2176-2182)
- None 时 `tracing::warn!` + `return`, 不 panic
- `quill_edge_to_wayland(edge)` 翻译, **本文件不 import wayland ResizeEdge**
- `state.window.resize(seat, serial, wl_edge)` 调 SCTK 0.19 Window::resize
  (签名 `(&self, &WlSeat, u32, xdg_toplevel::ResizeEdge)` 已核对 SCTK 源)
- 注释引 INV-006 (resize 中 configure event 走既定 dirty 路径) + INV-010

**PASS**.

### 6. 派单 Out 全守

```
$ git diff 62cb8b8..HEAD --name-only
src/wl/pointer.rs
src/wl/window.rs
tasks/T-0701-window-resize-edges-corners.md
```

- `src/wl/render.rs`: 0 diff ✓
- `src/text/`: 0 diff ✓
- `src/pty/`: 0 diff ✓
- `docs/invariants.md`: 0 diff ✓
- `Cargo.toml` / `Cargo.lock`: 0 diff (无新 crate) ✓

**PASS**.

### 7. INV-001..010 全维持

- INV-001 (State 字段顺序): window.rs 仅在 `Dispatch<WlPointer>` impl 内加 match arm, 不动 `struct State` 字段顺序 ✓
- INV-002 (Renderer 字段顺序): 不动 render.rs ✓
- INV-003 (Renderer::new 合同): 不动 ✓
- INV-004 (deny unsafe_code): 无新 unsafe block ✓
- INV-005 (calloop 唯一 IO 调度器): 不引线程 / 不动 calloop 注册 ✓
- INV-006 (resize_dirty 消费合同): 注释引该不变量, 说明 compositor 接管 resize 走既定 configure → dirty 路径 ✓
- INV-007 (WindowCore 状态机纯逻辑): 不动 WindowCore / handle_event ✓
- INV-008 (PtyHandle drop 顺序): 不动 ✓
- INV-009 (PTY O_NONBLOCK): 不动 ✓
- **INV-010 (类型隔离, 关键)**: ResizeEdge 自定 quill enum + 模块私有 quill_edge_to_wayland inherent fn + exhaustive match 无 `_` + window.rs 零 wayland::ResizeEdge import → **完美守住, 与 conventions §5 / 历史 6 ticket 0 违规一致**

**PASS**.

### 8. T-0504 时代 2 老测试坐标更新

`src/wl/pointer.rs` 测试 diff 显示 2 个老测试更名 + 坐标调整:

| 老名 | 新名 | 调整 |
|---|---|---|
| `hit_test_close_right_edge_inclusive` | `hit_test_close_button_inner_returns_close` | x=799 y=0 → x=790 y=12 (避开 4 边 / 8 角, 落 Close 内点) |
| `hit_test_extremely_narrow_surface_button_overflow_falls_to_titlebar` | `hit_test_narrow_surface_button_overflow_falls_to_titlebar` | 50×600 x=0 y=10 → 100×600 x=10 y=10 (出 corner / edge 后落 TitleBar, 同语义) |

两个测试**语义未变** (Close 区落 Close / 窄 surface fallback titlebar), 仅迁移坐标避开 T-0701 新增的 4 边 8 角优先级带, 注释明确标 "**T-0701 优先级修正**" 说明原因. 派单 In #A "行为变化在范围内" 允许此类调整. **PASS, accept**.

### 9. 单测覆盖 (派单 In #D)

T-0701 新增 14 个 #[test] (净 +6 lib total, 因 fork-point 缺 8 个 T-0604 render tests):

- 4 角 hit_test 各 1 个: TopLeft / TopRight / BottomLeft / BottomRight
- 4 边 hit_test 各 1 个: Top / Bottom / Left / Right
- 邻接验证 3 个: corner 覆盖 Close 优先 / 顶边 4 px 优先按钮 / 离开顶边落 Close
- StartResize 决策 2 个: edge 触发 + corner 触发
- quill_edge_to_wayland 8 variant 全表 1 个
- (派单可省的集成测略)

派单要求 ≥10 测试, **实际 14 ≥ 10, PASS**.

## P3 备注 (不阻 merge)

**P3-1** (注释精度): `src/wl/pointer.rs:227-234` `last_button_serial` 字段
`#[allow(dead_code)]` 注释提到 "Phase 6+ show_window_menu / resize 路径预备", 但
T-0701 起 resize 路径**已实装**且**直接用 button event 自带 serial** (与 StartMove
同套路), 不读此字段. 注释中 "resize 路径" 描述应改为 "show_window_menu 路径
预备", 否则后人误以为 resize 走 last_button_serial. 不阻 merge — 字段保留作
forward-compat hook 决策本身正确, 仅注释精度问题, 可于后续 ticket 顺手清.

## 总结

T-0701 实装质量高:
- ResizeEdge 与 wayland 协议 enum 一一对应, INV-010 类型隔离**模范实施** (与 from_alacritty 同水准)
- hit_test 优先级清晰, 6 层规则, 单测覆盖每层 + 邻接边界
- StartResize Dispatch 套路与 StartMove 完全对仗, 防御 + tracing + 翻译三段齐
- 4 门全绿, 0 clippy warning, 14 新测试全绿
- 派单 Out 边界严守, 无 scope creep
- 老测试坐标更新主动告知 + 注释说明, 透明度高

**Approve, 可 merge**. 手测 deliverable (cargo run --release + 拖窗口 4 边 8 角)
留 Lead 验证.
